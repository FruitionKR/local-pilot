openapi: 3.1.0
info:
  contact:
    email: wonbb3313@gmail.com
    name: Fruition
  description: Fruition PoC 백엔드 API
  title: Fruition API
  version: 0.0.1
security:
- bearerAuth: []
tags:
- description: 선택 가능한 AI 모델 카탈로그와 워크스페이스 모델 설정 API
  name: AI Models
- description: AI가 문서·Wiki를 바꾼 이력을 조회하고 복구를 미리 확인합니다.
  name: AI Operation Logs
- description: 워크스페이스 문맥에서 Agent 실행을 관리하는 API
  name: Agent
- description: 채팅 세션 및 메시지 기록 API
  name: Chat Sessions
- description: Markdown 문서 이미지 asset 조회 API
  name: Document Assets
- description: 문서 편집 잠금(활성 편집 추적) API
  name: Document Edit Lock
- description: Markdown 편집 문서와 업로드 원본 관리 API
  name: Documents
- description: 문서 위치와 폴더 배치 관리 API
  name: Documents
- description: "워크스페이스 문서 폴더의 생성, 이동, 삭제 및 복구 API"
  name: Folders
- description: 워크스페이스 문서·폴더 계층 탐색 API
  name: Navigation
- description: 워크스페이스 문서와 폴더 계층 탐색 API
  name: Navigation
- description: Wiki 기반 자연어 질의 API
  name: Query
- description: 비동기 질의 실행 상태와 실시간 진행 이벤트 API
  name: Query Runs
- description: "재사용 가능한 Agent Skill 작성, 게시 및 실행 설정 API"
  name: Skills
- description: Wiki 그래프 및 페이지 조회 API
  name: Wiki
- description: Wiki 정합성 검사와 유지보수 실행 API
  name: Wiki Maintenance
- description: "Wiki 생성 규칙의 미리보기, 초안 저장 및 활성화 API"
  name: Wiki Schema
paths:
  /api/ai-models:
    get:
      description: 선택할 수 있는 provider/model 조합을 반환합니다. API key는 노출하지 않습니다.
      operationId: list_4
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ModelsResponse"
          description: 조회 성공
      summary: AI 모델 카탈로그 조회
      tags:
      - AI Models
  /api/query/runs/{requestId}:
    get:
      description: 비동기 질의의 현재 상태와 완료 결과 또는 오류 정보를 반환합니다.
      operationId: getRun_1
      parameters:
      - description: 비동기 질의 요청 ID
        in: path
        name: requestId
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/QueryRunStatusResponse"
          description: 상태 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 질의 실행 또는 워크스페이스를 찾을 수 없음
      summary: 질의 실행 상태 조회
      tags:
      - Query Runs
  /api/query/runs/{requestId}/events:
    get:
      description: 비동기 질의의 진행 상황과 최종 결과를 Server-Sent Events로 전달합니다.
      operationId: subscribe
      parameters:
      - description: 비동기 질의 요청 ID
        in: path
        name: requestId
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            text/event-stream:
              schema:
                type: string
          description: SSE 구독 시작
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 질의 실행 또는 워크스페이스를 찾을 수 없음
      summary: 질의 진행 이벤트 구독
      tags:
      - Query Runs
  /api/workspaces/{workspace_id}/agent/runs/{run_id}:
    get:
      description: 자율 AgentRun 계획과 실행 상태를 조회합니다.
      operationId: getRun
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: run_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/JsonNode"
          description: OK
      summary: AgentRun 조회
      tags:
      - Agent
  /api/workspaces/{workspace_id}/agent/runs/{run_id}/approve:
    post:
      description: 현재 AgentRun 계획을 승인합니다.
      operationId: approve
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: run_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AgentRunApproveRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/JsonNode"
          description: OK
      summary: AgentRun 승인
      tags:
      - Agent
  /api/workspaces/{workspace_id}/agent/runs/{run_id}/cancel:
    post:
      description: 현재 AgentRun을 취소합니다.
      operationId: cancel
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: run_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/JsonNode"
          description: OK
      summary: AgentRun 취소
      tags:
      - Agent
  /api/workspaces/{workspace_id}/agent/runs/{run_id}/reject:
    post:
      description: 현재 AgentRun 계획을 거절합니다.
      operationId: reject
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: run_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/JsonNode"
          description: OK
      summary: AgentRun 거절
      tags:
      - Agent
  /api/workspaces/{workspace_id}/agent/runs/{run_id}/revise:
    post:
      description: 현재 AgentRun에 새 계획을 요청합니다.
      operationId: revise
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: run_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AgentRunReviseRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/JsonNode"
          description: OK
      summary: AgentRun 계획 수정
      tags:
      - Agent
  /api/workspaces/{workspace_id}/agent/turn:
    post:
      description: 사용자 요청을 비동기 Agent 실행 대기열에 등록합니다.
      operationId: turn
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AgentTurnRequest"
        required: true
      responses:
        "202":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/AgentTurnResponse"
          description: Agent 실행이 대기열에 등록됨
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 잘못된 요청
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 또는 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 version 충돌
        "423":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 다른 사용자가 문서를 편집 중
      summary: Agent turn 실행
      tags:
      - Agent
  /api/workspaces/{workspace_id}/agent/turn/{run_id}:
    get:
      description: 워크스페이스의 Agent 실행 결과를 조회합니다.
      operationId: getTurn
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 조회할 Agent 실행 ID
        in: path
        name: run_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/AgentTurnResponse"
          description: 결과 조회 성공
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Agent run ID 형식이 올바르지 않음
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 실행 또는 워크스페이스를 찾을 수 없음
        "503":
          content:
            application/json:
              schema:
                oneOf:
                - $ref: "#/components/schemas/JsonNode"
                - $ref: "#/components/schemas/ErrorResponse"
          description: Agent 상태 파이프라인 사용 불가
      summary: Agent turn 결과 조회
      tags:
      - Agent
  /api/workspaces/{workspace_id}/agent/turn/{run_id}/events:
    get:
      description: "Agent turn의 진행 상황과 최종 결과를 Server-Sent Events로 전달합니다. AI가 질의로 판정한 턴만 단계 이벤트를 내며, 편집·Skill 갈래는 완료 이벤트만 옵니다."
      operationId: subscribeTurnEvents
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 구독할 Agent 실행 ID
        in: path
        name: run_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            text/event-stream:
              schema:
                type: string
          description: SSE 구독 시작
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Agent run ID 형식이 올바르지 않음
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 실행 또는 워크스페이스를 찾을 수 없음
      summary: Agent turn 진행 이벤트 구독
      tags:
      - Agent
  /api/workspaces/{workspace_id}/ai-model-settings:
    get:
      description: ingest·lint 작업에 쓰는 provider/model 설정을 반환합니다. OWNER와 MEMBER 모두 조회할 수 있습니다.
      operationId: get
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SettingsResponse"
          description: 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
      summary: 워크스페이스 AI 모델 설정 조회
      tags:
      - AI Models
    put:
      description: "ingest·lint에 쓸 provider/model을 바꿉니다. OWNER만 호출할 수 있고, 활성 model catalog에 있는 조합만 허용합니다."
      operationId: update
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SettingsRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SettingsResponse"
          description: 변경 성공
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: catalog에 없는 provider/model 조합
        "403":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: OWNER가 아님
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
      summary: 워크스페이스 AI 모델 설정 변경
      tags:
      - AI Models
  /api/workspaces/{workspace_id}/ai-operation-logs:
    get:
      description: "최신순으로 반환합니다. 문서 편집은 실제 변경에 성공한 작업만 포함하며, 로그 테이블만 읽고 diff를 계산하지 않습니다."
      operationId: list_3
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 작업 유형
        example: ingest
        in: query
        name: type
        required: false
        schema:
          type: string
      - description: 상태
        example: succeeded
        in: query
        name: status
        required: false
        schema:
          type: string
      - description: 이전 응답의 next_cursor
        in: query
        name: cursor
        required: false
        schema:
          type: string
      - description: "페이지 크기. 기본 20, 최대 100"
        example: 20
        in: query
        name: size
        required: false
        schema:
          type: integer
          format: int32
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/OperationLogListResponse"
          description: 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
      summary: AI 작업 로그 목록
      tags:
      - AI Operation Logs
  /api/workspaces/{workspace_id}/ai-operation-logs/{operation_id}:
    get:
      description: 그 작업이 바꾼 리소스를 함께 반환합니다. 줄 수는 저장된 값이라 계산이 없습니다.
      operationId: detail
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 작업 식별자
        example: op_a2_7f3c9
        in: path
        name: operation_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/OperationLogDetailResponse"
          description: 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 작업 또는 워크스페이스를 찾을 수 없음
      summary: AI 작업 로그 상세
      tags:
      - AI Operation Logs
  /api/workspaces/{workspace_id}/ai-operation-logs/{operation_id}/restore:
    post:
      description: "복구 대상에 따라 처리 방식이 다릅니다. 문서 편집 복구는 즉시 완료되어 200을 반환하고, Wiki 복구는 queued 상태로 등록되어 202를 반환합니다. 미리보기와 같은 계산을 다시 하고 Wiki에 반영합니다. 받치는 기여가 남지 않은 페이지는 삭제하고, 되돌릴 버전이 그대로 있는 페이지는 그 내용으로 복원하며, 남은 조각을 합쳐야 하는 페이지는 llmPipeline에 재작성을 맡깁니다. 재작성이 있으면 status가 rebuilding으로 돌아오며 결과는 로그 상세로 확인합니다. ingest 되돌리기는 Wiki만 되돌리고 원문 문서는 건드리지 않습니다."
      operationId: restore_2
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: operation_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RestoreExecuteRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/RestoreExecuteResponse"
          description: 문서 편집 복구 즉시 완료
        "202":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/RestoreExecuteResponse"
          description: Wiki 복구 queued 등록
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 되돌릴 수 없는 작업이거나 대상이 없음
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 작업 또는 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 미리보기 이후 대상이 변경됨
      summary: 이 작업 되돌리기
      tags:
      - AI Operation Logs
  /api/workspaces/{workspace_id}/ai-operation-logs/{operation_id}/restore-preview:
    get:
      description: "이 작업을 되돌리면 무엇이 삭제·복원·재작성되는지 계산합니다. 지목한 작업과 그 이후 같은 문서의 작업을 전부 걷어내며, 그 과정에서 만들어진 페이지는 삭제됩니다. 문서 편집 복구는 canonical 편집 revision을 확인하며, 응답의 preview_token은 복구 실행에 그대로 전달해야 합니다."
      operationId: restorePreview
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: operation_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/RestorePreviewResponse"
          description: 계산 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 작업 또는 워크스페이스를 찾을 수 없음
      summary: 복구 미리보기
      tags:
      - AI Operation Logs
  /api/workspaces/{workspace_id}/assets/{asset_id}/content:
    get:
      description: 워크스페이스 멤버에게 관리 이미지 bytes를 반환합니다.
      operationId: getContent
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: asset_id
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          content:
            '*/*':
              schema:
                type: string
                format: binary
          description: 이미지 반환
        "304":
          content:
            '*/*':
              schema:
                type: string
                format: binary
          description: 캐시된 이미지 사용
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: asset 또는 워크스페이스를 찾을 수 없음
      summary: 문서 이미지 조회
      tags:
      - Document Assets
  /api/workspaces/{workspace_id}/chat/sessions:
    get:
      description: 가장 최근 메시지 순으로 정렬해 반환합니다.
      operationId: list_1
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ChatSessionListResponse"
          description: 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
      summary: 채팅 세션 목록 조회
      tags:
      - Chat Sessions
    post:
      description: 워크스페이스당 최대 10개까지 생성할 수 있습니다.
      operationId: create_1
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatSessionCreateRequest"
      responses:
        "201":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ChatSessionResponse"
          description: 생성 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 세션 개수 제한 초과
      summary: 채팅 세션 생성
      tags:
      - Chat Sessions
  /api/workspaces/{workspace_id}/chat/sessions/{session_id}:
    delete:
      description: 워크스페이스에서 지정한 채팅 세션과 해당 세션의 메시지 기록을 삭제합니다.
      operationId: delete_2
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 채팅 세션 ID
        example: session_abc12345
        in: path
        name: session_id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: 삭제 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 세션 또는 워크스페이스를 찾을 수 없음
      summary: 채팅 세션 삭제
      tags:
      - Chat Sessions
  /api/workspaces/{workspace_id}/chat/sessions/{session_id}/messages:
    get:
      description: 세션 내 채팅 메시지를 생성 순서대로 반환합니다.
      operationId: getMessages
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 채팅 세션 ID
        example: session_abc12345
        in: path
        name: session_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ChatMessagesResponse"
          description: 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 세션 또는 워크스페이스를 찾을 수 없음
      summary: 채팅 메시지 기록 조회
      tags:
      - Chat Sessions
  /api/workspaces/{workspace_id}/chat/sessions/{session_id}/query:
    post:
      description: "질문을 받아 Wiki 페이지를 검색하고 LLM으로 답변을 생성합니다. 응답에는 답변, 관련 Wiki 페이지, 원본 출처, 그래프 하이라이트 경로가 포함됩니다."
      operationId: query
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 채팅 세션 ID
        example: session_abc12345
        in: path
        name: session_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/QueryResponse"
          description: 질의 성공
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 잘못된 요청 (질문이 비어 있는 경우)
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 세션 또는 워크스페이스를 찾을 수 없음
        "500":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 서버 내부 오류
        "502":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 파이프라인 요청 거부
        "503":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 파이프라인 타임아웃 또는 사용 불가
      summary: Wiki 기반 자연어 질의 (동기)
      tags:
      - Query
  /api/workspaces/{workspace_id}/chat/sessions/{session_id}/query/runs:
    post:
      description: "질의를 비동기 run으로 시작합니다. 진행 상황은 GET /api/query/runs/{request_id}/events(SSE)로 구독합니다."
      operationId: createRun
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 채팅 세션 ID
        example: session_abc12345
        in: path
        name: session_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryRequest"
        required: true
      responses:
        "202":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/QueryRunCreateResponse"
          description: run 시작됨
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 잘못된 요청 (질문이 비어 있는 경우)
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 세션 또는 워크스페이스를 찾을 수 없음
      summary: Wiki 기반 자연어 질의 (비동기)
      tags:
      - Query
  /api/workspaces/{workspace_id}/chat/sessions/{session_id}/wiki:
    post:
      description: 세션(full) 또는 선택 문답(partial)을 Markdown 문서로 저장하고 처리 큐에 등록합니다. 위키 생성은 파이프라인이 비동기로 수행합니다.
      operationId: exportToWiki
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 채팅 세션 ID
        example: session_abc12345
        in: path
        name: session_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatWikiExportRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ChatWikiExportResponse"
          description: OK
      summary: 채팅 Wiki page화
      tags:
      - Chat Sessions
  /api/workspaces/{workspace_id}/chat/sessions/{session_id}/wiki/preview:
    post:
      description: 세션을 llmPipeline 입력용 Markdown으로 직렬화해 결과만 반환합니다. 저장/파이프라인 호출은 하지 않습니다.
      operationId: previewWikiMarkdown
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 채팅 세션 ID
        example: session_abc12345
        in: path
        name: session_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            text/plain;charset=UTF-8:
              schema:
                type: string
          description: OK
      summary: "[임시] 채팅 Wiki page화 Markdown 미리보기"
      tags:
      - Chat Sessions
  /api/workspaces/{workspace_id}/document-tree:
    get:
      description: 모든 폴더를 펼친 상태의 활성 폴더·문서 계층을 한 번에 반환합니다.
      operationId: tree
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentTreeResponse"
          description: 전체 트리 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 활성 워크스페이스 또는 멤버십을 찾을 수 없음
      summary: 전체 문서 트리 조회
      tags:
      - Navigation
  /api/workspaces/{workspace_id}/documents:
    get:
      description: 활성 문서의 호환용 평면 목록을 반환하며 파일명 검색을 지원합니다.
      operationId: list
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: query
        name: query
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentListResponse"
          description: 목록 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
        "500":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 서버 내부 오류
      summary: 문서 목록 조회
      tags:
      - Documents
    post:
      description: "PDF 또는 Markdown 파일을 업로드합니다. Markdown은 편집 상태와 처리 큐를 생성하고, PDF는 읽기 전용 원본으로만 저장합니다."
      operationId: upload
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 요청 멱등 키
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
      - in: query
        name: folder_id
        required: false
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "201":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentUploadResponse"
          description: 업로드 성공
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 파일 없음 또는 잘못된 요청
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Idempotency-Key 충돌
        "415":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 지원하지 않는 파일 형식
        "500":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 서버 내부 오류
      summary: 문서 업로드
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/markdown:
    post:
      description: 표시 이름과 전체 Markdown 본문으로 즉시 편집 가능한 문서를 생성합니다.
      operationId: createMarkdown
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 요청 멱등 키
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MarkdownDocumentCreateRequest"
        required: true
      responses:
        "201":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentUploadResponse"
          description: 생성 성공 또는 멱등 재요청
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 잘못된 본문 또는 Idempotency-Key
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Idempotency-Key 충돌
        "413":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Markdown 5MB 초과
      summary: Markdown 문서 생성
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/trash:
    get:
      description: 워크스페이스에서 소프트 삭제된 문서를 삭제 시각 역순으로 반환합니다.
      operationId: trash
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentTrashResponse"
          description: 휴지통 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
      summary: 문서 휴지통
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/{document_id}:
    delete:
      description: 원본과 편집 상태를 유지한 채 문서를 소프트 삭제합니다.
      operationId: delete_1
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 문서 ID
        example: doc_abc12345
        in: path
        name: document_id
        required: true
        schema:
          type: string
      - description: 요청 멱등 키
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocumentLifecycleRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentLifecycleResponse"
          description: 삭제 성공
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 잘못된 base_version 또는 Idempotency-Key
        "403":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 소유자가 아님
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 또는 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 version 또는 멱등 키 충돌
      summary: 문서 삭제
      tags:
      - Documents
    get:
      description: 특정 문서의 상세 정보를 반환합니다. 연결된 Wiki 페이지 목록이 포함됩니다.
      operationId: getById
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 문서 ID
        example: doc_abc12345
        in: path
        name: document_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentDetailResponse"
          description: 상세 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 또는 워크스페이스를 찾을 수 없음
        "500":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 서버 내부 오류
      summary: 문서 상세 조회
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/{document_id}/blocks:
    get:
      description: 원본 문서를 block 단위로 나눈 텍스트 목록을 반환합니다. 답변 인용 클릭 시 원본 block 하이라이트에 사용됩니다.
      operationId: blocks
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 문서 ID
        example: doc_abc12345
        in: path
        name: document_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentBlocksResponse"
          description: 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 또는 워크스페이스를 찾을 수 없음
      summary: 원본 문서 block 목록 조회
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/{document_id}/content:
    put:
      description: 전체 Markdown과 신규 이미지를 저장합니다. base_revision이 현재 편집 revision과 일치할 때만 반영하며 revision_write_id 재시도는 기존 결과를 반환합니다. 이미지 포함 저장은 metadata part를 사용합니다.
      operationId: saveContent
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: document_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                apply_operation_id:
                  type: string
                  description: AI 편집 적용 표. `POST /agent/turn` 응답의 apply_operation_id를 그대로 전달하면 AI 작업 로그가 남습니다.
                base_revision:
                  type: string
                  description: 클라이언트가 조회한 현재 편집 revision
                markdown:
                  type: string
                  description: 저장할 전체 Markdown 본문
                metadata:
                  type: string
                  description: 이미지 포함 저장 metadata JSON
                revision_write_id:
                  type: string
                  description: 같은 저장 재시도에 재사용하는 write ID
                source:
                  type: string
                  description: "저장 출처. AI 편집 승인 시 \"agent\", 수동 저장 시 생략합니다."
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentContentSaveResponse"
          description: 저장 성공 또는 동일 본문 no-op
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: "잘못된 Markdown, base_revision 또는 revision_write_id"
        "403":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 소유자가 아님
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 또는 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 편집 revision 또는 revision_write_id 충돌
        "413":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Markdown 5MB 또는 이미지 제한 초과
        "415":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 지원하지 않는 이미지 형식
      summary: Markdown 본문 저장
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/{document_id}/convert-markdown:
    post:
      description: "PDF 원본 문서를 Markdown 문서로 변환합니다. 변환 결과를 담을 편집 가능 placeholder 문서를 즉시 만들어 반환하고, 실제 변환은 백그라운드에서 진행됩니다."
      operationId: convertMarkdown
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: PDF 원본 문서 ID
        example: doc_abc12345
        in: path
        name: document_id
        required: true
        schema:
          type: string
      - description: 요청 멱등 키
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
      responses:
        "202":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentUploadResponse"
          description: 변환 요청 접수 및 placeholder 문서 생성
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: PDF 원본 문서가 아니거나 잘못된 Idempotency-Key
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 또는 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Idempotency-Key 충돌
      summary: PDF Markdown 변환
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/{document_id}/diff:
    get:
      description: 두 Markdown 버전을 줄 단위로 비교해 GitHub 스타일 diff hunk를 반환합니다.
      operationId: compareVersions
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: document_id
        required: true
        schema:
          type: string
      - in: query
        name: from_version
        required: true
        schema:
          type: integer
          format: int64
      - in: query
        name: to_version
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentContentDiffResponse"
          description: 비교 성공
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 편집 가능한 Markdown 문서가 아님
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 또는 비교할 버전을 찾을 수 없음
        "422":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 차이가 너무 커서 안전하게 비교할 수 없음
      summary: 콘텐츠 버전 비교
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/{document_id}/duplicate:
    post:
      description: 문서 소유자가 최신 Markdown 편집본을 같은 부모의 마지막 위치에 새 문서로 복제합니다.
      operationId: duplicate
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: document_id
        required: true
        schema:
          type: string
      - description: 요청 멱등 키
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
      responses:
        "201":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentDuplicateResponse"
          description: 복제 성공 또는 멱등 재요청
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 잘못된 Idempotency-Key
        "403":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 소유자가 아니거나 편집 문서가 아님
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 또는 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Idempotency-Key 충돌
      summary: Markdown 문서 복제
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/{document_id}/edit-lock:
    delete:
      description: 편집기 종료 시 호출한다. 보유자 본인의 잠금만 해제하며 멱등이다.
      operationId: release
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: document_id
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
      summary: 편집 잠금 해제
      tags:
      - Document Edit Lock
    post:
      description: 편집기 진입 시 호출한다. 비었거나 만료됐거나 본인 보유면 잠금을 부여(200)한다. 다른 사용자가 편집 중이면 423과 보유자 정보를 반환한다.
      operationId: acquire
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: document_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/EditLockResponse"
          description: 잠금 획득/갱신
        "403":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 소유자가 아님
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 또는 워크스페이스를 찾을 수 없음
        "423":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/EditLockResponse"
          description: 다른 사용자가 편집 중
      summary: 편집 잠금 획득
      tags:
      - Document Edit Lock
  /api/workspaces/{workspace_id}/documents/{document_id}/edit-lock/heartbeat:
    post:
      description: 편집 중 주기적으로 호출해 잠금을 연장한다. 보유자가 아니거나 만료됐으면 409.
      operationId: heartbeat
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: document_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/EditLockResponse"
          description: 잠금 연장
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 잠금 상실(만료/타인 보유)
      summary: 편집 잠금 heartbeat
      tags:
      - Document Edit Lock
  /api/workspaces/{workspace_id}/documents/{document_id}/export:
    get:
      description: 최신 Markdown 편집본을 내보냅니다. 관리 이미지가 있으면 이미지와 Markdown을 ZIP으로 반환합니다.
      operationId: export
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: document_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                type: string
                format: binary
          description: Markdown 다운로드
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: "workspace, Markdown 문서 또는 편집 상태를 찾을 수 없음"
      summary: Markdown 원문 내보내기
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/{document_id}/ingest:
    post:
      description: 편집 가능 Markdown 문서를 최신 편집본으로 다시 Wiki 파이프라인에 넣습니다. 편집본을 원본으로 승격한 뒤 재처리합니다.
      operationId: ingest
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: document_id
        required: true
        schema:
          type: string
      responses:
        "202":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentIngestResponse"
          description: 재처리 큐 등록됨
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 편집 가능한 Markdown 문서가 아님
        "403":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 소유자가 아님
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 또는 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 이미 처리 중인 문서
      summary: 문서 재ingest
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/{document_id}/original:
    get:
      description: "MinIO에 저장된 원본 파일을 스트리밍합니다. PDF는 inline, 그 외는 attachment로 반환됩니다."
      operationId: getOriginal
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 문서 ID
        example: doc_abc12345
        in: path
        name: document_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                type: string
                format: binary
          description: 원본 파일 반환
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: "문서, 원본 파일 또는 워크스페이스를 찾을 수 없음"
      summary: 원본 문서 조회
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/{document_id}/position:
    patch:
      description: 문서를 대상 폴더와 정렬 위치로 이동합니다. base version과 Idempotency-Key로 동시 변경을 검증합니다.
      operationId: move_1
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 이동할 문서 ID
        in: path
        name: document_id
        required: true
        schema:
          type: string
      - description: 요청 멱등 키
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocumentPositionRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentPositionResponse"
          description: 이동 성공 또는 멱등 재요청
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: "잘못된 위치 또는 version, 또는 INVALID_IDEMPOTENCY_KEY(멱등 키 누락/유효하지 않음)"
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: "문서, 대상 폴더 또는 워크스페이스를 찾을 수 없음"
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: "version 충돌, IDEMPOTENCY_CONFLICT(동일 키에 다른 payload 사용) 또는 IDEMPOTENCY_IN_PROGRESS(활성 lease 재사용)"
      summary: 문서 위치 이동
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/{document_id}/rename:
    patch:
      description: Notion의 page title처럼 표시 이름만 변경하며 본문과 Wiki 제목은 유지합니다.
      operationId: rename_2
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 문서 ID
        example: doc_abc12345
        in: path
        name: document_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocumentRenameRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentRenameResponse"
          description: 이름 변경 성공
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 유효하지 않은 파일명
        "403":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 소유자가 아님
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 또는 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 version 충돌
      summary: 문서 이름 변경
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/{document_id}/restore:
    post:
      description: 삭제 문서를 역할별 최상위 마지막 위치에 복구합니다.
      operationId: restore_1
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: document_id
        required: true
        schema:
          type: string
      - description: 요청 멱등 키
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocumentLifecycleRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentLifecycleResponse"
          description: 복구 성공
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 잘못된 base_version 또는 Idempotency-Key
        "403":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 소유자가 아님
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 삭제 문서 또는 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 version 또는 멱등 키 충돌
      summary: 문서 복구
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/{document_id}/versions:
    get:
      description: 편집 가능 Markdown 문서의 콘텐츠 버전 이력을 최신 순으로 반환합니다. 본문은 제외한 메타데이터만 제공합니다.
      operationId: listVersions
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: document_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentContentVersionListResponse"
          description: 조회 성공
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 편집 가능한 Markdown 문서가 아님
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 또는 워크스페이스를 찾을 수 없음
      summary: 콘텐츠 버전 이력 목록
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/{document_id}/versions/{version}:
    get:
      description: 특정 버전의 전체 Markdown 본문을 반환합니다.
      operationId: getVersion
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: document_id
        required: true
        schema:
          type: string
      - in: path
        name: version
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentContentVersionResponse"
          description: 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 또는 해당 버전을 찾을 수 없음
      summary: 콘텐츠 버전 단건 조회
      tags:
      - Documents
  /api/workspaces/{workspace_id}/documents/{document_id}/versions/{version}/restore:
    post:
      description: 과거 버전을 새 버전으로 복원합니다(비파괴적). base_version이 현재 version과 일치할 때만 반영합니다.
      operationId: restoreVersion
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: path
        name: document_id
        required: true
        schema:
          type: string
      - in: path
        name: version
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocumentContentRestoreRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/DocumentContentSaveResponse"
          description: 복원 성공 또는 동일 본문 no-op
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 편집 가능한 Markdown 문서가 아니거나 base_version 오류
        "403":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 소유자가 아님
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 또는 해당 버전을 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 문서 version 충돌
      summary: 콘텐츠 버전 복원
      tags:
      - Documents
  /api/workspaces/{workspace_id}/folders:
    post:
      description: 워크스페이스의 최상위 또는 지정한 상위 폴더 아래에 새 폴더를 생성합니다.
      operationId: create
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 요청 멱등 키
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FolderCreateRequest"
        required: true
      responses:
        "201":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/FolderResponse"
          description: 생성 성공 또는 멱등 재요청
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: "잘못된 이름 또는 위치, 또는 INVALID_IDEMPOTENCY_KEY(멱등 키 누락/유효하지 않음)"
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스 또는 상위 폴더를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: IDEMPOTENCY_CONFLICT(동일 키에 다른 payload 사용) 또는 IDEMPOTENCY_IN_PROGRESS(활성 lease 재사용)
      summary: 폴더 생성
      tags:
      - Folders
  /api/workspaces/{workspace_id}/folders/{folder_id}:
    delete:
      description: 폴더와 하위 항목을 휴지통 상태로 전환하며 base version으로 동시 변경을 검증합니다.
      operationId: delete
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 삭제할 폴더 ID
        in: path
        name: folder_id
        required: true
        schema:
          type: string
          format: uuid
      - description: 요청 멱등 키
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocumentLifecycleRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/FolderLifecycleResponse"
          description: 삭제 성공 또는 멱등 재요청
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 잘못된 version 또는 INVALID_IDEMPOTENCY_KEY(멱등 키 누락/유효하지 않음)
        "403":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 내용이 있는 폴더를 삭제할 권한이 없음
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 폴더 또는 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: "version 충돌, IDEMPOTENCY_CONFLICT(동일 키에 다른 payload 사용) 또는 IDEMPOTENCY_IN_PROGRESS(활성 lease 재사용)"
      summary: 폴더 삭제
      tags:
      - Folders
    patch:
      description: 폴더 이름을 변경하고 base version으로 동시 변경을 검증합니다.
      operationId: rename_1
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 이름을 변경할 폴더 ID
        in: path
        name: folder_id
        required: true
        schema:
          type: string
          format: uuid
      - description: 요청 멱등 키
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FolderRenameRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/FolderResponse"
          description: 변경 성공 또는 멱등 재요청
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: "잘못된 이름 또는 version, 또는 INVALID_IDEMPOTENCY_KEY(멱등 키 누락/유효하지 않음)"
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 폴더 또는 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: "version 충돌, IDEMPOTENCY_CONFLICT(동일 키에 다른 payload 사용) 또는 IDEMPOTENCY_IN_PROGRESS(활성 lease 재사용)"
      summary: 폴더 이름 변경
      tags:
      - Folders
  /api/workspaces/{workspace_id}/folders/{folder_id}/children:
    get:
      description: 폴더 바로 아래의 하위 폴더와 문서를 정렬 순서로 반환합니다.
      operationId: children
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 조회할 폴더 ID
        in: path
        name: folder_id
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/FolderChildrenResponse"
          description: 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 폴더 또는 워크스페이스를 찾을 수 없음
      summary: 폴더 하위 항목 조회
      tags:
      - Folders
  /api/workspaces/{workspace_id}/folders/{folder_id}/position:
    patch:
      description: 폴더를 대상 상위 폴더와 정렬 위치로 이동합니다. 자기 자신이나 하위 폴더로는 이동할 수 없습니다.
      operationId: move
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 이동할 폴더 ID
        in: path
        name: folder_id
        required: true
        schema:
          type: string
          format: uuid
      - description: 요청 멱등 키
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FolderPositionRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/FolderResponse"
          description: 이동 성공 또는 멱등 재요청
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 잘못된 요청 또는 INVALID_IDEMPOTENCY_KEY(멱등 키 누락/유효하지 않음)
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: "폴더, 대상 폴더 또는 워크스페이스를 찾을 수 없음"
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: "순환 이동, version 충돌, IDEMPOTENCY_CONFLICT(동일 키에 다른 payload 사용) 또는 IDEMPOTENCY_IN_PROGRESS(활성 lease 재사용)"
      summary: 폴더 위치 이동
      tags:
      - Folders
  /api/workspaces/{workspace_id}/folders/{folder_id}/restore:
    post:
      description: 삭제된 폴더와 하위 항목을 복구하고 유효한 탐색 위치에 배치합니다.
      operationId: restore
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 복구할 폴더 ID
        in: path
        name: folder_id
        required: true
        schema:
          type: string
          format: uuid
      - description: 요청 멱등 키
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocumentLifecycleRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/FolderLifecycleResponse"
          description: 복구 성공 또는 멱등 재요청
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 잘못된 version 또는 INVALID_IDEMPOTENCY_KEY(멱등 키 누락/유효하지 않음)
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 삭제된 폴더 또는 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: "version 충돌, IDEMPOTENCY_CONFLICT(동일 키에 다른 payload 사용) 또는 IDEMPOTENCY_IN_PROGRESS(활성 lease 재사용)"
      summary: 폴더 복구
      tags:
      - Folders
  /api/workspaces/{workspace_id}/navigation:
    get:
      description: 워크스페이스 최상위의 폴더와 문서를 정렬 순서로 반환합니다.
      operationId: root
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/FolderChildrenResponse"
          description: 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
      summary: 최상위 항목 조회
      tags:
      - Navigation
  /api/workspaces/{workspace_id}/navigation/breadcrumb:
    get:
      description: 폴더 또는 문서까지 이어지는 상위 폴더 경로를 최상위부터 반환합니다.
      operationId: breadcrumb
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 경로를 조회할 폴더 ID. document_id와 함께 사용할 수 없습니다.
        in: query
        name: folder_id
        required: false
        schema:
          type: string
          format: uuid
      - description: 경로를 조회할 문서 ID. folder_id와 함께 사용할 수 없습니다.
        in: query
        name: document_id
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/BreadcrumbResponse"
          description: 경로 조회 성공
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: folder_id와 document_id가 모두 없거나 함께 전달됨
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 대상 또는 워크스페이스를 찾을 수 없음
      summary: 이동 경로 조회
      tags:
      - Navigation
  /api/workspaces/{workspace_id}/navigation/search:
    get:
      description: 워크스페이스의 폴더 이름과 문서 파일명을 검색해 계층 경로를 반환합니다.
      operationId: search
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 폴더 이름 또는 문서 파일명 검색어
        in: query
        name: query
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/HierarchySearchResponse"
          description: 검색 성공
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 검색어가 비어 있거나 잘못됨
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
      summary: 폴더·문서 검색
      tags:
      - Navigation
  /api/workspaces/{workspace_id}/skills:
    get:
      description: 현재 사용자가 사용할 수 있는 워크스페이스 Skill 목록을 반환합니다.
      operationId: list_2
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      responses:
        "200":
          content:
            '*/*':
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SkillResponse"
          description: 목록 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill 요청 충돌
        "410":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill이 더 이상 유효하지 않음
        "503":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: llmPipeline 사용 불가
      summary: Skill 목록 조회
      tags:
      - Skills
  /api/workspaces/{workspace_id}/skills/author:
    post:
      description: 자연어 요구를 바탕으로 저장 전 Skill 초안을 생성합니다.
      operationId: author
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SkillAuthoringRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                type: object
                properties:
                  allowed_tools:
                    type: string
                  capabilities:
                    type: string
                  description:
                    type: string
                  instructions_markdown:
                    type: string
                  issues:
                    type: string
                  name:
                    type: string
                  question:
                    type: string
                  scope_type:
                    type: string
                  skill_id:
                    type: string
                  skill_markdown:
                    type: string
                  status:
                    type: string
                  version_id:
                    type: string
                required:
                - status
          description: 초안 작성 성공
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 잘못된 요청
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스 또는 참조 문서를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill 이름 또는 버전 충돌
        "410":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill이 더 이상 유효하지 않음
        "413":
          content:
            application/json:
              schema:
                oneOf:
                - $ref: "#/components/schemas/JsonNode"
                - $ref: "#/components/schemas/ErrorResponse"
          description: 참조 문서 또는 요청 본문이 너무 큼
        "422":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill 요청 검증 실패
        "503":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: llmPipeline 사용 불가
      summary: Skill 초안 작성
      tags:
      - Skills
  /api/workspaces/{workspace_id}/skills/author/publish:
    post:
      description: 작성된 Skill 정의를 검토 후 게시합니다.
      operationId: publish
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SkillPublishRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                type: object
                properties:
                  allowed_tools:
                    type: string
                  capabilities:
                    type: string
                  description:
                    type: string
                  instructions_markdown:
                    type: string
                  issues:
                    type: string
                  name:
                    type: string
                  question:
                    type: string
                  scope_type:
                    type: string
                  skill_id:
                    type: string
                  skill_markdown:
                    type: string
                  status:
                    type: string
                  version_id:
                    type: string
                required:
                - status
          description: 게시 성공
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 잘못된 Skill 정의
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스 또는 참조 문서를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill 이름 또는 버전 충돌
        "410":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill이 더 이상 유효하지 않음
        "413":
          content:
            application/json:
              schema:
                oneOf:
                - $ref: "#/components/schemas/JsonNode"
                - $ref: "#/components/schemas/ErrorResponse"
          description: 요청 본문이 너무 큼
        "422":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill 요청 검증 실패
        "503":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: llmPipeline 사용 불가
      summary: Skill 게시
      tags:
      - Skills
  /api/workspaces/{workspace_id}/skills/{skill_id}:
    get:
      description: Skill의 현재 정의와 실행 설정을 반환합니다.
      operationId: get_1
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 조회할 Skill ID
        in: path
        name: skill_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SkillResponse"
          description: 상세 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill 또는 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill 요청 충돌
        "410":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill이 더 이상 유효하지 않음
        "503":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: llmPipeline 사용 불가
      summary: Skill 상세 조회
      tags:
      - Skills
    patch:
      description: Skill의 정의를 수정합니다.
      operationId: update_1
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 수정할 Skill ID
        in: path
        name: skill_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SkillUpdateRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                type: object
                properties:
                  description:
                    type: string
                  instructions_markdown:
                    type: string
                  issues:
                    type: string
                  name:
                    type: string
                  question:
                    type: string
                  scope_type:
                    type: string
                  skill_id:
                    type: string
                  skill_markdown:
                    type: string
                  status:
                    type: string
                  version_id:
                    type: string
                required:
                - status
          description: 수정 성공
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 잘못된 Skill 정의
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill 또는 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill 이름 또는 버전 충돌
        "410":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill이 더 이상 유효하지 않음
        "413":
          content:
            application/json:
              schema:
                oneOf:
                - $ref: "#/components/schemas/JsonNode"
                - $ref: "#/components/schemas/ErrorResponse"
          description: 요청 본문이 너무 큼
        "422":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill 요청 검증 실패
        "503":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: llmPipeline 사용 불가
      summary: Skill 수정
      tags:
      - Skills
  /api/workspaces/{workspace_id}/skills/{skill_id}/disable:
    post:
      description: Skill을 Agent 실행 대상에서 제외합니다.
      operationId: disable
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 비활성화할 Skill ID
        in: path
        name: skill_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SkillResponse"
          description: 비활성화 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill 또는 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill 요청 충돌
        "410":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill이 더 이상 유효하지 않음
        "503":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: llmPipeline 사용 불가
      summary: Skill 비활성화
      tags:
      - Skills
  /api/workspaces/{workspace_id}/skills/{skill_id}/enable:
    post:
      description: Skill을 Agent 실행 대상에 포함합니다.
      operationId: enable
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 활성화할 Skill ID
        in: path
        name: skill_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SkillResponse"
          description: 활성화 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill 또는 워크스페이스를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill 요청 충돌
        "410":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: Skill이 더 이상 유효하지 않음
        "503":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: llmPipeline 사용 불가
      summary: Skill 활성화
      tags:
      - Skills
  /api/workspaces/{workspace_id}/wiki-schema/active:
    get:
      description: 활성 Schema가 없으면 null을 포함한 200 응답을 반환합니다.
      operationId: getActive
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/WikiSchemaResponse"
          description: 활성 Schema 조회 성공(null 가능)
        "404":
          content:
            application/json:
              schema:
                oneOf:
                - $ref: "#/components/schemas/JsonNode"
                - $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
        "503":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: llmPipeline 사용 불가
      summary: 활성 Wiki Schema 조회
      tags:
      - Wiki Schema
  /api/workspaces/{workspace_id}/wiki-schema/drafts:
    post:
      description: 검토할 Wiki 생성 규칙을 초안 상태로 저장합니다.
      operationId: createDraft
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WikiSchemaDraftRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/WikiSchemaDraftResponse"
          description: 초안 생성 성공
        "400":
          content:
            application/json:
              schema:
                oneOf:
                - $ref: "#/components/schemas/JsonNode"
                - $ref: "#/components/schemas/ErrorResponse"
          description: 잘못된 Schema 정의
        "404":
          content:
            application/json:
              schema:
                oneOf:
                - $ref: "#/components/schemas/JsonNode"
                - $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
        "422":
          content:
            application/json:
              schema:
                oneOf:
                - $ref: "#/components/schemas/JsonNode"
                - $ref: "#/components/schemas/ErrorResponse"
          description: Schema 요청 검증 실패
        "503":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: llmPipeline 사용 불가
      summary: Wiki Schema 초안 생성
      tags:
      - Wiki Schema
  /api/workspaces/{workspace_id}/wiki-schema/preview:
    post:
      description: Schema 규칙을 저장하지 않고 적용해 예상 Wiki 구조를 반환합니다.
      operationId: preview
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WikiSchemaPreviewRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/WikiSchemaPreviewResponse"
          description: 미리보기 생성 성공
        "400":
          content:
            application/json:
              schema:
                oneOf:
                - $ref: "#/components/schemas/JsonNode"
                - $ref: "#/components/schemas/ErrorResponse"
          description: 잘못된 Schema 또는 입력
        "404":
          content:
            application/json:
              schema:
                oneOf:
                - $ref: "#/components/schemas/JsonNode"
                - $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
        "422":
          content:
            application/json:
              schema:
                oneOf:
                - $ref: "#/components/schemas/JsonNode"
                - $ref: "#/components/schemas/ErrorResponse"
          description: Schema 요청 검증 실패
        "503":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: llmPipeline 사용 불가
      summary: Wiki Schema 미리보기
      tags:
      - Wiki Schema
  /api/workspaces/{workspace_id}/wiki-schema/{schema_id}/activate:
    post:
      description: 선택한 Wiki Schema ID의 활성화를 요청합니다.
      operationId: activate
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 활성화할 Wiki Schema ID
        in: path
        name: schema_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/WikiSchemaResponse"
          description: 활성화 성공
        "404":
          content:
            application/json:
              schema:
                oneOf:
                - $ref: "#/components/schemas/JsonNode"
                - $ref: "#/components/schemas/ErrorResponse"
          description: Schema 또는 워크스페이스를 찾을 수 없음
        "503":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: llmPipeline 사용 불가
      summary: Wiki Schema 활성화
      tags:
      - Wiki Schema
  /api/workspaces/{workspace_id}/wiki/graph:
    get:
      description: 모든 Wiki 노드(pages)와 엣지(links)를 반환합니다. 중앙 그래프 렌더링과 답변 후 하이라이트에 사용됩니다.
      operationId: getGraph
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/WikiGraphResponse"
          description: 그래프 조회 성공
        "500":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 서버 내부 오류
      summary: Wiki 그래프 조회
      tags:
      - Wiki
  /api/workspaces/{workspace_id}/wiki/maintenance/lint:
    post:
      description: 워크스페이스 Wiki 정합성 검사 실행을 비동기 대기열에 등록합니다.
      operationId: lint
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WikiLintRequest"
      responses:
        "202":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/WikiLintResponse"
          description: Wiki 정합성 검사 실행이 대기열에 등록됨
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 잘못된 검사 옵션
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
        "503":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: llmPipeline 사용 불가
      summary: Wiki 정합성 검사
      tags:
      - Wiki Maintenance
  /api/workspaces/{workspace_id}/wiki/maintenance/runs/{run_id}:
    get:
      description: 실행 중이거나 완료된 Wiki 정합성 검사 결과를 반환합니다.
      operationId: run
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: 조회할 검사 실행 ID
        in: path
        name: run_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/JsonNode"
          description: 결과 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 검사 실행 또는 워크스페이스를 찾을 수 없음
      summary: Wiki 정합성 검사 결과 조회
      tags:
      - Wiki Maintenance
  /api/workspaces/{workspace_id}/wiki/maintenance/status:
    get:
      description: 워크스페이스 Wiki 유지보수 작업의 현재 상태를 반환합니다.
      operationId: status
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/WikiMaintenanceStatusResponse"
          description: 상태 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 워크스페이스를 찾을 수 없음
      summary: Wiki 유지보수 상태 조회
      tags:
      - Wiki Maintenance
  /api/workspaces/{workspace_id}/wiki/pages/{wiki_page_id}:
    get:
      description: 특정 Wiki 페이지의 상세 정보를 반환합니다. source_documents와 related_pages를 포함합니다.
      operationId: getPage
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: Wiki 페이지 ID
        example: wp_abc123
        in: path
        name: wiki_page_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/WikiPageDetailResponse"
          description: 페이지 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 페이지를 찾을 수 없음
        "500":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 서버 내부 오류
      summary: Wiki 페이지 상세 조회
      tags:
      - Wiki
  /api/workspaces/{workspace_id}/wiki/pages/{wiki_page_id}/diff:
    get:
      description: "두 revision 사이의 diff를 반환합니다. 저장된 본문을 읽어 요청 시점에 계산하며, 사용자가 펼칠 때만 호출됩니다."
      operationId: diff
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: Wiki 페이지 ID
        example: wp_abc123
        in: path
        name: wiki_page_id
        required: true
        schema:
          type: string
      - description: 비교 기준 revision
        example: 1
        in: query
        name: from
        required: true
        schema:
          type: integer
          format: int64
      - description: 비교 대상 revision
        example: 2
        in: query
        name: to
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/WikiPageDiffResponse"
          description: 조회 성공
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 페이지 또는 버전을 찾을 수 없음
        "422":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 두 본문의 차이가 너무 커서 비교할 수 없음
      summary: Wiki 페이지 변경분 조회
      tags:
      - Wiki
  /api/workspaces/{workspace_id}/wiki/pages/{wiki_page_id}/rename:
    patch:
      description: Wiki 페이지 제목을 변경합니다. update_slug=true이면 slug도 재생성하며 중복 여부를 검증합니다.
      operationId: rename
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - description: Wiki 페이지 ID
        example: wp_abc123
        in: path
        name: wiki_page_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WikiPageRenameRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/WikiPageRenameResponse"
          description: 이름 변경 성공
        "400":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 유효하지 않은 제목
        "404":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: 페이지를 찾을 수 없음
        "409":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/ErrorResponse"
          description: slug 충돌
      summary: Wiki 페이지 이름 변경
      tags:
      - Wiki
  /internal/agent/skill-authoring/references/read:
    post:
      operationId: read_1
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SkillReferenceReadRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SkillReferenceReadResponse"
          description: OK
      tags:
      - skill-reference-controller
  /internal/agent/tools/execute/{tool_name}:
    post:
      operationId: execute
      parameters:
      - in: path
        name: tool_name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AgentToolExecuteRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                type: object
          description: OK
      tags:
      - agent-tool-controller
  /internal/agent/tools/read/{tool_name}:
    post:
      operationId: read
      parameters:
      - in: path
        name: tool_name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AgentToolReadRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                type: object
          description: OK
      tags:
      - agent-tool-controller
  /internal/documents/{document_id}/pipeline-source:
    get:
      operationId: findPipelineSource
      parameters:
      - in: path
        name: document_id
        required: true
        schema:
          type: string
      - in: header
        name: X-Internal-Token
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            '*/*':
              schema:
                type: object
          description: OK
      tags:
      - internal-document-controller
  /internal/wiki/contributions:
    post:
      operationId: find
      parameters:
      - in: header
        name: X-Internal-Token
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContributionRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                type: object
          description: OK
      tags:
      - internal-wiki-contribution-controller
  /internal/workspaces/{workspace_id}/initial-note:
    post:
      operationId: createInitialNote
      parameters:
      - example: ws_9d47a0e9a6324341b47562553b75f92a
        in: path
        name: workspace_id
        required: true
        schema:
          type: string
          default: ws_9d47a0e9a6324341b47562553b75f92a
      - in: header
        name: X-Internal-Token
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InitialNoteRequest"
        required: true
      responses:
        "200":
          content:
            '*/*':
              schema:
                type: object
          description: OK
      tags:
      - internal-document-controller
components:
  schemas:
    AgentRunApproveRequest:
      type: object
      description: Agent 편집안 승인 요청. 정의되지 않은 필드가 있으면 거절된다.
      properties:
        operation_hash:
          type: string
          description: 승인 대상 편집안의 해시(64자). 내용이 바뀌면 값이 달라져 잘못된 승인을 막는다.
          maxLength: 64
          minLength: 64
        plan_version:
          type: integer
          format: int32
          description: 승인 대상 plan의 버전. 서버 값과 다르면 거절된다.
          example: 1
          minimum: 1
      required:
      - operation_hash
    AgentRunReviseRequest:
      type: object
      description: Agent 편집안 수정 요청. 정의되지 않은 필드가 있으면 거절된다.
      properties:
        instruction:
          type: string
          description: 어떻게 고칠지 알려주는 지시문(1000자 이하)
          example: 표를 목록으로 바꿔줘
          maxLength: 1000
          minLength: 0
      required:
      - instruction
    AgentToolExecuteRequest:
      type: object
      properties:
        arguments:
          $ref: "#/components/schemas/JsonNode"
        idempotency_key:
          type: string
          minLength: 1
        operation_hash:
          type: string
          maxLength: 64
          minLength: 64
        operation_id:
          type: string
          minLength: 1
        plan_id:
          type: string
          minLength: 1
        plan_version:
          type: integer
          format: int32
        run_id:
          type: string
          minLength: 1
        user_id:
          type: string
          minLength: 1
        workspace_id:
          type: string
          minLength: 1
      required:
      - arguments
      - idempotency_key
      - operation_hash
      - operation_id
      - plan_id
      - run_id
      - user_id
      - workspace_id
    AgentToolReadRequest:
      type: object
      properties:
        arguments:
          $ref: "#/components/schemas/JsonNode"
        run_id:
          type: string
          minLength: 1
        user_id:
          type: string
          minLength: 1
        workspace_id:
          type: string
          minLength: 1
      required:
      - arguments
      - run_id
      - user_id
      - workspace_id
    AgentTurnRequest:
      type: object
      description: "Agent turn 요청. 접수되면 202로 돌아오고 결과는 비동기로 만들어진다. 문서를 열지 않은 상태에서도 보낼 수 있으며, 그때는 편집 대신 답변·되물음만 나온다. skill_* 필드는 snake_case이고 나머지는 camelCase다."
      properties:
        allow_web_search:
          type: boolean
          description: AI가 질의로 판정했을 때 웹 검색을 허용할지. 생략하면 내부 문서만 사용한다. 편집·Skill 갈래에는 영향이 없다.
        baseVersion:
          type: integer
          format: int64
          description: 편집 기준으로 삼을 문서 버전. documentId와 함께 생략한다.
          example: 3
          minimum: 0
        conversationContext:
          $ref: "#/components/schemas/ConversationContext"
          description: 이전 대화 맥락. 있으면 편집 판단에 함께 쓴다.
        documentId:
          type: string
          description: 편집 대상 문서 ID. 문서를 열지 않았으면 생략한다. 생략하면 baseVersion·editorSnapshot도 함께 생략해야 한다.
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        editorSnapshot:
          $ref: "#/components/schemas/EditorSnapshot"
          description: 편집 시작 시점의 에디터 상태. documentId와 함께 생략한다.
        message:
          type: string
          description: 사용자 지시문
          example: 이 문단을 표로 정리해줘
          minLength: 1
        model:
          type: string
          description: 모델명. provider와 짝을 이뤄야 한다.
          example: gpt-5-nano
        provider:
          type: string
          description: LLM provider. model과 짝을 이뤄야 하며 생략하면 설정값을 쓴다.
          enum:
          - openai
          - gemini
          - claude
          example: openai
        session_id:
          type: string
          description: 이 턴을 남길 채팅 세션 ID. 질의와 Agent 요청이 한 세션에 함께 쌓인다.
          example: session_0ff8564ea24047cd8144d3f48badfe3f
          minLength: 1
        skill_draft_excluded_literals:
          type: array
          description: Skill 초안에서 빼야 하는 리터럴 목록
          items:
            type: string
        skill_draft_sources:
          type: array
          description: Skill 초안을 뽑아낼 이전 run 목록(최대 3개)
          items:
            $ref: "#/components/schemas/SkillDraftSourceSelector"
          maxItems: 3
          minItems: 0
        skill_draft_user_directives:
          type: array
          description: Skill 초안에 반영할 사용자 지시 목록
          items:
            type: string
        skill_id:
          type: string
          description: explicit 모드에서 적용할 Skill ID(128자 이하)
          maxLength: 128
          minLength: 0
        skill_mode:
          type: string
          default: auto
          description: "Skill 적용 방식. explicit이면 skill_id가 필수이고, auto·off이면 skill_id를 넣을 수 없다."
          enum:
          - auto
          - explicit
          - "off"
          example: auto
          pattern: auto|explicit|off
        skill_scope_type:
          type: string
          description: Skill을 적용할 범위 종류
      required:
      - message
      - session_id
    AgentTurnResponse:
      type: object
      description: Markdown Agent 편집 턴 결과. apply_operation_id를 뺀 나머지 필드는 camelCase다.
      properties:
        apply_operation_id:
          type: string
          description: 편집안을 적용할 때 저장 요청에 apply_operation_id로 실어야 하는 값. 서버가 발급한 값이라야 AI 작업 로그가 남는다. 문서를 열지 않은 턴은 null이다.
        baseVersion:
          type: integer
          format: int64
          description: 이 턴이 기준으로 삼은 문서 버전. 문서를 열지 않은 턴은 null이다.
          example: 3
        documentId:
          type: string
          description: 대상 문서 ID. 문서를 열지 않은 턴은 null이다.
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        error:
          type: string
          description: 실패 사유. 성공이면 null이다.
        requestId:
          type: string
          description: 이 턴의 run ID. 상태 조회에 쓴다.
          example: agent_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        result:
          $ref: "#/components/schemas/JsonNode"
          description: 완료된 턴의 편집 제안 결과
        status:
          type: string
          description: 턴 처리 상태
          example: completed
    AiModelSelectionRequest:
      type: object
      description: 설정할 provider와 model 조합. 활성 catalog에 있는 짝만 허용된다.
      properties:
        model:
          type: string
          description: 모델명
          example: gpt-5-nano
          minLength: 1
        provider:
          type: string
          description: LLM provider
          enum:
          - openai
          - gemini
          - claude
          example: openai
          minLength: 1
      required:
      - model
      - provider
    AiModelSelectionResponse:
      type: object
      description: 현재 설정된 provider와 model 조합
      properties:
        model:
          type: string
          description: 모델명
          example: gpt-5-nano
        provider:
          type: string
          description: LLM provider
          enum:
          - openai
          - gemini
          - claude
          example: openai
    BreadcrumbNode:
      type: object
      description: 경로 상의 한 단계
      properties:
        id:
          type: string
          description: "폴더면 UUID, 문서면 doc_ 접두 ID"
        name:
          type: string
          description: 화면에 보여줄 이름
          example: 설계
        type:
          type: string
          description: 항목 종류
          enum:
          - folder
          - document
          example: folder
    BreadcrumbResponse:
      type: object
      properties:
        path:
          type: array
          description: 루트에서 대상까지의 경로. 마지막 원소가 대상 자신이다.
          items:
            $ref: "#/components/schemas/BreadcrumbNode"
    Change:
      type: object
      description: 이 작업이 바꾼 리소스 하나. 값이 없는 필드는 키 자체가 빠진다.
      properties:
        additions:
          type: integer
          format: int32
          description: 추가된 줄 수. 저장 시점에 계산해 둔 값이다.
          example: 12
        after_revision:
          type: integer
          format: int64
          description: 이 작업이 만든 revision. 위임·실패면 null이다.
          example: 3
        before_revision:
          type: integer
          format: int64
          description: 손대기 직전 revision. 새로 만든 것이면 null이다.
          example: 2
        change_summary:
          type: string
          description: 변경 요약
        change_type:
          type: string
          description: 변경 종류
          example: updated
        deletions:
          type: integer
          format: int32
          description: 삭제된 줄 수. 저장 시점에 계산해 둔 값이다.
          example: 4
        diff_too_large:
          type: boolean
          description: 두 본문 차이가 너무 커서 계산하지 못한 경우 true. 개별 diff로도 볼 수 없다.
          example: true
        hunks:
          type: array
          description: 실제 변경분. 비교할 짝이 없거나 계산이 거부되면 키가 빠진다.
          items:
            $ref: "#/components/schemas/Hunk"
        id:
          type: integer
          format: int64
          description: 변경 내역 ID
          example: 1
        resource_id:
          type: string
          description: 바뀐 리소스 ID
        resource_type:
          type: string
          description: 바뀐 리소스 종류
          example: wiki_page
    ChatMessageReference:
      type: object
      description: 답변의 근거가 된 원문 참조. 값이 없는 필드는 키 자체가 빠진다.
      properties:
        id:
          type: integer
          format: int64
          description: 참조 ID
          example: 1
        rank:
          type: integer
          format: int32
          description: 근거 순위(0부터). 낮을수록 강한 근거다.
          example: 0
        reference_type:
          type: string
          description: 참조 종류
        source_block_ids:
          type: array
          description: 근거가 된 원문 block ID 목록
          items:
            type: string
        source_document_id:
          type: string
          description: 근거가 된 원본 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        source_refs:
          type: array
          description: 문서·block 쌍으로 표현한 근거 위치 목록
          items:
            $ref: "#/components/schemas/ChatSourceRef"
        text:
          type: string
          description: 근거 구절 원문
    ChatMessageRelatedPageResponse:
      type: object
      description: 답변과 관련된 Wiki 페이지
      properties:
        depth:
          type: integer
          format: int32
          description: 그래프 탐색에서의 거리. 0이면 직접 매칭이다.
          example: 1
        page_type:
          type: string
          description: 페이지 종류
          example: Concept
        rank:
          type: integer
          format: int32
          description: 관련도 순위(0부터)
          example: 0
        relevance_score:
          type: number
          format: double
          description: 질문과의 관련도 점수
          example: 0.87
        role:
          type: string
          description: 이 페이지가 답변에서 맡은 역할
        slug:
          type: string
          description: URL에 쓰는 식별자
          example: search-indexing
        title:
          type: string
          description: 페이지 제목
          example: 검색 인덱싱
        wiki_page_id:
          type: string
          description: Wiki 페이지 ID
    ChatMessageResponse:
      type: object
      description: 채팅 메시지 한 건. 질문(user)과 답변(assistant)이 pair_id로 묶인다.
      properties:
        action:
          type: string
          description: AI가 고른 갈래. 화면이 이 값으로 편집 미리보기와 일반 답변을 나눈다. 질의 메시지는 키가 빠진다.
          enum:
          - chat_answer
          - conversation_reply
          - markdown_edit
          - markdown_create
          - clarify
          - reject
          - folder_organize
          - workspace_workflow
          - skill_authoring
          - skill_draft_proposal
          example: markdown_edit
        content:
          type: string
          description: 메시지 본문
        created_at:
          type: string
          format: date-time
          description: 생성 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
        error_message:
          type: string
          description: 실패했을 때의 사유. 성공이면 키가 빠진다.
        id:
          type: string
          description: 메시지 ID
        model:
          type: string
          description: 이 답변을 생성한 모델명
          example: gpt-5-nano
        pair_id:
          type: string
          description: 질문·답변을 묶는 ID. Wiki export에서 선택 단위로 쓴다.
        partial_wiki_page_ids:
          type: array
          description: 부분 선택 export로 만들어진 Wiki 페이지 ID 목록
          items:
            type: string
        provider:
          type: string
          description: 이 답변을 생성한 LLM provider. 실행 시점 값이 snapshot으로 남는다.
          example: openai
        references:
          type: array
          description: 답변의 근거가 된 원문 참조 목록
          items:
            $ref: "#/components/schemas/ChatMessageReference"
        related_pages:
          type: array
          description: 답변과 관련된 Wiki 페이지 목록
          items:
            $ref: "#/components/schemas/ChatMessageRelatedPageResponse"
        role:
          type: string
          description: 발화 주체
          enum:
          - user
          - assistant
          example: assistant
        run_id:
          type: string
          description: Agent turn이 만든 메시지의 run ID. 승인 상태와 미리보기 본문을 이 run에서 읽는다. 질의 메시지는 키가 빠진다.
          example: agent_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        status:
          type: string
          description: 처리 상태. 비동기 질의는 완료 전 pending 상태로 먼저 나타난다.
          example: completed
        web_search_enabled:
          type: boolean
          description: 이 질의에 웹 검색이 허용됐는지. 실행 당시 값이 snapshot으로 남는다.
          example: false
        wiki_page_id:
          type: string
          description: 이 메시지에서 만들어진 Wiki 페이지 ID
    ChatMessagesResponse:
      type: object
      properties:
        messages:
          type: array
          description: 세션의 메시지 기록. 오래된 것부터 온다.
          items:
            $ref: "#/components/schemas/ChatMessageResponse"
    ChatSessionCreateRequest:
      type: object
      properties:
        title:
          type: string
          description: 세션 제목. 생략하면 서버가 기본 제목을 붙인다.
          example: 검색 인덱싱 질문
    ChatSessionListResponse:
      type: object
      properties:
        sessions:
          type: array
          description: 워크스페이스의 채팅 세션 목록
          items:
            $ref: "#/components/schemas/ChatSessionResponse"
    ChatSessionResponse:
      type: object
      properties:
        created_at:
          type: string
          format: date-time
          description: 생성 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
        id:
          type: string
          description: 채팅 세션 ID
          example: session_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        last_message_at:
          type: string
          format: date-time
          description: 마지막 메시지 시각(ISO-8601 UTC). 목록 정렬에 쓴다.
          example: 2026-08-13T04:25:24.371948Z
        title:
          type: string
          description: 세션 제목
          example: 검색 인덱싱 질문
    ChatSourceRef:
      type: object
      description: 근거가 가리키는 문서·block 쌍. 한 근거가 여러 문서를 참조할 수 있어 쌍으로 표현한다.
      properties:
        source_block_id:
          type: string
          description: 문서 안에서 근거가 있는 block ID
        source_document_id:
          type: string
          description: 근거가 있는 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
    ChatWikiExportRequest:
      type: object
      description: 채팅 내용을 Wiki 페이지로 내보내는 요청
      properties:
        pair_ids:
          type: array
          description: partial일 때 내보낼 문답(pair) ID 목록. full이면 무시된다.
          items:
            type: string
        selection_mode:
          type: string
          description: "full은 세션 전체, partial은 선택한 문답만 내보낸다."
          enum:
          - full
          - partial
          example: full
    ChatWikiExportResponse:
      type: object
      description: 채팅 Wiki export 접수 결과. 실제 처리는 비동기다.
      properties:
        exportDocumentId:
          type: string
          description: 생성된 export 문서 ID. 같은 내용이 이미 있으면 기존 문서 ID다.
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        status:
          type: string
          description: "processing은 새로 등록, skipped는 같은 내용이 이미 있어 건너뜀을 뜻한다."
          enum:
          - processing
          - skipped
          example: processing
    ContributionRequest:
      type: object
      properties:
        page_ids:
          type: array
          items:
            type: string
        workspace_id:
          type: string
    ConversationContext:
      type: object
      description: 이전 대화 맥락. 대화 내용 자체는 서버가 세션에서 읽어 조립한다.
      properties:
        pendingSkillProposal:
          $ref: "#/components/schemas/PendingSkillProposal"
          description: 사용자 확인을 기다리는 Skill 제안
        referenceContext:
          type: object
          additionalProperties: {}
          description: 참조 문맥. 키·값 형태의 자유 구조다.
        selected_pair_ids:
          type: array
          description: 맥락으로 쓸 문답(pair) ID 목록. 비우면 세션의 최근 완결 문답을 쓴다. 이 세션에 속하지 않은 ID는 무시한다.
          items:
            type: string
          maxItems: 20
          minItems: 0
    DocumentAttachmentSaveResponse:
      type: object
      description: 본문 저장과 함께 등록된 이미지 asset
      properties:
        asset_id:
          type: string
          format: uuid
          description: asset ID. 내려받기 경로에 넣는다.
        attachment_id:
          type: string
          format: uuid
          description: 문서-asset 연결 ID
        content_path:
          type: string
          description: Markdown 본문에서 이 이미지를 가리키는 경로
    DocumentBlockResponse:
      type: object
      properties:
        block_id:
          type: string
          description: block ID. Wiki 근거가 이 값으로 원문 위치를 가리킨다.
        text:
          type: string
          description: block 본문 텍스트
    DocumentBlocksResponse:
      type: object
      properties:
        blocks:
          type: array
          description: 원문에서 추출한 block 목록. Wiki 근거 링크가 이 block을 가리킨다.
          items:
            $ref: "#/components/schemas/DocumentBlockResponse"
        document_id:
          type: string
          description: 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
    DocumentContentDiffResponse:
      type: object
      description: 두 버전 사이의 본문 차이. 줄 단위 hunk로 준다.
      properties:
        additions:
          type: integer
          format: int32
          description: 추가된 줄 수
          example: 12
        deletions:
          type: integer
          format: int32
          description: 삭제된 줄 수
          example: 4
        document_id:
          type: string
          description: 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        from_version:
          type: integer
          format: int64
          description: 비교 기준 버전
          example: 2
        hunks:
          type: array
          description: 변경 구간 목록
          items:
            $ref: "#/components/schemas/Hunk"
        to_version:
          type: integer
          format: int64
          description: 비교 대상 버전
          example: 3
    DocumentContentRestoreRequest:
      type: object
      description: 이전 버전 본문으로 되돌리는 요청. 되돌린 결과도 새 버전으로 쌓인다.
      properties:
        base_version:
          type: integer
          format: int64
          description: 직전에 읽은 current_version. 서버 값과 다르면 409다.
          example: 4
          minimum: 1
      required:
      - base_version
    DocumentContentSaveResponse:
      type: object
      properties:
        attachments:
          type: array
          description: 본문과 함께 저장된 이미지 asset 목록
          items:
            $ref: "#/components/schemas/DocumentAttachmentSaveResponse"
        changed:
          type: boolean
          description: 실제로 본문이 바뀌었는지 여부. 같은 내용이면 false다.
          example: true
        content_hash:
          type: string
          description: 저장된 본문의 해시. 같은 내용을 다시 저장했는지 판별에 쓴다.
        current_version:
          type: integer
          format: int64
          description: 저장 후 버전. 다음 저장의 base_version으로 쓴다.
          example: 4
        document_id:
          type: string
          description: 저장된 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        markdown:
          type: string
          description: 서버가 정규화한 뒤의 Markdown 본문
        updated_at:
          type: string
          format: date-time
          description: 저장 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
    DocumentContentVersionItem:
      type: object
      description: 본문 저장 이력 한 건
      properties:
        content_hash:
          type: string
          description: 그 시점 본문의 해시
        created_at:
          type: string
          format: date-time
          description: 저장 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
        created_by:
          type: string
          description: 저장한 사용자 ID
          example: user_3f1c8a6b52d7411e9c04ab5d2e7f6081
        version:
          type: integer
          format: int64
          description: 해당 시점의 버전 번호
          example: 3
    DocumentContentVersionListResponse:
      type: object
      properties:
        current_version:
          type: integer
          format: int64
          description: 현재 버전
          example: 4
        document_id:
          type: string
          description: 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        versions:
          type: array
          description: 저장 이력. 최신이 먼저 온다.
          items:
            $ref: "#/components/schemas/DocumentContentVersionItem"
    DocumentContentVersionResponse:
      type: object
      description: 특정 버전의 본문 스냅샷
      properties:
        content_hash:
          type: string
          description: 그 시점 본문의 해시
        created_at:
          type: string
          format: date-time
          description: 저장 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
        created_by:
          type: string
          description: 저장한 사용자 ID
          example: user_3f1c8a6b52d7411e9c04ab5d2e7f6081
        document_id:
          type: string
          description: 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        markdown:
          type: string
          description: 그 시점의 전체 Markdown 본문
        version:
          type: integer
          format: int64
          description: 이 스냅샷의 버전 번호
          example: 3
    DocumentDetailResponse:
      type: object
      description: 문서 상세. 값이 없는 필드는 키 자체가 빠지므로 존재 여부로 확인한다.
      properties:
        byte_size:
          type: integer
          format: int64
          description: 파일 크기(바이트)
          example: 482913
        current_version:
          type: integer
          format: int64
          description: 낙관적 잠금 버전. 이후 쓰기 요청의 base_version에 그대로 넣는다.
          example: 3
        display_name:
          type: string
          description: 화면에 보여줄 이름
          example: 설계문서
        document_role:
          type: string
          description: "문서 역할. Markdown은 EDITABLE, PDF는 ORIGINAL이다."
          enum:
          - EDITABLE
          - ORIGINAL
        edit_lock:
          $ref: "#/components/schemas/EditLockResponse"
          description: 다른 사용자가 편집 중이면 그 잠금 정보. 없으면 키가 빠진다.
        edit_revision:
          type: integer
          format: int64
          description: 본문 편집 회차. 내용 저장마다 증가한다.
          example: 12
        editable:
          type: boolean
          description: 본문 편집이 가능한지 여부
          example: true
        error_message:
          type: string
          description: status=failed일 때의 실패 사유
        extracted_text_uri:
          type: string
          description: 추출된 본문 텍스트가 저장된 경로. 처리 전이면 없다.
        file_type:
          type: string
          description: 확장자 기준 파일 종류
          example: pdf
        filename:
          type: string
          description: 저장된 파일명
          example: 설계문서.pdf
        id:
          type: string
          description: 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        markdown:
          type: string
          description: 편집 가능 문서의 전체 Markdown 본문
        mime_type:
          type: string
          description: 파일 MIME 타입
          example: application/pdf
        pipeline_run_id:
          type: string
          description: 이 문서를 처리한 pipeline run ID
        processed_at:
          type: string
          format: date-time
          description: 처리 완료 시각(ISO-8601 UTC). 처리 전이면 없다.
        processing_stage:
          type: string
          description: 현재 처리 단계 이름
          example: ingest
        processing_state:
          type: string
          description: 처리 진행 상태. status보다 세분화된 값이다.
          enum:
          - starting
          - running
          - stalled
          - completed
          - failed
        source_document_id:
          type: string
          description: 변환으로 만들어진 문서라면 원본 문서 ID
        source_uri:
          type: string
          description: 원본 파일이 저장된 오브젝트 스토리지 경로
        status:
          type: string
          description: 문서 처리 상태
          enum:
          - uploaded
          - processing
          - completed
          - failed
        updated_at:
          type: string
          format: date-time
          description: 마지막 변경 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
        uploaded_at:
          type: string
          format: date-time
          description: 업로드 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
        wiki_pages:
          type: array
          description: 이 문서에서 생성된 Wiki 페이지 참조 목록
          items:
            $ref: "#/components/schemas/DocumentWikiPageRef"
    DocumentDuplicateResponse:
      type: object
      description: 복제로 새로 만들어진 문서
      properties:
        byte_size:
          type: integer
          format: int64
          description: 파일 크기(바이트)
          example: 482913
        current_version:
          type: integer
          format: int64
          description: 새 문서의 시작 버전
          example: 1
        display_name:
          type: string
          description: 화면에 보여줄 이름
          example: 설계문서 (사본)
        filename:
          type: string
          description: 저장된 파일명
          example: 설계문서 (사본).pdf
        folder_id:
          type: string
          format: uuid
          description: 복제본이 놓인 폴더 ID. 루트면 null이다.
        id:
          type: string
          description: 새 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        mime_type:
          type: string
          description: 파일 MIME 타입
          example: application/pdf
        sort_order:
          type: integer
          format: int64
          description: 같은 폴더 안에서의 정렬 순서
          example: 2048
        source_document_id:
          type: string
          description: 복제 원본 문서 ID
          example: doc_8d4f1e6c3b0a97d25e4f831b9f4c7e2a
    DocumentIngestResponse:
      type: object
      description: ingest 요청 접수 결과. 실제 처리는 비동기로 진행되며 run_id로 상태를 추적한다.
      properties:
        id:
          type: string
          description: 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        run_id:
          type: string
          description: 이번 ingest를 처리하는 pipeline run ID
        status:
          type: string
          description: 접수 직후의 문서 상태
          enum:
          - uploaded
          - processing
          - completed
          - failed
    DocumentItem:
      type: object
      description: 값이 없는 필드는 키 자체가 빠진다. null 체크가 아니라 존재 여부로 확인한다.
      properties:
        area:
          type: string
          description: 문서가 속한 영역
        byte_size:
          type: integer
          format: int64
          description: 파일 크기(바이트)
          example: 482913
        current_version:
          type: integer
          format: int64
          description: 낙관적 잠금 버전. 이후 쓰기 요청의 base_version에 그대로 넣는다.
          example: 1
        display_name:
          type: string
          description: 화면에 보여줄 이름
          example: 설계문서
        document_role:
          type: string
          description: "문서 역할. Markdown은 EDITABLE, PDF는 ORIGINAL이다."
          enum:
          - EDITABLE
          - ORIGINAL
        editable:
          type: boolean
          description: 본문 편집이 가능한지 여부
          example: false
        error_message:
          type: string
          description: status=failed일 때의 실패 사유
        extracted_text_uri:
          type: string
          description: 추출된 본문 텍스트가 저장된 경로. 처리 전이면 없다.
        file_type:
          type: string
          description: 확장자 기준 파일 종류
          example: pdf
        filename:
          type: string
          description: 저장된 파일명
          example: 설계문서.pdf
        id:
          type: string
          description: 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        item_kind:
          type: string
          description: 트리에서의 항목 종류
          example: document
        mime_type:
          type: string
          description: 파일 MIME 타입
          example: application/pdf
        needs_reingest:
          type: boolean
          description: 마지막 ingest 이후 편집본이 바뀌어 재분석이 필요한지 여부. 편집 가능 문서만 true가 될 수 있다.
          example: false
        pipeline_run_id:
          type: string
          description: 이 문서를 처리한 pipeline run ID
        processed_at:
          type: string
          format: date-time
          description: 처리 완료 시각(ISO-8601 UTC). 처리 전이면 없다.
        processing_stage:
          type: string
          description: 현재 처리 단계 이름
          example: ingest
        processing_state:
          type: string
          description: 처리 진행 상태. status보다 세분화된 값이다.
          enum:
          - starting
          - running
          - stalled
          - completed
          - failed
        source_document_id:
          type: string
          description: 변환으로 만들어진 문서라면 원본 문서 ID
        source_uri:
          type: string
          description: 원본 파일이 저장된 오브젝트 스토리지 경로
        status:
          type: string
          description: 문서 처리 상태
          enum:
          - uploaded
          - processing
          - completed
          - failed
        updated_at:
          type: string
          format: date-time
          description: 마지막 변경 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
        uploaded_at:
          type: string
          format: date-time
          description: 업로드 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
    DocumentLifecycleRequest:
      type: object
      description: 문서 삭제·복구 요청. 낙관적 잠금으로 동시 변경을 막는다.
      properties:
        base_version:
          type: integer
          format: int64
          description: 직전에 읽은 current_version. 서버 값과 다르면 409 DOCUMENT_VERSION_CONFLICT다.
          example: 1
      required:
      - base_version
    DocumentLifecycleResponse:
      type: object
      properties:
        current_version:
          type: integer
          format: int64
          description: 변경 후 버전
          example: 2
        deleted:
          type: boolean
          description: 휴지통에 있는지 여부. 복구하면 false다.
          example: true
        deleted_at:
          type: string
          format: date-time
          description: 삭제 시각(ISO-8601 UTC). 복구된 경우 null이다.
          example: 2026-08-13T04:25:24.371948Z
        id:
          type: string
          description: 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        sort_order:
          type: integer
          format: int64
          description: 같은 폴더 안에서의 정렬 순서
          example: 1024
    DocumentListResponse:
      type: object
      properties:
        documents:
          type: array
          description: 활성 문서 목록. 휴지통 문서는 포함되지 않는다.
          items:
            $ref: "#/components/schemas/DocumentItem"
    DocumentPositionRequest:
      type: object
      description: 문서 이동·순서 변경 요청
      properties:
        base_version:
          type: integer
          format: int64
          description: 직전에 읽은 current_version. 서버 값과 다르면 409다.
          example: 1
        folder_id:
          type: string
          format: uuid
          description: 옮길 폴더 ID. null이면 루트로 옮긴다.
          example: 8d4f1e6c-3b0a-497d-25e4-f831b9f4c7e2
        position:
          type: integer
          format: int32
          description: 새 폴더 안에서의 0-based 삽입 위치. 생략하면 맨 뒤에 놓는다.
          example: 0
      required:
      - base_version
    DocumentPositionResponse:
      type: object
      properties:
        current_version:
          type: integer
          format: int64
          description: 이동 후 버전
          example: 2
        folder_id:
          type: string
          format: uuid
          description: 이동 후 폴더 ID. 루트면 null이다.
        id:
          type: string
          description: 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        sort_order:
          type: integer
          format: int64
          description: 이동 후 정렬 순서
          example: 1024
    DocumentRenameRequest:
      type: object
      properties:
        base_version:
          type: integer
          format: int64
          description: 직전에 읽은 current_version. 서버 값과 다르면 409 DOCUMENT_VERSION_CONFLICT다.
          example: 1
        display_name:
          type: string
          description: 새 표시 이름
          example: 이름 바꾼 회의록
      required:
      - base_version
    DocumentRenameResponse:
      type: object
      properties:
        changed:
          type: boolean
          description: 실제로 이름이 바뀌었는지 여부. 같은 이름으로 요청하면 false다.
          example: true
        current_version:
          type: integer
          format: int64
          description: 변경 후 버전. 다음 쓰기의 base_version으로 쓴다.
          example: 2
        display_name:
          type: string
          description: 변경된 표시 이름
          example: 이름 바꾼 회의록
        filename:
          type: string
          description: 저장된 파일명
          example: 회의록.md
        id:
          type: string
          description: 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        updated_at:
          type: string
          format: date-time
          description: 변경 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
    DocumentRestorePlan:
      type: object
      description: 문서 편집 되돌리기 계획. 편집 revision이 선형이라 목표 revision 하나로 끝난다.
      properties:
        document_id:
          type: string
          description: 되돌릴 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        from_version:
          type: integer
          format: int64
          description: 현재 편집 revision. 미리보기 이후 바뀌면 실행이 거절된다.
          example: 5
        to_version:
          type: integer
          format: int64
          description: 되돌릴 편집 revision. 대상 작업이 손대기 직전 값이다.
          example: 3
    DocumentTrashItem:
      type: object
      properties:
        current_version:
          type: integer
          format: int64
          description: 복구 요청의 base_version에 넣을 현재 버전
          example: 3
        delete_operation_id:
          type: string
          format: uuid
          description: 삭제를 수행한 작업 ID. 같은 작업으로 지워진 항목을 묶어 복구할 때 쓴다.
        deleted_at:
          type: string
          format: date-time
          description: 삭제 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
        deleted_by:
          type: string
          description: 삭제한 사용자 ID
          example: user_3f1c8a6b52d7411e9c04ab5d2e7f6081
        display_name:
          type: string
          description: 화면에 보여줄 이름
          example: 설계문서
        document_role:
          type: string
          description: 문서 역할
          enum:
          - EDITABLE
          - ORIGINAL
        filename:
          type: string
          description: 저장된 파일명
          example: 설계문서.pdf
        id:
          type: string
          description: 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        source_document_id:
          type: string
          description: 변환으로 만들어진 문서라면 원본 문서 ID
    DocumentTrashResponse:
      type: object
      properties:
        documents:
          type: array
          description: 삭제되어 휴지통에 있는 문서 목록
          items:
            $ref: "#/components/schemas/DocumentTrashItem"
    DocumentTreeItem:
      type: object
      description: 트리 항목. 폴더면 children이 있고 문서면 빠진다.
      properties:
        children:
          type: array
          description: 하위 항목. 문서 항목에는 키 자체가 없다.
          items:
            $ref: "#/components/schemas/DocumentTreeItem"
        current_version:
          type: integer
          format: int64
          description: 낙관적 잠금 버전. 이동·이름변경 요청의 base_version으로 쓴다.
          example: 1
        document:
          $ref: "#/components/schemas/DocumentItem"
          description: 문서 메타데이터. 목록 조회가 주는 것과 같은 항목이다. 폴더 항목에는 키 자체가 없다.
        has_children:
          type: boolean
          description: 하위 항목이 있는지 여부
          example: true
        id:
          type: string
          description: "폴더면 UUID, 문서면 doc_ 접두 ID"
        name:
          type: string
          description: "트리에 보여줄 이름. 폴더는 폴더 이름, 문서는 확장자를 포함한 파일명이다. 문서가 사람에게 보이는 제목은 document.display_name에 있다."
          example: 설계
        sort_order:
          type: integer
          format: int64
          description: 같은 부모 안에서의 정렬 순서
          example: 1024
        type:
          type: string
          description: 항목 종류
          enum:
          - folder
          - document
          example: folder
    DocumentTreeResponse:
      type: object
      properties:
        items:
          type: array
          description: 루트부터 재귀적으로 펼친 폴더·문서 트리
          items:
            $ref: "#/components/schemas/DocumentTreeItem"
    DocumentUploadResponse:
      type: object
      properties:
        byte_size:
          type: integer
          format: int64
          description: 파일 크기(바이트)
          example: 482913
        current_version:
          type: integer
          format: int64
          description: 낙관적 잠금 버전. 이후 쓰기 요청의 base_version에 그대로 넣는다.
          example: 1
        document_role:
          type: string
          description: "문서 역할. Markdown은 EDITABLE, PDF는 ORIGINAL이다."
          enum:
          - EDITABLE
          - ORIGINAL
        editable:
          type: boolean
          description: "본문 편집이 가능한지 여부. Markdown은 true, PDF는 false다."
          example: false
        filename:
          type: string
          description: 업로드된 원본 파일명
          example: 설계문서.pdf
        id:
          type: string
          description: 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        mime_type:
          type: string
          description: 파일 MIME 타입. PDF와 Markdown만 허용되며 그 외는 415다.
          example: application/pdf
        source_uri:
          type: string
          description: 원본 파일이 저장된 오브젝트 스토리지 경로
        status:
          type: string
          description: 문서 처리 상태
          enum:
          - uploaded
          - processing
          - completed
          - failed
        uploaded_at:
          type: string
          format: date-time
          description: 업로드 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
    DocumentWikiPageRef:
      type: object
      description: 이 문서에서 생성된 Wiki 페이지 참조
      properties:
        confidence:
          type: number
          format: double
          description: 관계 신뢰도(0~1)
          example: 0.87
        id:
          type: string
          description: Wiki 페이지 ID
        page_type:
          type: string
          description: 페이지 종류
          example: Concept
        relation_type:
          type: string
          description: 문서와 페이지의 관계 종류
        slug:
          type: string
          description: URL에 쓰는 식별자
          example: search-indexing
        title:
          type: string
          description: 페이지 제목
          example: 검색 인덱싱
    EditLockResponse:
      type: object
      description: 편집 잠금 상태. 잠금이 없으면 필드 자체가 빠진다.
      properties:
        expires_at:
          type: string
          format: date-time
          description: 잠금 만료 시각(ISO-8601 UTC). 이 시각이 지나면 다른 사용자가 잠글 수 있다.
          example: 2026-08-13T04:25:24.371948Z
        holder_display_name:
          type: string
          description: 현재 편집 중인 사용자의 표시 이름
          example: 표시 이름
        holder_user_id:
          type: string
          description: 현재 편집 중인 사용자 ID
          example: user_3f1c8a6b52d7411e9c04ab5d2e7f6081
    EditorSnapshot:
      type: object
      description: 편집 시작 시점의 에디터 상태
      properties:
        markdown:
          type: string
          description: 에디터에 열려 있는 전체 Markdown 본문
        target:
          $ref: "#/components/schemas/Target"
          description: 편집 대상 범위. 생략하면 문서 전체가 대상이다.
      required:
      - markdown
    ErrorDetail:
      type: object
      properties:
        code:
          type: string
          description: 분기 기준으로 쓰는 값. message가 아니라 이 값으로 분기한다.
          example: INVALID_REQUEST
        details:
          type: array
          description: 검증 실패(400 INVALID_REQUEST)일 때만 존재한다. 값이 없으면 키 자체가 빠지므로 null 체크가 아니라 존재 여부로 확인한다.
          items:
            $ref: "#/components/schemas/FieldError"
        message:
          type: string
          description: 사용자에게 그대로 보여도 되는 한국어 문구
          example: 요청 형식이 올바르지 않습니다.
    ErrorResponse:
      type: object
      description: 컨트롤러까지 도달한 요청이 실패했을 때 모든 API가 공유하는 에러 응답 형식. 인증 필터가 막는 401은 HttpStatusEntryPoint가 상태 코드만 내보내므로 이 본문이 없다.
      properties:
        error:
          $ref: "#/components/schemas/ErrorDetail"
          description: 에러 상세
    EvidenceSnippet:
      type: object
      description: 답변을 뒷받침하는 원문 구절
      properties:
        rank:
          type: integer
          format: int32
          description: 근거 순위(0부터). 낮을수록 강한 근거다.
          example: 0
        source_block_ids:
          type: array
          description: 대표 문서 안에서의 근거 block ID 목록
          items:
            type: string
        source_document_id:
          type: string
          description: 대표 근거 문서 ID. 여러 문서를 참조하면 source_refs를 본다.
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        source_refs:
          type: array
          description: 문서·block 쌍으로 표현한 전체 근거 위치. 여러 문서를 걸칠 수 있다.
          items:
            $ref: "#/components/schemas/QuerySourceRef"
        text:
          type: string
          description: 근거 구절 원문
    FieldError:
      type: object
      properties:
        field:
          type: string
          description: 문제가 된 요청 필드명
          example: email
        reason:
          type: string
          description: 그 필드의 위반 사유
          example: email은 필수입니다.
    FolderChildItem:
      type: object
      description: 폴더 바로 아래 항목. 트리와 달리 children을 담지 않는다.
      properties:
        current_version:
          type: integer
          format: int64
          description: 낙관적 잠금 버전. 이동·이름변경 요청의 base_version으로 쓴다.
          example: 1
        has_children:
          type: boolean
          description: 하위 항목이 있는지 여부. 문서는 항상 false다.
          example: false
        id:
          type: string
          description: "폴더면 UUID, 문서면 doc_ 접두 ID"
        name:
          type: string
          description: 화면에 보여줄 이름
          example: 회의록
        sort_order:
          type: integer
          format: int64
          description: 같은 부모 안에서의 정렬 순서
          example: 1024
        type:
          type: string
          description: 항목 종류
          enum:
          - folder
          - document
          example: document
    FolderChildrenResponse:
      type: object
      properties:
        items:
          type: array
          description: 해당 폴더 바로 아래 항목 목록. 손자 항목은 포함하지 않는다.
          items:
            $ref: "#/components/schemas/FolderChildItem"
    FolderCreateRequest:
      type: object
      properties:
        name:
          type: string
          description: 폴더 이름
          example: 설계
          minLength: 1
        parent_folder_id:
          type: string
          format: uuid
          description: 상위 폴더 ID. 생략하면 루트에 만든다.
          example: 8d4f1e6c-3b0a-497d-25e4-f831b9f4c7e2
      required:
      - name
    FolderLifecycleResponse:
      type: object
      properties:
        current_version:
          type: integer
          format: int64
          description: 변경 후 버전
          example: 2
        delete_operation_id:
          type: string
          format: uuid
          description: 삭제 작업 ID. 폴더와 함께 지워진 하위 항목을 한 번에 복구할 때 쓴다.
        deleted:
          type: boolean
          description: 휴지통에 있는지 여부. 복구하면 false다.
          example: true
        deleted_at:
          type: string
          format: date-time
          description: 삭제 시각(ISO-8601 UTC). 복구된 경우 null이다.
          example: 2026-08-13T04:25:24.371948Z
        id:
          type: string
          format: uuid
          description: 폴더 ID
          example: 8d4f1e6c-3b0a-497d-25e4-f831b9f4c7e2
    FolderPositionRequest:
      type: object
      description: 폴더 이동·순서 변경 요청
      properties:
        base_version:
          type: integer
          format: int64
          description: 직전에 읽은 current_version. 서버 값과 다르면 409다.
          example: 1
        parent_folder_id:
          type: string
          format: uuid
          description: 옮길 상위 폴더 ID. null이면 루트로 옮긴다.
          example: 8d4f1e6c-3b0a-497d-25e4-f831b9f4c7e2
        position:
          type: integer
          format: int32
          description: 새 부모 안에서의 0-based 삽입 위치. 생략하면 맨 뒤에 놓는다.
          example: 0
      required:
      - base_version
    FolderRenameRequest:
      type: object
      properties:
        base_version:
          type: integer
          format: int64
          description: 직전에 읽은 current_version. 서버 값과 다르면 409다.
          example: 1
        name:
          type: string
          description: 새 폴더 이름
          example: 설계 문서
          minLength: 1
      required:
      - base_version
      - name
    FolderResponse:
      type: object
      properties:
        created_at:
          type: string
          format: date-time
          description: 생성 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
        current_version:
          type: integer
          format: int64
          description: 낙관적 잠금 버전. 이후 쓰기 요청의 base_version에 그대로 넣는다.
          example: 1
        id:
          type: string
          format: uuid
          description: 폴더 ID
          example: 8d4f1e6c-3b0a-497d-25e4-f831b9f4c7e2
        name:
          type: string
          description: 폴더 이름
          example: 설계
        parent_folder_id:
          type: string
          format: uuid
          description: 상위 폴더 ID. 루트 폴더면 null이다.
        sort_order:
          type: integer
          format: int64
          description: 같은 부모 안에서의 정렬 순서
          example: 1024
        updated_at:
          type: string
          format: date-time
          description: 마지막 변경 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
    GraphContext:
      type: object
      description: 답변에 쓰인 Wiki 그래프의 부분 그래프
      properties:
        edges:
          type: array
          description: 노드 사이의 관계
          items:
            $ref: "#/components/schemas/GraphEdge"
        nodes:
          type: array
          description: 문맥에 포함된 페이지 노드
          items:
            $ref: "#/components/schemas/RelatedPage"
    GraphEdge:
      type: object
      description: 질의 문맥에서의 페이지 간 관계
      properties:
        from_page_id:
          type: string
          description: 시작 페이지 ID
        link_type:
          type: string
          description: 관계 종류
          example: related
        role:
          type: string
          description: 이 관계가 답변에서 맡은 역할
        score:
          type: number
          format: double
          description: 관계 점수
          example: 0.72
        to_page_id:
          type: string
          description: 도착 페이지 ID
    HierarchySearchMatch:
      type: object
      description: 검색 결과 한 건
      properties:
        breadcrumb:
          type: array
          description: 루트부터 이 항목까지의 경로
          items:
            $ref: "#/components/schemas/BreadcrumbNode"
        id:
          type: string
          description: "폴더면 UUID, 문서면 doc_ 접두 ID"
        name:
          type: string
          description: 화면에 보여줄 이름
          example: 회의록
        type:
          type: string
          description: 항목 종류
          enum:
          - folder
          - document
          example: document
    HierarchySearchResponse:
      type: object
      properties:
        results:
          type: array
          description: 이름 부분 일치로 찾은 폴더·문서 목록. 본문은 검색하지 않는다.
          items:
            $ref: "#/components/schemas/HierarchySearchMatch"
    Hunk:
      type: object
      description: 연속된 변경 구간 하나
      properties:
        lines:
          type: array
          description: 구간을 이루는 줄 목록
          items:
            $ref: "#/components/schemas/Line"
        new_lines:
          type: integer
          format: int32
          description: 새 본문에서 이 구간이 차지하는 줄 수
          example: 5
        new_start:
          type: integer
          format: int32
          description: 새 본문에서의 시작 줄 번호(1-based)
          example: 10
        old_lines:
          type: integer
          format: int32
          description: 이전 본문에서 이 구간이 차지하는 줄 수
          example: 3
        old_start:
          type: integer
          format: int32
          description: 이전 본문에서의 시작 줄 번호(1-based)
          example: 10
    InitialNoteRequest:
      type: object
      properties:
        user_id:
          type: string
          minLength: 1
      required:
      - user_id
    JsonNode: {}
    Line:
      type: object
      description: diff의 한 줄
      properties:
        content:
          type: string
          description: 줄 내용
        new_line:
          type: integer
          format: int32
          description: 새 본문에서의 줄 번호. 삭제된 줄이면 null이다.
          example: 10
        old_line:
          type: integer
          format: int32
          description: 이전 본문에서의 줄 번호. 추가된 줄이면 null이다.
          example: 10
        type:
          type: string
          description: 줄의 성격
          enum:
          - CONTEXT
          - DELETE
          - ADD
    MarkdownDocumentCreateRequest:
      type: object
      properties:
        display_name:
          type: string
          description: 문서 표시 이름
          example: 회의록
        folder_id:
          type: string
          format: uuid
          description: 생성 위치 폴더 ID. 생략하면 루트에 만든다.
          example: 8d4f1e6c-3b0a-497d-25e4-f831b9f4c7e2
        markdown:
          type: string
          description: 문서 전체 Markdown 본문. 5MB를 넘으면 413이다.
          example: |-
            # 회의록

            - 첫 번째 안건
    MessageSummary:
      type: object
      description: 질의로 저장된 메시지 요약
      properties:
        content:
          type: string
          description: 메시지 본문
        created_at:
          type: string
          format: date-time
          description: 생성 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
        id:
          type: string
          description: 메시지 ID
        role:
          type: string
          description: 발화 주체
          enum:
          - user
          - assistant
          example: assistant
        status:
          type: string
          description: 처리 상태
          example: completed
    ModelResponse:
      type: object
      description: 선택 가능한 모델 하나
      properties:
        display_name:
          type: string
          description: 화면에 보여줄 이름
          example: GPT-5 nano
        model:
          type: string
          description: 모델명. 요청의 model 필드에 그대로 넣는다.
          example: gpt-5-nano
        provider:
          type: string
          description: LLM provider
          enum:
          - openai
          - gemini
          - claude
          example: openai
    ModelsResponse:
      type: object
      description: 선택 가능한 모델 목록
      properties:
        models:
          type: array
          description: 활성화된 provider/model 조합 목록
          items:
            $ref: "#/components/schemas/ModelResponse"
    OperationLogDetailResponse:
      type: object
      description: AI 작업 상세. 그 작업이 바꾼 리소스를 함께 반환한다. additions·deletions는 저장 시점 값이고 hunks는 조회 시점에 계산한다.
      properties:
        changed_resource_count:
          type: integer
          format: int32
          description: 이 작업이 바꾼 리소스 수
          example: 3
        changes:
          type: array
          description: 이 작업이 바꾼 리소스별 변경 내역
          items:
            $ref: "#/components/schemas/Change"
        completed_at:
          type: string
          format: date-time
          description: 작업 완료 시각(ISO-8601 UTC). 진행 중이면 null이다.
        created_at:
          type: string
          format: date-time
          description: 작업 시작 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
        operation_id:
          type: string
          description: 작업 ID
          example: op_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        operation_type:
          type: string
          description: 작업 종류
          example: ingest
        restore:
          $ref: "#/components/schemas/RestoreSummary"
          description: 복구 작업일 때만 담기는 계획·결과 요약. 그 외에는 키가 빠진다.
        restored_from:
          type: string
          description: 복구 작업이라면 되돌린 원래 작업 ID
        status:
          type: string
          description: 작업 상태
          example: succeeded
        summary:
          type: string
          description: 작업 요약
        target_document_id:
          type: string
          description: 이 작업이 대상으로 삼은 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
    OperationLogItem:
      type: object
      description: AI 작업 로그 한 건
      properties:
        changed_resource_count:
          type: integer
          format: int32
          description: 이 작업이 바꾼 리소스 수
          example: 3
        completed_at:
          type: string
          format: date-time
          description: 작업 완료 시각(ISO-8601 UTC). 진행 중이면 null이다.
        created_at:
          type: string
          format: date-time
          description: 작업 시작 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
        operation_id:
          type: string
          description: 작업 ID. 상세 조회·복구에 쓴다.
          example: op_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        operation_type:
          type: string
          description: 작업 종류
          example: ingest
        restored_from:
          type: string
          description: 복구 작업이라면 되돌린 원래 작업 ID
        status:
          type: string
          description: 작업 상태
          example: succeeded
        summary:
          type: string
          description: 작업 요약
        target_document_id:
          type: string
          description: 이 작업이 대상으로 삼은 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
    OperationLogListResponse:
      type: object
      description: AI 작업 로그 목록. 변경분(diff)은 담기지 않으므로 상세 조회로 본다.
      properties:
        logs:
          type: array
          description: 작업 로그 목록. 최신이 먼저 온다.
          items:
            $ref: "#/components/schemas/OperationLogItem"
        next_cursor:
          type: string
          description: 다음 페이지를 받을 커서. null이면 마지막 페이지다.
    PendingSkillProposal:
      type: object
      description: 사용자 확인 대기 중인 Skill 제안
      properties:
        allowed_tools:
          type: array
          description: 검토된 Tool 허용 목록
          items:
            type: string
          maxItems: 2147483647
          minItems: 1
        capabilities:
          type: array
          description: 검토된 Skill capability
          items:
            type: string
          maxItems: 2147483647
          minItems: 1
        description:
          type: string
          description: 제안된 Skill 설명
        instructions_markdown:
          type: string
          description: 제안된 Skill의 지침 Markdown
        name:
          type: string
          description: 제안된 Skill 이름
        scope_type:
          type: string
          description: 제안된 Skill의 적용 범위 종류
      required:
      - allowed_tools
      - capabilities
    PlanPage:
      type: object
      description: 계획 상의 페이지 하나
      properties:
        action:
          type: string
          description: 이 페이지에 하기로 한 일
          enum:
          - delete
          - restore
          - rebuild
          example: rebuild
        contribution_count:
          type: integer
          format: int32
          description: 복구 후 남는 기여 수. 알 수 없으면 null이다.
          example: 2
        page_id:
          type: string
          description: Wiki 페이지 ID
    QueryRequest:
      type: object
      description: Wiki 기반 자연어 질의 요청. provider와 model은 함께 생략하거나 함께 전달해야 한다.
      properties:
        allow_web_search:
          type: boolean
          description: 이 질의에만 적용되는 웹 검색 허용 여부. true/false만 받으며 다른 값은 400이다. false면 내부 문서가 뒷받침하는 범위만 답한다.
          example: false
        model:
          type: string
          description: 모델명. provider와 짝을 이뤄야 한다.
          example: gpt-5-nano
        provider:
          type: string
          description: LLM provider. model과 짝을 이뤄야 하며 생략하면 워크스페이스 설정을 쓴다.
          enum:
          - openai
          - gemini
          - claude
          example: openai
        question:
          type: string
          description: 질문 문장
          example: 검색 인덱싱은 어떻게 동작하나요?
          minLength: 1
      required:
      - allow_web_search
      - question
    QueryResponse:
      type: object
      description: 질의 결과. 웹 검색 결과 URL과 인증 토큰은 포함되지 않는다.
      properties:
        assistant_message:
          $ref: "#/components/schemas/MessageSummary"
          description: 저장된 답변 메시지
        error_code:
          type: string
          description: 웹 검색이 실패했을 때의 사유
          enum:
          - web_search_unavailable
          - web_search_failed
        evidence_snippets:
          type: array
          description: 답변을 뒷받침하는 원문 구절 목록
          items:
            $ref: "#/components/schemas/EvidenceSnippet"
        graph_context:
          $ref: "#/components/schemas/GraphContext"
          description: 답변에 쓰인 Wiki 그래프 문맥
        related_pages:
          type: array
          description: 답변 근거가 된 Wiki 페이지 목록
          items:
            $ref: "#/components/schemas/RelatedPage"
        result_count:
          type: integer
          format: int32
          description: 근거로 쓴 결과 수
          example: 5
        traversal_paths:
          type: array
          description: 그래프를 따라간 경로. 왜 이 페이지가 근거인지 설명한다.
          items:
            $ref: "#/components/schemas/TraversalPath"
        user_message:
          $ref: "#/components/schemas/MessageSummary"
          description: 저장된 질문 메시지
        web_search_executed:
          type: boolean
          description: 실제로 웹 검색이 수행됐는지. 허용해도 실패하면 false다.
          example: false
        web_search_requested:
          type: boolean
          description: 요청에서 웹 검색을 허용했는지
          example: false
    QueryRunCreateResponse:
      type: object
      description: 비동기 질의 접수 결과. request_id로 상태를 조회하거나 SSE를 구독한다.
      properties:
        request_id:
          type: string
          description: 질의 run ID
          example: query_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        status:
          type: string
          description: 접수 직후 상태
          example: pending
    QueryRunStatusResponse:
      type: object
      description: "비동기 질의 상태. 완료 전에는 result가 없고, 실패하면 error가 채워진다."
      properties:
        error:
          type: string
          description: 실패 사유. 성공이면 키가 빠진다.
        model:
          type: string
          description: 이 질의를 처리한 모델명(실행 시점 snapshot)
          example: gpt-5-nano
        provider:
          type: string
          description: 이 질의를 처리한 LLM provider(실행 시점 snapshot)
          example: openai
        request_id:
          type: string
          description: 질의 run ID
          example: query_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        result:
          $ref: "#/components/schemas/QueryResponse"
          description: 완료된 질의의 결과. 완료 전에는 키가 빠진다.
        status:
          type: string
          description: 현재 상태
          example: completed
        web_search_enabled:
          type: boolean
          description: 이 질의에 웹 검색이 허용됐는지(실행 시점 snapshot)
          example: false
    QuerySourceRef:
      type: object
      description: 근거가 가리키는 문서·block 쌍. 한 근거가 여러 문서를 참조할 수 있어 쌍으로 표현한다.
      properties:
        source_block_id:
          type: string
          description: 문서 안에서 근거가 있는 block ID
        source_document_id:
          type: string
          description: 근거가 있는 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
    RelatedPage:
      type: object
      description: 답변 근거가 된 Wiki 페이지
      properties:
        depth:
          type: integer
          format: int32
          description: 그래프 탐색에서의 거리. 0이면 직접 매칭이다.
          example: 1
        id:
          type: string
          description: Wiki 페이지 ID
        page_type:
          type: string
          description: 페이지 종류
          example: Concept
        relevance_score:
          type: number
          format: double
          description: 질문과의 관련도 점수
          example: 0.87
        role:
          type: string
          description: 이 페이지가 답변에서 맡은 역할
        slug:
          type: string
          description: URL에 쓰는 식별자
          example: search-indexing
        title:
          type: string
          description: 페이지 제목
          example: 검색 인덱싱
    RestoreExecuteRequest:
      type: object
      description: 복구 실행 요청. 미리보기에서 받은 토큰으로 대상이 그대로인지 확인한다.
      properties:
        preview_token:
          type: string
          description: 미리보기 응답에서 받은 토큰. 그사이 대상이 바뀌었으면 409로 거절된다.
          minLength: 1
      required:
      - preview_token
    RestoreExecuteResponse:
      type: object
      description: 복구 실행 결과. Wiki 복구는 재작성이 끝나야 확정되므로 이 응답만으로 완료가 아니다.
      properties:
        delete_count:
          type: integer
          format: int32
          description: 삭제되는 Wiki 페이지 수
          example: 1
        operation_id:
          type: string
          description: 이번 복구 작업 ID. 진행 상황을 이 값으로 조회한다.
          example: op_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        rebuild_count:
          type: integer
          format: int32
          description: 남은 기여로 재작성되는 페이지 수
          example: 3
        rebuilding:
          type: boolean
          description: 재작성 결과를 기다리는 중인지 여부. false면 통지가 실패해 보류된 것이다.
          example: true
        restore_count:
          type: integer
          format: int32
          description: 이전 revision으로 되돌아가는 페이지 수
          example: 2
        restored_from:
          type: string
          description: 되돌린 대상 작업 ID
          example: op_8d4f1e6c3b0a97d25e4f831b9f4c7e2a
        run_id:
          type: string
          description: 재작성을 처리하는 run ID. 문서 편집 되돌리기에는 없다.
        status:
          type: string
          description: 복구 상태
          enum:
          - succeeded
          - rebuilding
          - notify_pending
          - queued
          example: rebuilding
    RestorePlanSummary:
      type: object
      description: 복구 계획 요약
      properties:
        delete_count:
          type: integer
          format: int32
          description: 삭제하기로 한 페이지 수
          example: 1
        pages:
          type: array
          description: 페이지별 계획
          items:
            $ref: "#/components/schemas/PlanPage"
        rebuild_count:
          type: integer
          format: int32
          description: 재작성하기로 한 페이지 수
          example: 3
        restore_count:
          type: integer
          format: int32
          description: 이전 revision으로 되돌리기로 한 페이지 수
          example: 2
    RestorePreviewPage:
      type: object
      description: 복구 대상 Wiki 페이지 하나의 계획
      properties:
        action:
          type: string
          description: 이 페이지에 할 일
          enum:
          - delete
          - restore
          - rebuild
          example: rebuild
        contribution_count:
          type: integer
          format: int32
          description: 복구 후 남는 기여 수. 삭제면 0이다.
          example: 2
        page_id:
          type: string
          description: Wiki 페이지 ID
        target_revision:
          type: integer
          format: int64
          description: restore일 때 되돌릴 revision. 그 외에는 null이다.
          example: 4
    RestorePreviewResponse:
      type: object
      description: "복구 미리보기. 되돌리기는 무를 수 없어 무엇이 바뀌는지 먼저 보여준다. Wiki 복구면 pages가, 문서 편집 복구면 document가 찬다 — 둘이 동시에 차지는 않는다."
      properties:
        delete_count:
          type: integer
          format: int32
          description: 삭제될 Wiki 페이지 수
          example: 1
        document:
          $ref: "#/components/schemas/DocumentRestorePlan"
          description: 문서 편집 복구일 때의 계획
        operation_id:
          type: string
          description: 되돌릴 대상 작업 ID
          example: op_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        pages:
          type: array
          description: Wiki 복구일 때 페이지별 계획
          items:
            $ref: "#/components/schemas/RestorePreviewPage"
        preview_token:
          type: string
          description: 실행 요청에 그대로 넣어야 하는 토큰. 그사이 대상이 바뀌면 409다.
        rebuild_count:
          type: integer
          format: int32
          description: 남은 기여로 재작성될 페이지 수
          example: 3
        restore_count:
          type: integer
          format: int32
          description: 이전 revision으로 되돌아갈 페이지 수
          example: 2
    RestoreResult:
      type: object
      description: 복구가 실제로 만들어낸 결과. 계획과 다르면 일부가 실패한 것이다.
      properties:
        deleted_count:
          type: integer
          format: int32
          description: 실제로 삭제된 페이지 수
          example: 1
        failed_count:
          type: integer
          format: int32
          description: 처리하지 못한 페이지 수
          example: 0
        rebuilt_count:
          type: integer
          format: int32
          description: 실제로 재작성된 페이지 수
          example: 3
        removed_link_count:
          type: integer
          format: int32
          description: 함께 지워진 페이지 간 링크 수
          example: 4
        restored_count:
          type: integer
          format: int32
          description: 실제로 되돌아간 페이지 수
          example: 2
        restored_link_count:
          type: integer
          format: int32
          description: 함께 되살아난 페이지 간 링크 수
          example: 2
    RestoreSummary:
      type: object
      description: 복구 작업의 계획과 실제 결과. 둘을 비교하면 계획대로 됐는지 알 수 있다.
      properties:
        plan:
          $ref: "#/components/schemas/RestorePlanSummary"
          description: 복구 전에 세운 계획
        result:
          $ref: "#/components/schemas/RestoreResult"
          description: 실제로 일어난 결과
    SettingsRequest:
      type: object
      description: AI 모델 설정 변경 요청
      properties:
        ingest_lint:
          $ref: "#/components/schemas/AiModelSelectionRequest"
          description: ingest·lint에 쓸 provider/model 조합
      required:
      - ingest_lint
    SettingsResponse:
      type: object
      description: AI 모델 설정. 기본값은 openai + gpt-5-nano다.
      properties:
        can_update:
          type: boolean
          description: 호출자가 이 설정을 변경할 수 있는지(워크스페이스 OWNER 여부)
          example: true
        ingest_lint:
          $ref: "#/components/schemas/AiModelSelectionResponse"
          description: ingest·lint에 쓰는 provider/model 조합
    SkillAuthoringRequest:
      type: object
      description: Skill 초안 작성 요청. 실제 작성은 pipeline에 위임한다.
      properties:
        authoring_mode:
          type: string
          default: enhance
          description: "기존 내용을 얼마나 살릴지. preserve는 유지, enhance는 보강, regenerate는 새로 씀."
          enum:
          - preserve
          - enhance
          - regenerate
          example: enhance
          pattern: preserve|enhance|regenerate
        description:
          type: string
          description: Skill 설명(500자 이하)
          maxLength: 500
          minLength: 0
        instruction:
          type: string
          description: 작성 지시문(30000자 이하)
          maxLength: 30000
          minLength: 0
        name:
          type: string
          description: Skill 이름. 소문자·숫자로 시작하고 하이픈을 쓸 수 있다(1~63자).
          example: meeting-notes
          pattern: "^[a-z0-9][a-z0-9-]{0,62}$"
        reference_document_ids:
          type: array
          description: 작성에 참고할 문서 ID 목록(최대 3개)
          items:
            type: string
            minLength: 1
          maxItems: 3
          minItems: 0
        scope_type:
          type: string
          description: Skill 공개 범위
          enum:
          - personal
          - team
          example: personal
          minLength: 1
          pattern: personal|team
      required:
      - instruction
      - scope_type
    SkillDraftSourceSelector:
      type: object
      description: Skill 초안의 출처가 되는 이전 run
      properties:
        run_id:
          type: string
          description: 초안 근거로 쓸 Agent run ID
          minLength: 1
      required:
      - run_id
    SkillPublishRequest:
      type: object
      description: Skill 게시 요청. 게시하면 새 버전이 만들어진다.
      properties:
        allowed_tools:
          type: array
          description: 사용자가 승인한 초안의 Tool 허용 목록
          items:
            type: string
          maxItems: 2147483647
          minItems: 1
        capabilities:
          type: array
          description: 사용자가 승인한 초안의 capability
          items:
            type: string
          maxItems: 2147483647
          minItems: 1
        description:
          type: string
          description: Skill 설명(500자 이하)
          maxLength: 500
          minLength: 0
        instructions_markdown:
          type: string
          description: Skill 지침 Markdown(30000자 이하)
          maxLength: 30000
          minLength: 0
        name:
          type: string
          description: Skill 이름. 소문자·숫자로 시작하고 하이픈을 쓸 수 있다(1~63자).
          example: meeting-notes
          minLength: 1
          pattern: "^[a-z0-9][a-z0-9-]{0,62}$"
        scope_type:
          type: string
          description: Skill 공개 범위
          enum:
          - personal
          - team
          example: personal
          minLength: 1
          pattern: personal|team
      required:
      - allowed_tools
      - capabilities
      - description
      - instructions_markdown
      - name
      - scope_type
    SkillReferenceReadRequest:
      type: object
      properties:
        document_id:
          type: string
          minLength: 1
        user_id:
          type: string
          minLength: 1
        workspace_id:
          type: string
          minLength: 1
      required:
      - document_id
      - user_id
      - workspace_id
    SkillReferenceReadResponse:
      type: object
      properties:
        document_role:
          type: string
        markdown:
          type: string
    SkillResponse:
      type: object
      description: Skill 하나. 게시된 버전(enabled_version)과 가장 최근 버전(latest_version)이 다를 수 있다.
      properties:
        enabled_version:
          allOf:
          - $ref: "#/components/schemas/SkillVersionResponse"
          description: 현재 활성화된 버전. 없으면 null이다.
        id:
          type: string
          description: Skill ID
        latest_version:
          allOf:
          - $ref: "#/components/schemas/SkillVersionResponse"
          description: 가장 최근에 만들어진 버전. 없으면 null이다.
        owner_user_id:
          type: string
          description: personal 범위일 때의 소유자 사용자 ID. team이면 null이다.
        scope_type:
          type: string
          description: 공개 범위
          enum:
          - personal
          - team
          example: personal
        slug:
          type: string
          description: URL·참조에 쓰는 식별자
          example: meeting-notes
        status:
          type: string
          description: Skill 상태
          example: published
        workspace_id:
          type: string
          description: team 범위일 때의 워크스페이스 ID. personal이면 null이다.
      required:
      - enabled_version
      - id
      - latest_version
      - owner_user_id
      - scope_type
      - slug
      - status
      - workspace_id
    SkillUpdateRequest:
      type: object
      description: 이미 게시된 Skill의 내용 수정 요청. scope_type은 바꿀 수 없다.
      properties:
        description:
          type: string
          description: Skill 설명(500자 이하)
          maxLength: 500
          minLength: 0
        instructions_markdown:
          type: string
          description: Skill 지침 Markdown(30000자 이하)
          maxLength: 30000
          minLength: 0
        name:
          type: string
          description: Skill 이름. 소문자·숫자로 시작하고 하이픈을 쓸 수 있다(1~63자).
          example: meeting-notes
          minLength: 1
          pattern: "^[a-z0-9][a-z0-9-]{0,62}$"
      required:
      - description
      - instructions_markdown
      - name
    SkillVersionResponse:
      type: object
      description: Skill의 특정 버전 내용
      properties:
        allowed_tools:
          type: array
          description: 이 Skill이 쓸 수 있는 도구 목록
          items:
            type: string
        capabilities:
          type: array
          description: 이 Skill이 수행할 수 있는 기능 목록
          items:
            type: string
        description:
          type: string
          description: Skill 설명
        id:
          type: string
          description: 버전 ID
        instructions_markdown:
          type: string
          description: Skill 지침 Markdown
        lint_result:
          type: object
          additionalProperties: {}
          description: 게시 전 점검 결과. 항목별 통과 여부가 담긴다.
        name:
          type: string
          description: Skill 이름
          example: meeting-notes
        status:
          type: string
          description: 버전 상태
          example: published
        version:
          type: integer
          format: int32
          description: 버전 번호. 게시할 때마다 1씩 올라간다.
          example: 3
      required:
      - allowed_tools
      - capabilities
      - description
      - id
      - instructions_markdown
      - lint_result
      - name
      - status
      - version
    SourceDocRef:
      type: object
      description: 노드가 참조하는 원본 문서
      properties:
        filename:
          type: string
          description: 문서 파일명
          example: 설계문서.pdf
        id:
          type: string
          description: 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
    Target:
      type: object
      description: 편집 대상 범위(1-based 줄 번호)
      properties:
        endLine:
          type: integer
          format: int32
          description: "끝 줄 번호(1부터, 포함)"
          example: 24
          minimum: 1
        startLine:
          type: integer
          format: int32
          description: 시작 줄 번호(1부터)
          example: 10
          minimum: 1
        type:
          type: string
          description: 범위 종류
          example: selection
          minLength: 1
      required:
      - type
    TraversalPath:
      type: object
      description: 그래프를 따라간 한 경로. 왜 이 페이지가 근거인지 설명한다.
      properties:
        edges:
          type: array
          description: 경로를 이루는 관계 목록(순서대로)
          items:
            $ref: "#/components/schemas/GraphEdge"
        nodes:
          type: array
          description: 경로가 지나간 페이지 ID 목록(순서대로)
          items:
            type: string
        path_id:
          type: string
          description: 경로 ID
        role:
          type: string
          description: 이 경로가 답변에서 맡은 역할
        score:
          type: number
          format: double
          description: 경로 점수
          example: 0.72
        stop_reason:
          type: string
          description: 탐색을 멈춘 이유
        used_for_answer:
          type: boolean
          description: 이 경로가 실제 답변 생성에 쓰였는지 여부
          example: true
    WikiGraphEdge:
      type: object
      description: Wiki 페이지 사이의 방향 있는 관계
      properties:
        confidence:
          type: number
          format: double
          description: 관계 신뢰도(0~1)
          example: 0.87
        from_page_id:
          type: string
          description: 시작 페이지 ID
        label:
          type: string
          description: 관계를 설명하는 짧은 문구
        link_type:
          type: string
          description: 관계 종류
          example: related
        to_page_id:
          type: string
          description: 도착 페이지 ID
    WikiGraphNode:
      type: object
      description: 그래프의 Wiki 페이지 노드. 값이 없는 필드는 키 자체가 빠진다.
      properties:
        id:
          type: string
          description: Wiki 페이지 ID
        page_type:
          type: string
          description: 페이지 종류
          example: Concept
        slug:
          type: string
          description: URL에 쓰는 식별자
          example: search-indexing
        source_document:
          $ref: "#/components/schemas/SourceDocRef"
          description: 이 페이지의 근거가 된 원본 문서
        status:
          type: string
          description: 페이지 상태
          example: published
        summary:
          type: string
          description: 페이지 요약
        title:
          type: string
          description: 페이지 제목
          example: 검색 인덱싱
    WikiGraphResponse:
      type: object
      description: "워크스페이스 Wiki 그래프 전체. 노드는 페이지, 엣지는 페이지 간 관계다."
      properties:
        edges:
          type: array
          description: 페이지 사이의 관계 목록
          items:
            $ref: "#/components/schemas/WikiGraphEdge"
        nodes:
          type: array
          description: Wiki 페이지 노드 목록
          items:
            $ref: "#/components/schemas/WikiGraphNode"
    WikiLintRequest:
      type: object
      description: Wiki 정합성 점검(lint) 실행 요청. 두 값 모두 true/false만 받는다.
      properties:
        dry_run:
          type: boolean
          description: true면 Wiki를 바꾸지 않고 점검 결과만 낸다.
          example: true
        materialize_promotions:
          type: boolean
          description: 점검에서 찾은 승격 후보를 실제로 반영할지 여부
          example: false
    WikiLintResponse:
      type: object
      description: lint 실행 접수 결과. 실제 점검은 비동기로 진행된다.
      properties:
        operation_id:
          type: string
          description: 이 점검이 만든 AI 작업 ID. dry_run이면 null이다.
        run_id:
          type: string
          description: 이번 점검을 처리하는 run ID
        status:
          type: string
          description: 접수 직후 상태
          example: queued
      required:
      - operation_id
      - run_id
      - status
    WikiMaintenanceStatusResponse:
      type: object
      description: Wiki 유지보수 상태. 값이 없는 필드는 키 자체가 빠진다.
      properties:
        last_lint_at:
          type: string
          format: date-time
          description: 마지막 lint 실행 시각(ISO-8601 UTC). 한 번도 안 돌렸으면 키가 빠진다.
          example: 2026-08-13T04:25:24.371948Z
        last_wiki_change_at:
          type: string
          format: date-time
          description: Wiki가 마지막으로 바뀐 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
        needs_lint:
          type: boolean
          description: 마지막 lint 이후 Wiki가 바뀌어 재점검이 필요한지 여부
          example: true
    WikiPageDetailResponse:
      type: object
      description: Wiki 페이지 상세. 값이 없는 필드는 키 자체가 빠진다.
      properties:
        created_at:
          type: string
          format: date-time
          description: 생성 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
        id:
          type: string
          description: Wiki 페이지 ID
        markdown:
          type: string
          description: 페이지 본문 Markdown
        markdown_uri:
          type: string
          description: 본문이 저장된 오브젝트 스토리지 경로
        page_type:
          type: string
          description: 페이지 종류
          example: Concept
        related_pages:
          type: array
          description: 그래프에서 이어진 관련 페이지 목록
          items:
            $ref: "#/components/schemas/WikiRelatedPage"
        slug:
          type: string
          description: URL에 쓰는 식별자
          example: search-indexing
        source_documents:
          type: array
          description: 이 페이지의 근거가 된 원본 문서 목록
          items:
            $ref: "#/components/schemas/WikiPageSourceDoc"
        status:
          type: string
          description: 페이지 상태
          example: published
        summary:
          type: string
          description: 페이지 요약
        title:
          type: string
          description: 페이지 제목
          example: 검색 인덱싱
        updated_at:
          type: string
          format: date-time
          description: 마지막 변경 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
    WikiPageDiffResponse:
      type: object
      description: Wiki 페이지 두 revision 사이의 변경분. hunk 구조는 문서 diff와 같다.
      properties:
        additions:
          type: integer
          format: int32
          description: 추가된 줄 수
          example: 12
        deletions:
          type: integer
          format: int32
          description: 삭제된 줄 수
          example: 4
        from_revision:
          type: integer
          format: int64
          description: 비교 기준 revision
          example: 2
        hunks:
          type: array
          description: 변경 구간 목록
          items:
            $ref: "#/components/schemas/Hunk"
        page_id:
          type: string
          description: Wiki 페이지 ID
        to_revision:
          type: integer
          format: int64
          description: 비교 대상 revision
          example: 3
    WikiPageRenameRequest:
      type: object
      properties:
        title:
          type: string
          description: 새 페이지 제목
          example: 검색 인덱싱
        update_slug:
          type: boolean
          description: 제목에 맞춰 slug도 바꿀지 여부. slug가 바뀌면 기존 링크가 깨질 수 있다.
          example: false
    WikiPageRenameResponse:
      type: object
      properties:
        id:
          type: string
          description: Wiki 페이지 ID
        page_type:
          type: string
          description: 페이지 종류
          example: Concept
        previous_slug:
          type: string
          description: 변경 전 slug
          example: indexing
        previous_title:
          type: string
          description: 변경 전 제목
          example: 인덱싱
        slug:
          type: string
          description: 변경 후 slug
          example: search-indexing
        slug_updated:
          type: boolean
          description: slug가 실제로 바뀌었는지 여부
          example: false
        title:
          type: string
          description: 변경된 제목
          example: 검색 인덱싱
        updated_at:
          type: string
          format: date-time
          description: 변경 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
    WikiPageSourceDoc:
      type: object
      description: Wiki 페이지의 근거가 된 원본 문서
      properties:
        confidence:
          type: number
          format: double
          description: 관계 신뢰도(0~1)
          example: 0.87
        filename:
          type: string
          description: 문서 파일명
          example: 설계문서.pdf
        id:
          type: string
          description: 문서 ID
          example: doc_1b9f4c7e2a8d4f1e6c3b0a97d25e4f83
        relation_type:
          type: string
          description: 문서와 페이지의 관계 종류
        source_uri:
          type: string
          description: 원본 파일이 저장된 오브젝트 스토리지 경로
    WikiRelatedPage:
      type: object
      description: 그래프에서 이어진 관련 Wiki 페이지
      properties:
        confidence:
          type: number
          format: double
          description: 관계 신뢰도(0~1)
          example: 0.87
        id:
          type: string
          description: Wiki 페이지 ID
        label:
          type: string
          description: 관계를 설명하는 짧은 문구
        link_type:
          type: string
          description: 관계 종류
          example: related
        page_type:
          type: string
          description: 페이지 종류
          example: Concept
        slug:
          type: string
          description: URL에 쓰는 식별자
          example: inverted-index
        title:
          type: string
          description: 페이지 제목
          example: 역색인
    WikiSchemaDraftRequest:
      type: object
      description: Wiki 스키마 초안 생성 요청
      properties:
        name:
          type: string
          description: 초안 이름. 생략하면 서버가 붙인다.
          example: 설계 문서 스키마
        rawMarkdown:
          type: string
          description: 스키마를 뽑아낼 원본 Markdown
          example: |-
            # 설계

            ## 구성요소
          minLength: 1
      required:
      - rawMarkdown
    WikiSchemaDraftResponse:
      type: object
      description: 초안 생성 결과
      properties:
        wiki_schema:
          $ref: "#/components/schemas/WikiSchemaResponse"
          description: 생성된 스키마 초안
      required:
      - wiki_schema
    WikiSchemaFragmentsResponse:
      type: object
      description: 스키마를 쓰이는 곳별로 쪼갠 조각. 각 조각이 해당 단계의 프롬프트에 들어간다.
      properties:
        concept_markdown:
          type: string
          description: Concept 페이지 작성 규칙
        edit_markdown:
          type: string
          description: 편집 단계에만 적용되는 규칙
        global_markdown:
          type: string
          description: 모든 단계에 공통으로 들어가는 규칙
        ingest_markdown:
          type: string
          description: 문서 ingest 단계에만 적용되는 규칙
        query_markdown:
          type: string
          description: 질의 단계에만 적용되는 규칙
        template_markdown:
          type: string
          description: 페이지 템플릿 정의
      required:
      - concept_markdown
      - edit_markdown
      - global_markdown
      - ingest_markdown
      - query_markdown
      - template_markdown
    WikiSchemaIssueResponse:
      type: object
      description: 스키마 해석 중 발견한 문제 하나
      properties:
        category:
          type: string
          description: 문제 분류
        reason:
          type: string
          description: 왜 문제인지에 대한 설명
        section:
          type: string
          description: 문제가 발견된 섹션 이름. 특정할 수 없으면 null이다.
        severity:
          type: string
          description: 심각도. blocked면 활성화할 수 없고 unclear는 경고다.
          enum:
          - blocked
          - unclear
          example: unclear
        text:
          type: string
          description: 문제가 된 원문 구절
      required:
      - category
      - reason
      - severity
      - text
    WikiSchemaPreviewRequest:
      type: object
      description: Wiki 스키마 미리보기 요청. 저장하지 않고 결과만 돌려준다.
      properties:
        rawMarkdown:
          type: string
          description: 스키마를 뽑아낼 원본 Markdown
          example: |-
            # 설계

            ## 구성요소
          minLength: 1
      required:
      - rawMarkdown
    WikiSchemaPreviewResponse:
      type: object
      description: 저장하지 않고 스키마 해석 결과만 보여주는 응답
      properties:
        fragments:
          $ref: "#/components/schemas/WikiSchemaFragmentsResponse"
          description: 용도별로 쪼갠 스키마 조각
        has_blocked_issues:
          type: boolean
          description: 활성화를 막는 blocked 문제가 있는지 여부
          example: false
        issues:
          type: array
          description: 해석 중 발견한 문제 목록
          items:
            $ref: "#/components/schemas/WikiSchemaIssueResponse"
        preview_markdown:
          type: string
          description: 해석 결과를 사람이 읽도록 정리한 Markdown
      required:
      - fragments
      - has_blocked_issues
      - issues
      - preview_markdown
    WikiSchemaResponse:
      type: object
      description: Wiki 스키마 하나. 활성화되면 Wiki 생성·질의에 쓰인다.
      properties:
        activated_at:
          type: string
          description: 활성화된 시각(ISO-8601 UTC). 활성화 전이면 null이다.
        created_at:
          type: string
          description: 생성 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
        fragments:
          $ref: "#/components/schemas/WikiSchemaFragmentsResponse"
          description: 용도별로 쪼갠 스키마 조각
        has_blocked_issues:
          type: boolean
          description: 활성화를 막는 blocked 문제가 있는지 여부
          example: false
        id:
          type: string
          description: 스키마 ID
        issues:
          type: array
          description: 해석 중 발견한 문제 목록
          items:
            $ref: "#/components/schemas/WikiSchemaIssueResponse"
        name:
          type: string
          description: 스키마 이름
          example: 설계 문서 스키마
        preview_markdown:
          type: string
          description: 해석 결과를 사람이 읽도록 정리한 Markdown
        raw_markdown:
          type: string
          description: 입력으로 준 원본 Markdown
        schema_version:
          type: string
          description: 스키마 형식 버전
          example: v1
        status:
          type: string
          description: 스키마 상태
          example: draft
        updated_at:
          type: string
          description: 마지막 변경 시각(ISO-8601 UTC)
          example: 2026-08-13T04:25:24.371948Z
        user_id:
          type: string
          description: 초안을 만든 사용자 ID
          example: user_3f1c8a6b52d7411e9c04ab5d2e7f6081
        workspace_id:
          type: string
          description: 이 스키마가 속한 워크스페이스 ID
          example: ws_9d47a0e9a6324341b47562553b75f92a
      required:
      - fragments
      - has_blocked_issues
      - id
      - issues
      - name
      - preview_markdown
      - raw_markdown
      - schema_version
      - status
      - user_id
      - workspace_id
  securitySchemes:
    bearerAuth:
      bearerFormat: JWT
      name: bearerAuth
      scheme: bearer
      type: http
