apiVersion: apps/v1
kind: Deployment
metadata:
  name: access-svc
  namespace: fruition
  labels:
    app: access-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: access-svc
  template:
    metadata:
      labels:
        app: access-svc
    spec:
      containers:
        - name: access-svc
          # 로컬 빌드 이미지 (kind load docker-image로 주입)
          image: fruition-access-svc:dev
          imagePullPolicy: Never
          ports:
            - containerPort: 8081
            # actuator 전용. 아래 env의 MANAGEMENT_PORT와 같아야 한다.
            - containerPort: 8082
          # 관리 포트를 probe 옆에 못박는다. application.properties 기본값은 서비스마다
          # 다른데(document 8082 / access 8083) k8s는 ALB healthcheck-port 주석이
          # Ingress 단위라 두 서비스가 같은 번호를 써야 한다. envFrom보다 우선한다.
          env:
            - name: ACCESS_DB_RUNTIME_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: fruition-secret
                  key: ACCESS_DB_RUNTIME_PASSWORD
            - name: ACCESS_DB_MIGRATION_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: fruition-secret
                  key: ACCESS_DB_MIGRATION_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: fruition-secret
                  key: JWT_SECRET
            - name: INTERNAL_CALLBACK_TOKEN
              valueFrom:
                secretKeyRef:
                  name: fruition-secret
                  key: INTERNAL_CALLBACK_TOKEN
            - name: MFA_ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: fruition-mfa
                  key: MFA_ENCRYPTION_KEY
            - name: MANAGEMENT_PORT
              value: "8082"
          envFrom:
            - configMapRef:
                name: fruition-config
          # 로컬에서는 자기 access_db migration 후 기동한다. AWS는 별도 Job으로 실행한다.
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            periodSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            periodSeconds: 15
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              memory: 1024Mi
---
apiVersion: v1
kind: Service
metadata:
  name: access-svc
  namespace: fruition
spec:
  # 클러스터 내부에서는 http://access-svc:8081, 호스트에서는 http://localhost:30081
  type: NodePort
  selector:
    app: access-svc
  ports:
    - port: 8081
      targetPort: 8081
      nodePort: 30081
