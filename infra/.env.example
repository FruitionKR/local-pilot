APP_ENV=development
WEB_ORIGIN=http://localhost:5173

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=fruition_mvp
POSTGRES_USER=fruition
POSTGRES_PASSWORD=fruition_dev_password
DATABASE_URL=postgresql://fruition:fruition_dev_password@localhost:5432/fruition_mvp

S3_ENDPOINT=http://localhost:9000
S3_PUBLIC_ENDPOINT=http://localhost:9000
S3_REGION=ap-northeast-2
S3_BUCKET=fruition-storage
S3_ACCESS_KEY=fruition
S3_SECRET_KEY=fruition_dev_secret
S3_FORCE_PATH_STYLE=true

LLM_PROVIDER=openai
LLM_MODEL=gpt-4.1-mini
OPENAI_API_KEY=

# Pipeline API
PROCESSING_ENDPOINT=http://localhost:8000/pipeline/runs
QUERY_ENDPOINT=http://localhost:8000/query
QUERY_TIMEOUT_SECONDS=30
WIKI_SCHEMA_ENDPOINT=http://localhost:8000/wiki-schema
WIKI_SCHEMA_TIMEOUT_SECONDS=30

# Query run SSE callback (pipeline container -> backend on host, via Docker Desktop's host gateway)
CALLBACK_BASE_URL=http://host.docker.internal:8080
PIPELINE_API_PORT=8000
UPSTAGE_API_KEY=
UPSTAGE_MODEL=solar-pro2
UPSTAGE_BASE_URL=https://api.upstage.ai/v1
QUERY_EMBEDDING_MODE=text-only

# LangSmith tracing
LANGSMITH_TRACING=false
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=local-pilot-dev
# Default US: https://api.smith.langchain.com
# APAC/Sydney: https://apac.api.smith.langchain.com
# EU: https://eu.api.smith.langchain.com
# AWS US: https://aws.api.smith.langchain.com
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
# LangSmith에서 query evaluator graph를 보려면 LANGSMITH_TRACING=true와 함께 llm으로 설정합니다.
QUERY_EVALUATOR_MODE=disabled
QUERY_EVALUATOR_MAX_ATTEMPTS=2

# Query web fallback
# disabled | tavily
QUERY_WEB_SEARCH_MODE=disabled
TAVILY_API_KEY=
QUERY_WEB_SEARCH_MAX_RESULTS=5
QUERY_WEB_SEARCH_TIMEOUT_SECONDS=20
QUERY_MIN_INTERNAL_RELEVANCE_SCORE=0.5

# JWT
JWT_SECRET=
JWT_ACCESS_EXPIRATION_SECONDS=900
JWT_REFRESH_EXPIRATION_SECONDS=1209600

# 로컬 이메일 인증 고정 코드. 운영 환경에서는 설정하지 않습니다.
AUTH_EMAIL_DEV_FIXED_CODE=9700

# OAuth2 (docs/spec/oauth-provider-setup.md 참고)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=
OAUTH_FRONTEND_REDIRECT_URI=http://localhost:3000/oauth/callback
CORS_ALLOWED_ORIGINS=http://localhost:3000
