# 2026-07-25 해결 이슈 기록

## Backend — Markdown 원문 내보내기

### 해결 내용

- `GET /api/workspaces/{workspace_id}/documents/{document_id}/export`를 구현했다.
- 활성 workspace 멤버가 최신 Markdown 편집본을 UTF-8 `.md`로 내려받을 수 있다.
- 한글 파일명과 문서 상태 불변을 service·controller·PostgreSQL 통합 테스트로 검증했다.
- `./gradlew clean test` 전체 246개 테스트가 통과했다.

### 확인 위치

- `backend/src/main/java/fruition/document/controller/DocumentController.java` — `GET /{document_id}/export`
- `backend/src/main/java/fruition/document/service/DocumentExportService.java`
- `docs/spec/sdd/markdown-document-core.md` — `REQ-019 Markdown 내보내기`
- `docs/spec/sdd/tasks/markdown-document-core-tasks.md` — `TASK-009 Markdown 원문 내보내기`

## Backend — wiki-schema Java 프록시

### 해결 내용

- llmPipeline `wiki_schema` 모듈을 workspace 범위 public API로 중계하는 Spring 프록시를 추가했다.
- endpoint 4개를 `AgentTurnController`/`PipelineAgentRequester` 패턴 그대로 구현했다.
  - `POST /api/workspaces/{workspace_id}/wiki-schema/preview` → llmPipeline `POST /wiki-schema/preview`
  - `POST …/wiki-schema/drafts` → llmPipeline `POST /wiki-schema/drafts` (`workspace_id`는 path, `user_id`는 `@AuthenticationPrincipal`에서 주입)
  - `POST …/wiki-schema/{schema_id}/activate` → llmPipeline `POST /wiki-schema/{schema_id}/activate`
  - `GET …/wiki-schema/active` → llmPipeline `GET /wiki-schema/active?workspace_id&user_id` (활성 스키마가 없으면 `null` 그대로 반환)
- workspace 멤버십을 검증하고 비멤버는 `WorkspaceNotFoundException`(404)으로 차단한다.
- 오류 매핑은 `PipelineWikiSchemaException`/`GlobalExceptionHandler`로 처리한다. pipeline의 400/422/404는 원본 detail을 보존하고, 그 외는 503(`WIKI_SCHEMA_PIPELINE_UNAVAILABLE`)으로 변환한다.
- pipeline base URL 설정 `app.wiki-schema.endpoint`(`WIKI_SCHEMA_ENDPOINT`)와 timeout을 추가했다.

### 검증

- requester(snake_case 변환·null name 생략·활성 없음 시 `null` 통과·422/404 body 보존·500→503), service(멤버십·위임·비멤버 차단), controller(4개 endpoint·미인증 401·blank 입력 400) 테스트를 추가했다.
- `./gradlew test` 전체가 통과했다.

### 확인 위치

- `backend/src/main/java/fruition/wikischema/controller/WikiSchemaController.java`
- `backend/src/main/java/fruition/wikischema/service/WikiSchemaService.java`
- `backend/src/main/java/fruition/wikischema/repository/PipelineWikiSchemaRequester.java`
- `backend/src/main/java/fruition/util/GlobalExceptionHandler.java` — `handlePipelineWikiSchema`
- `backend/src/main/resources/application.properties` — `app.wiki-schema.*`
- llmPipeline 계약: `llmPipeline/app/modules/wiki_schema/interfaces/http/routes.py`, `.../schemas.py`
- 남은 Frontend 작업(목업 교체): `frontend/app/_lib/api/schema.ts`, `frontend/app/_components/schema/`
