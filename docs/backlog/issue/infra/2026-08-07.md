# 2026-08-07 infra 이슈 — AWS 전환 잔여 작업

AWS 전환 IaC·overlay·CI가 추가됨 (`infra/terraform/`, `k8s/overlays/aws/`, `.github/workflows/deploy.yml`).
실배포 전 아래 작업 필요. 절차 상세: `infra/terraform/README.md`, `k8s/overlays/aws/README.md`.

## 1. terraform apply (담당: infra)

- ~~terraform CLI 설치·validate~~ 완료 (2026-08-07): terraform 1.15.8 + awscli 설치,
  `terraform init && terraform validate && terraform fmt` 통과.
- **AWS 자격증명 미설정** — `aws login`(또는 `aws configure`/SSO) 후
  `terraform plan/apply -var budget_email=<이메일>` 진행. 여기서 블록 중.
- 원격 state(S3 backend) 결정 필요 — 현재 로컬 state (`versions.tf` 주석 참조).

## 2. 수동 설정 (apply 후)

- Secrets Manager `fruition/app`의 `JWT_SECRET`·`INTERNAL_CALLBACK_TOKEN`·`AGENT_INTERNAL_TOKEN`·`LLM_API_KEY` 교체 (CHANGE_ME 상태로는 기동 불가).
- ACM 인증서 발급(api.·access. 도메인) + Route 53 alias 연결.
- GitHub repo Variable `AWS_DEPLOY_ROLE_ARN` 등록.
- Vercel env: `NEXT_PUBLIC_BACKEND_URL`, `NEXT_PUBLIC_ACCESS_URL`.
- `k8s/overlays/aws/`의 `REPLACE_ME_*` 치환 (README 표 참조).
- helm addon 설치: ALB Controller → External Secrets → Cluster Autoscaler → Strimzi·KEDA.

## 3. 후속 구조 작업

- `pipeline-runs` PVC(RWO) → S3 아티팩트 저장 이전. 완료 시 ingest-worker podAffinity 제거,
  AI Worker Spot 노드(taint `fruition.io/ai-worker`) 분리 활성화 — `k8s/overlays/aws/kustomization.yaml` 주석 참조.
- Access/Core 물리 DB 분할 (현재 단일 RDS).
- S3 정적 키 → IRSA (앱이 default credential chain 지원하도록 코드 수정 필요).

## 4. 기존 파일 참고

- ~~`.github/workflows/tests.yml`의 `services/backend/**` stale 경로~~ **수정 완료 (2026-08-07)**:
  경로를 access-svc·document-svc·java-shared로 갱신, backend job은 `services/`에서
  3개 모듈 테스트 실행. 각 job 명령 로컬 실행 검증 완료 — push 트리거 실동작만 GitHub에서 확인 필요.
