# AI(llmPipeline) 해결 기록 — 2026-08-05

## AI 작업 결과 콜백 E2E 확인

격리된 PostgreSQL·MinIO에 Backend와 llmPipeline을 함께 실행하고 두 서비스에 같은 `INTERNAL_CALLBACK_TOKEN`을 주입했습니다. Backend는 `AIHISTORY_INGEST_LOGGING_ENABLED=true`로 기동했습니다.

- [x] Markdown 업로드 후 재ingest 요청이 `202`를 반환하고 llmPipeline 실행이 시작됨
- [x] llmPipeline 결과 콜백이 `X-Internal-Token` 인증을 통과해 `200`을 받음
- [x] 작업 로그 `op_3CjiIbTE6NCnWjYumPbYdw`가 `partially_succeeded`로 확정됨

외부 Upstage API key가 결제 문제로 `401 api_key_is_blocked`를 반환해 Wiki 변경은 0건이었지만, 파이프라인은 실패 결과를 정상 수집해 Backend 콜백까지 완료했습니다. 따라서 이번 확인 범위인 ingest 작업 등록·내부 토큰 전송·결과 수신·종결 상태 전이는 모두 통과했습니다.

## 복구 삭제 페이지의 링크·임베딩 정리

Ingestion·Lint 복구의 `deleted_pages`를 callback 전에 정리하도록 llmPipeline 복구 저장소를 연결했습니다.

정리 대상:

- `wiki_page_links`의 from/to 양방향 관계
- `document_wiki_links`
- `wiki_embedding_units`, 참조가 사라진 `wiki_embedding_vectors`
- 이전 단일 Page 임베딩인 `wiki_page_embeddings`
- `wiki_pages.status='deleted'`

완료 조건:

- [x] `deleted_pages`에 연결된 양방향 Wiki link와 Document link 제거
- [x] `deleted_pages`의 embedding unit·미참조 vector 제거
- [x] 같은 정리를 두 번 실행해도 결과 동일
- [x] 삭제되지 않은 페이지의 link·embedding과 `active` 상태 유지

검증:

- llmPipeline Wiki ingestion 테스트: `119 passed`
- 격리 PostgreSQL에 삭제·유지 Page를 함께 구성하고 정리를 두 번 실행
- 삭제 Page는 `deleted`, 관련 link·unit·vector·legacy embedding은 0건
- 유지 Page는 `active`, Document link·unit·vector·legacy embedding이 각각 유지됨

관련 위치:

- 복구 흐름: `llmPipeline/app/modules/wiki_ingestion/application/restore_wiki_pages.py`
- PostgreSQL 정리: `llmPipeline/app/modules/wiki_ingestion/infrastructure/postgres_wiki_ingestion_repository.py`
- API 계약: `docs/spec/llmpipeline-backend-api-contract.md` — §4.10, §4.11

## `INTERNAL_CALLBACK_TOKEN` llmPipeline 양방향 적용

Backend가 호출하는 llmPipeline 업무 route에 `X-Internal-Token` 검증을 적용했습니다. `INTERNAL_CALLBACK_TOKEN` 미설정은 `503`, 헤더 누락·불일치는 `401`이며 `/health`는 인증에서 제외했습니다.

llmPipeline이 Backend로 보내는 진행 로그와 Query event callback에도 기존 작업 결과 callback과 같은 헤더를 추가했습니다.

완료 조건:

- [x] 보호 route가 토큰 누락·불일치 시 요청 본문 처리 전에 `401` 반환
- [x] 일치하는 토큰으로 기존 업무 route 처리
- [x] 진행 로그·Query event·작업 결과 callback에 같은 헤더 전송
- [x] `/health`는 토큰 없이 `200` 유지

검증: 관련 API·callback 테스트 `46 passed`. Backend 송신·callback 검증 잔여 작업은 `docs/backlog/issue/backend/2026-08-05.md`에서 관리합니다.
