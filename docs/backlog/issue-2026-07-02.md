# 이전 이슈 자료

이 문서는 workspace 격리·계약 정합(B1~B3, B5~B7, F5)은 PR 58/60/62/66/67로 해결된 이슈 기록입니다. source_refs llmPipeline 측 변경 기록은 `docs/changelog/ai.md` 2026-07-02 항목에 이미 반영되어 있습니다. 잔여분(lint proxy B4/B8, maintenance UI F1~F4/F6~F7, source_refs backend/frontend 소비)은 `docs/issue/2026-07-15.md`에서 관리합니다.
현재 활성 이슈는 `docs/issue/`의 최신 날짜 문서를 기준으로 확인합니다.

---

# 2026-07-02 이슈 정리

## LLM Wiki cluster promotion/lint 연동 후속 작업

### 배경

이번 변경으로 llmPipeline의 wiki ingest/lint 흐름이 바뀌었다.

기존에는 section/mention 후보가 source/concept page 주변에만 남고, lint가 실질적인 유지보수 작업을 하지 못했다. 변경 후에는 section/mention/evidence 후보가 `clusters/active.md`에 누적되고, lint가 pending 상태를 읽어 promotion, archive 이동, relation materialization을 수행한다.

핵심 정책:

- section/mention/evidence 후보는 같은 ingest pass에서 즉시 core로 승격하지 않는다.
- 새 cluster는 promotion candidate가 될 수 없다.
- 기존 active cluster에 claim/source가 누적되고 LLM이 재사용 가능한 core로 판단한 경우에만 `Promotion.status: candidate`가 생긴다.
- lint dry-run은 proposal만 보여준다.
- lint 실행은 promotion concept page 생성, active cluster 제거, archive 이동, materializable relation link 생성을 수행한다.

### 현재 llmPipeline 변경 사항

관련 코드:

- `llmPipeline/api.py`
- `llmPipeline/run_lab.py`
- `llmPipeline/app/modules/wiki_generation/infrastructure/assemble.py`
- `llmPipeline/app/modules/wiki_ingestion/infrastructure/postgres_wiki_ingestion_repository.py`
- `llmPipeline/prompts/semantic_extraction.system.md`
- `llmPipeline/prompts/wiki_generation_evaluator.system.md`
- `llmPipeline/tests/modules/wiki_generation/test_source_extraction_artifact.py`
- `llmPipeline/tests/modules/wiki_ingestion/test_concept_index.py`

주요 API 변화:

- `POST /pipeline/runs` request에 `user_id`, `workspace_id`가 추가됐다.
- `POST /pipeline/runs` manifest에 `maintenance_summary`가 추가된다.
- `POST /wiki/maintenance/lint`가 추가됐다.
- `POST /wiki/maintenance/lint`는 `dry_run` 여부에 따라 proposal 조회 또는 실제 materialization을 수행한다.

구현상 반드시 유지해야 하는 세부 계약:

- `wiki_pages.id`는 `source:{id}` 또는 `concept:{slug}`가 아니라 opaque id다. 현재 llmPipeline은 `wiki_page_{uuid}` 형태를 생성하지만, backend/frontend는 문자열 형식에 의존하면 안 된다.
- source/concept page의 중복 판단은 id가 아니라 `(user_id, workspace_id, page_type, slug)`로 한다.
- 기존 concept과 같은 후보는 cluster를 만들지 않고 기존 concept page의 `## Evidence`에 병합한다.
- promotion candidate가 기존 concept slug와 같으면 새 page를 만들지 않고 기존 concept에 병합한 뒤 active cluster를 archive로 옮긴다.
- promotion candidate가 새 concept이면 lint 실행 시 LLM concept page generator로 page를 만들고 active cluster를 archive로 옮긴다.
- lint 실행은 promotion뿐 아니라 `active.md` 전체의 materializable relation candidate도 처리한다.
- `related_evidence`는 core graph edge로 materialize하지 않고 후보/근거 수준에 남긴다.
- ref 없는 evidence claim 또는 promotion은 invalid로 분류되어 materialization 대상에서 제외된다.
- lint 실행 결과는 `clusters/active.md`, `clusters/archived.md`, `logs/{yyyy-mm-dd}.md`, `wiki_pages`, `wiki_page_links`, embedding unit/vector까지 함께 바꿀 수 있다.
- meaning cluster 정리본에는 `### Evidence Claims`, `### Core Relation Candidates`, `### Promotion`만 유지한다. cluster 판단용 `### Summary`, `### Observations`는 다시 추가하지 않는다.

### llmPipeline 계약

#### POST /pipeline/runs 요청

Backend 문서 처리 요청 시 다음 값을 포함해야 한다.

```json
{
  "input_markdown": "...",
  "input_name": "paper.md",
  "wait": false,
  "provider": "upstage",
  "user_id": "local-user",
  "workspace_id": "local-workspace",
  "source_page_mode": "section-polish",
  "concept_page_mode": "skeleton",
  "log_callback_url": "http://backend:8080/api/documents/{document_id}/pipeline-events/callback"
}
```

Backend가 아직 workspace/user를 실제 계정 모델로 관리하지 않는다면 임시 기본값을 한 곳에서 정의한다.

```text
user_id: local-user
workspace_id: local-workspace
```

#### POST /pipeline/runs 응답 중 maintenance_summary

ingest 완료 manifest에는 다음 형태가 포함된다.

```json
{
  "maintenance_summary": {
    "promotion_candidate_count": 2,
    "promotion_candidates": [
      {
        "cluster_id": "anova-analysis",
        "representative": "ANOVA Analysis",
        "source_refs": ["api-inline-c36e9f1b-4851-46f3-beea-dd5d2f4a55aa"],
        "reason": "기존 active cluster에 근거가 누적되어 후보 승격"
      }
    ],
    "relation_candidate_count": 9,
    "relation_candidates": [
      {
        "cluster_id": "anova-analysis",
        "target": "concept:robust-design",
        "relation": "uses_or_depends_on",
        "evidence": ["claim_a555af46_002"]
      }
    ],
    "invalid_candidate_count": 0,
    "invalid_candidates": [],
    "invalid_promotion_count": 0,
    "invalid_promotions": [],
    "lint_action_available": true
  }
}
```

이 값은 사용자가 “생성은 끝났지만 유지보수 proposal이 남아 있다”는 것을 볼 수 있게 하는 UI 신호다.

#### POST /wiki/maintenance/lint 요청

proposal 조회:

```json
{
  "user_id": "local-user",
  "workspace_id": "local-workspace",
  "dry_run": true,
  "materialize_promotions": true,
  "provider": "upstage"
}
```

실행:

```json
{
  "user_id": "local-user",
  "workspace_id": "local-workspace",
  "dry_run": false,
  "materialize_promotions": true,
  "provider": "upstage"
}
```

#### POST /wiki/maintenance/lint 응답

```json
{
  "user_id": "local-user",
  "workspace_id": "local-workspace",
  "active_path": "wiki/local-user/local-workspace/clusters/active.md",
  "cluster_count": 54,
  "source_ref_count": 59,
  "orphan_refs": [],
  "promotion_candidates": ["anova-analysis", "multi-objective-optimization"],
  "needs_review": [],
  "relation_candidates": [
    {
      "cluster_id": "anova-analysis",
      "target": "concept:robust-design",
      "relation": "uses_or_depends_on",
      "evidence": ["claim_a555af46_002"]
    }
  ],
  "invalid_relations": [],
  "invalid_promotions": [],
  "materialized_promotions": [
    {
      "cluster_id": "anova-analysis",
      "concept_slug": "anova-analysis",
      "page_id": "wiki_page_eade5a66-f23f-4da4-a3da-5fceb25b254e"
    }
  ],
  "merged_promotions": [],
  "materialized_relations": [
    {
      "from": "anova-analysis",
      "to": "robust-design",
      "relation": "uses_or_depends_on",
      "evidence": ["claim_a555af46_002"],
      "source_refs": [
        "api-inline-c36e9f1b-4851-46f3-beea-dd5d2f4a55aa:B0008"
      ]
    }
  ]
}
```

### Backend 후속 작업

#### B1. wiki_pages schema를 llmPipeline과 맞춘다

현재 Spring domain은 `wiki_pages` unique key를 `(page_type, slug)`로 본다.

```java
@UniqueConstraint(
    name = "uq_wiki_pages_type_slug",
    columnNames = {"page_type", "slug"}
)
```

llmPipeline은 workspace/user 범위에서 slug uniqueness를 사용한다.

필요 변경:

- `wiki_pages`에 `user_id`, `workspace_id` 컬럼을 domain/migration에 반영한다.
- unique key를 `(user_id, workspace_id, page_type, slug)`로 맞춘다.
- `WikiPageRepository.findByPageTypeAndSlug(...)`는 workspace/user scope를 받도록 바꾼다.
- rename slug conflict 검사도 workspace/user scope 안에서만 수행한다.

검증:

- 같은 slug의 concept page가 서로 다른 workspace에 공존할 수 있다.
- 같은 workspace/user 안에서는 같은 page_type/slug 중복이 막힌다.
- `docs/spec/backend-mvp-erd.md`와 `docs/Fruition_MVP_API_Contract.md`의 `wiki_pages.id` 설명에서 `source:xxx`, `concept:xxx` 고정 prefix 전제를 제거한다.
- API 계약 문서에 `user_id`, `workspace_id`, scoped unique key를 반영한다.

#### B2. source page id를 `source:{documentId}`로 가정하는 코드를 제거한다

이번 llmPipeline은 wiki page id를 UUID 형태로 만든다.

```text
wiki_page_eade5a66-f23f-4da4-a3da-5fceb25b254e
```

현재 `DocumentService.delete()`는 source page id를 다음처럼 가정한다.

```java
wikiPageRepository.findById("source:" + documentId)
```

필요 변경:

- source page 삭제는 `document_wiki_links`에서 `document_id + relation_type=source_of`로 연결된 wiki page를 찾아 삭제한다.
- source page id 문자열 구조에 의존하지 않는다.
- concept page는 공유 가능하므로 문서 삭제만으로 삭제하지 않는다.

검증:

- UUID source page id에서도 문서 삭제 시 source page와 관련 link가 삭제된다.
- concept page는 삭제되지 않는다.

#### B3. processing request에 user_id/workspace_id를 포함한다

현재 `DocumentProcessingRequester`는 `/pipeline/runs` 요청 body를 JSON 문자열로 직접 조립한다.

```java
String body = "{\"document_id\":\"" + documentId + "\""
        + (callbackUrl != null ? ",\"log_callback_url\":\"" + callbackUrl + "\"" : "")
        + "}";
```

이 방식은 새 필드 추가와 escaping에 취약하므로 DTO/record 기반 request body로 바꾼다.

필요 변경:

- `PipelineRunRequest` record를 추가한다.
- request body에 `user_id`, `workspace_id`를 추가한다.
- 가능하면 `source_page_mode`, `concept_page_mode`, `provider`도 backend 설정값으로 명시한다.
- 임시 기본값을 쓰더라도 `DocumentProcessingRequester` 내부에 하드코딩하지 말고 config 또는 workspace resolver를 둔다.
- callback 저장 시 manifest의 `maintenance_summary`를 문서 상태 또는 별도 endpoint에서 조회 가능하게 할지 결정한다.

검증:

- pipeline run 생성 후 `wiki_pages.user_id/workspace_id`가 backend 기대 scope와 일치한다.
- `GET /api/wiki/graph`에서 해당 workspace의 page만 반환한다.

#### B4. lint proxy endpoint를 추가한다

Frontend가 FastAPI를 직접 호출하지 않도록 Spring backend가 proxy endpoint를 제공한다.

권장 endpoint:

```text
POST /api/wiki/maintenance/lint
```

역할:

- `POST dry_run=true`: proposal 조회
- `POST dry_run=false`: promotion/relation materialization 실행

Spring DTO:

- `WikiMaintenanceLintRequest`
- `WikiMaintenanceLintResponse`
- `WikiPromotionCandidate`
- `WikiRelationCandidate`
- `WikiMaterializedPromotion`
- `WikiMergedPromotion`
- `WikiMaterializedRelation`

검증:

- dry-run 호출은 DB graph를 바꾸지 않는다.
- 실행 호출 후 `GET /api/wiki/graph`의 concept node/link가 증가한다.
- FastAPI 장애 시 frontend에 표시 가능한 에러 메시지를 반환한다.
- 실제 실행 후 backend는 `GET /api/wiki/graph`를 재조회했을 때 새 concept node와 edge를 반환한다.

#### B5. graph API가 새 relation type을 그대로 노출하는지 확인한다

이번 lint는 다음 relation type을 만들 수 있다.

```text
part_of
child_of
uses_or_depends_on
contrasts_with
supports_or_enables
```

`related_evidence`는 기본 graph edge로 materialize하지 않는다.

필요 변경:

- `WikiGraphEdge.link_type`을 enum으로 제한하고 있다면 위 값을 허용한다.
- 현재처럼 string이면 그대로 통과해도 된다.
- frontend가 link type별 스타일/라벨을 만들 수 있도록 `label`, `confidence`를 유지한다.

검증:

- `uses_or_depends_on` link가 `/api/wiki/graph`에 노출된다.
- graph edge 클릭 또는 tooltip에서 label을 볼 수 있다.

#### B6. source/concept markdown URI scope를 확인한다

llmPipeline은 MinIO object를 다음 prefix에 저장한다.

```text
wiki/{user_id}/{workspace_id}/sources/{slug}.md
wiki/{user_id}/{workspace_id}/concepts/{slug}.md
wiki/{user_id}/{workspace_id}/clusters/active.md
wiki/{user_id}/{workspace_id}/clusters/archived.md
wiki/{user_id}/{workspace_id}/logs/{yyyy-mm-dd}.md
```

필요 변경:

- `WikiService.readMarkdown()`은 이미 `s3://bucket/...`와 object key를 처리하므로 대체로 유지 가능하다.
- 단, graph/detail API가 workspace/user scope 없이 `findAll()`을 쓰면 다른 workspace page가 섞일 수 있다.

검증:

- `GET /api/wiki/pages/{wiki_page_id}`가 `s3://bucket/wiki/{user}/{workspace}/...` markdown을 읽는다.
- graph/detail이 현재 workspace 범위만 반환한다.

#### B7. WikiGraphNode.source_document 계약을 유지한다

Spring `WikiGraphNode`는 source page에 대해 `source_document`를 내려줄 수 있다.

```java
@JsonProperty("source_document") SourceDocRef sourceDocument
```

Frontend가 UUID source page id를 쓰려면 이 값이 중요하다.

필요 변경:

- `GET /api/wiki/graph`에서 source node의 `source_document.id`가 채워지는지 테스트한다.
- `document_wiki_links`의 `source_of` relation이 UUID source page와 연결되어 있어야 한다.
- source page가 없는 문서는 raw document node만 보여줄 수 있도록 frontend fallback을 허용한다.

검증:

- `wiki_page_xxx` source page id와 document id를 `source_document.id`로 매핑할 수 있다.
- `source:` prefix가 없어도 raw document node에서 source wiki node로 edge를 만들 수 있다.

#### B8. lint 실행 결과의 쓰기 범위를 backend가 인지한다

`POST /wiki/maintenance/lint` 실행은 단순 조회가 아니다.

실제 변경 가능 대상:

- `wiki_pages`: 새 concept page 생성 또는 기존 concept page markdown 갱신
- `document_wiki_links`: source document와 wiki page 연결 유지
- `wiki_page_links`: materialized core relation link 생성
- `wiki_embedding_units`, `wiki_embedding_vectors`: 새 concept/병합 evidence 검색 단위 갱신
- MinIO object:
  - `wiki/{user}/{workspace}/concepts/{slug}.md`
  - `wiki/{user}/{workspace}/clusters/active.md`
  - `wiki/{user}/{workspace}/clusters/archived.md`
  - `wiki/{user}/{workspace}/logs/{yyyy-mm-dd}.md`

필요 변경:

- backend proxy endpoint는 dry-run과 execute를 명확히 구분한다.
- execute 성공 후 graph/page/detail cache가 있다면 무효화한다.
- execute 성공 후 document list, wiki graph, selected page detail을 다시 읽을 수 있게 응답을 그대로 frontend에 전달한다.
- backend가 별도 DB transaction으로 감싸는 구조가 아니라면 FastAPI lint 실행 실패 시 부분 반영 가능성을 UI에 표시할 수 있는 에러 처리를 둔다.

검증:

- dry-run 전후 `wiki_pages`, `wiki_page_links`, MinIO cluster object가 바뀌지 않는다.
- execute 후 active cluster의 promotion section이 제거되고 archive에 promoted/merged 기록이 남는다.
- execute 후 `logs/{yyyy-mm-dd}.md`에서 promoted, merged, linked, invalid 항목을 확인할 수 있다.

### Frontend 후속 작업

#### F1. Wiki maintenance panel을 추가한다

사용자가 ingest 후 남은 proposal을 볼 수 있어야 한다.

권장 위치:

- Wiki graph 상단 toolbar
- 또는 오른쪽 agent/maintenance panel

표시 항목:

```text
Promotion candidates: N
Relation candidates: N
Invalid relations: N
Invalid promotions: N
Orphan refs: N
```

상호작용:

- `제안 확인` 버튼: backend dry-run lint 호출
- `Lint 실행` 버튼: backend actual lint 호출
- 실행 후 graph/documents refresh

검증:

- ingest 완료 후 `lint_action_available=true`이면 panel에 표시된다.
- dry-run은 page/link 수를 바꾸지 않는다.
- 실행 후 promotion candidate가 사라지고 graph에 새 concept/link가 보인다.

#### F2. promotion proposal 상세를 보여준다

사용자는 “무엇이 왜 page가 되는지”를 봐야 한다.

표시 예:

```text
cluster: anova-analysis
reason: 기존 active cluster에 근거가 누적되어 후보 승격
source_refs:
- api-inline-c36e...:B0008
- api-inline-c36e...:B0009
```

현재 lint 응답의 `promotion_candidates`는 cluster id 배열만 제공한다. 더 풍부한 UI가 필요하면 backend가 `clusters/active.md`에서 `reason/source_refs`까지 파싱해 별도 DTO로 보강하거나, llmPipeline 응답을 확장해야 한다.

최소 구현:

- cluster id list만 표시
- 실행 결과의 `materialized_promotions`에서 생성된 page link 제공

확장 구현:

- cluster reason/source_refs/evidence count 표시
- active cluster 원문 preview 제공

#### F3. materialized result를 toast/summary로 보여준다

lint 실행 후 사용자가 실제 변화량을 알 수 있어야 한다.

표시 예:

```text
생성된 concept page: 2개
- anova-analysis
- multi-objective-optimization

생성된 relation link: 1개
- anova-analysis uses_or_depends_on robust-design

archive 이동: 2개
```

검증:

- 실행 직후 summary가 보인다.
- `materialized_promotions.page_id` 클릭 시 해당 wiki page detail을 연다.
- graph refresh 후 새 node가 표시된다.

#### F4. graph edge type 표시를 확장한다

새 edge type:

```text
uses_or_depends_on
part_of
child_of
contrasts_with
supports_or_enables
```

권장 표시:

- `uses_or_depends_on`: 의존
- `part_of`: 일부
- `child_of`: 하위
- `contrasts_with`: 대비
- `supports_or_enables`: 지원

검증:

- unknown edge type도 깨지지 않고 raw label로 fallback 표시한다.
- edge label/tooltip에 backend `label`을 표시한다.

#### F5. UUID wiki page id를 전제로 한다

Frontend가 page id에서 `source:` 또는 `concept:` prefix를 파싱하면 안 된다.

현재 실제로 prefix를 가정하는 위치:

```text
frontend/app/_lib/graph.ts
- NODE_PREFIX.source = "source:"
- nodeIdToPageType()
- makeSourceId()
- buildGraphFromBackend(): node.id.startsWith("source:")
- rawSourceLinks target: source:${document.id}

frontend/app/_hooks/useTreeSelection.ts
- nodeIdToPageType(nodeId)로 pageType 판단
- openSourceBlockPreview()가 source:${documentId}에 focus

frontend/app/_components/agent-panel/AgentBody.tsx
- source_document_id -> makeSourceId(source_document_id)
```

필요 변경:

- page type은 `node.page_type`을 사용한다.
- source/concept 구분은 id가 아니라 `page_type`으로 한다.
- click/detail fetch는 opaque id 그대로 `GET /api/wiki/pages/{wiki_page_id}`에 넘긴다.
- `WikiGraphNodeResponse` 타입에 `source_document?: { id: string; filename?: string | null }`를 추가한다.
- `buildGraphFromBackend()`는 backend source node를 `source_document.id`로 찾아 raw document node와 연결한다.
- source wiki page가 아직 없을 때만 fallback node id로 `source:${document.id}` 같은 client-only id를 만들 수 있다. 이 fallback id는 backend wiki page id로 사용하면 안 된다.
- `nodeIdToPageType()`는 backend graph node map을 모르면 UUID page type을 판정할 수 없다. selection 로직은 graph node의 `kind` 또는 `page_type`을 함께 넘기는 구조로 바꾼다.
- chat related page는 `wiki_page_id`가 opaque UUID일 수 있으므로 graph node lookup으로 page type/title을 보강한다.

검증:

- `wiki_page_xxx` 형태 id인 source/concept page 모두 tree와 detail에서 열린다.
- raw document node 클릭은 document block/source preview로 열리고, source wiki node 클릭은 wiki page detail로 열린다.
- query answer의 related page card가 UUID wiki page id를 그대로 열 수 있다.

#### F6. maintenance API client 타입을 추가한다

`frontend/app/_lib/api.ts`에 lint proxy 호출 함수를 추가한다.

필요 타입:

```ts
export type WikiMaintenanceLintResponse = {
  user_id: string;
  workspace_id: string;
  active_path: string;
  cluster_count: number;
  source_ref_count: number;
  orphan_refs: string[];
  promotion_candidates: string[];
  relation_candidates: WikiRelationCandidate[];
  invalid_relations: unknown[];
  invalid_promotions: unknown[];
  materialized_promotions: WikiMaterializedPromotion[];
  merged_promotions: WikiMergedPromotion[];
  materialized_relations: WikiMaterializedRelation[];
};
```

함수:

```ts
dryRunWikiMaintenance(): Promise<WikiMaintenanceLintResponse>
runWikiMaintenance(): Promise<WikiMaintenanceLintResponse>
```

검증:

- dry-run과 실행 요청이 같은 DTO를 반환한다.
- 실행 후 `fetchBackendData()`를 다시 호출한다.

#### F7. lint 실행 후 화면 상태를 다시 동기화한다

lint execute는 graph node/link와 page markdown을 실제로 변경한다.

필요 변경:

- 실행 성공 후 graph 데이터를 재조회한다.
- 선택 중인 page가 promotion/merge/archive 영향권이면 detail markdown도 재조회한다.
- graph layout/cache key가 있다면 `wiki_page_links` 증가 또는 lint response 기준으로 갱신한다.
- 실행 결과 summary에서 `materialized_promotions`, `merged_promotions`, `materialized_relations`, `invalid_promotions`, `invalid_relations`를 모두 보여준다.
- active cluster preview를 보여주는 UI를 만들 경우 execute 후 active/archive 상태가 바뀌었음을 반영한다.

검증:

- execute 직후 새 concept node가 보인다.
- execute 직후 새 relation edge가 보인다.
- 이미 열려 있던 concept page에 evidence가 병합된 경우 markdown detail이 새 내용으로 갱신된다.
- promotion 후보가 0개가 된 post-lint dry-run 결과가 panel에 반영된다.

### Agent 작업 순서 제안

#### 1차 PR: backend contract/schema alignment

목표:

- backend가 llmPipeline 새 DB/API 계약을 깨지 않게 수용한다.

작업:

1. `wiki_pages`에 `user_id`, `workspace_id` 반영
2. repository/service query scope 추가
3. source page 삭제를 `document_wiki_links` 기반으로 변경
4. `/api/wiki/maintenance/lint` proxy 추가
5. ERD/API contract 문서 갱신
6. graph/detail 테스트 갱신
7. lint execute 후 변경되는 DB/MinIO/cache 범위 반영

검증:

```text
./gradlew test
```

#### 2차 PR: frontend maintenance UX

목표:

- 사용자가 promotion proposal과 lint 실행 결과를 볼 수 있게 한다.

작업:

1. lint API client 추가
2. maintenance panel 추가
3. dry-run/execute 버튼 추가
4. 실행 후 graph refresh
5. edge type label/tooltip 확장
6. UUID page id 가정으로 tree/detail 코드 점검
7. `WikiGraphNodeResponse.source_document` 타입 반영
8. raw document node와 source wiki node 연결 로직 수정
9. lint execute 후 graph/detail 상태 재동기화

검증:

```text
npm test
npm run lint
```

#### 3차 PR: richer proposal details

목표:

- cluster proposal의 evidence/reason/source_refs를 사람이 이해하기 쉽게 보여준다.

작업:

1. backend lint response에 structured promotion details 추가
2. frontend proposal detail drawer 추가
3. active/archive cluster preview 추가

### 완료 기준

- 문서 ingest 후 frontend에서 promotion/relation proposal 수가 보인다.
- dry-run은 graph를 변경하지 않는다.
- lint 실행 후 새 concept page와 relation edge가 graph에 반영된다.
- source page/concept page id가 UUID여도 backend/frontend가 정상 동작한다.
- document delete가 source page id prefix에 의존하지 않는다.
- workspace/user가 다른 wiki page가 graph에 섞이지 않는다.

### 이번 변경 검증 기록

로컬 단위 테스트:

```text
PYTHONPATH=llmPipeline llmPipeline/.venv/bin/python -m pytest \
  llmPipeline/tests/modules/wiki_generation/test_source_extraction_artifact.py \
  llmPipeline/tests/modules/wiki_ingestion/test_concept_index.py

14 passed
```

실제 ingest/lint 흐름 검증:

- 실행 환경: Docker 재빌드 후 DB wiki 관련 테이블 초기화
- LLM provider/model: Upstage `solar-pro2`
- user/workspace: `flow-report-user` / `flow-report-20260702-accumulate`
- 입력: 깨지는 markdown을 제외한 논문 markdown 4개
- ingest 결과:
  - source pages: 4
  - concept pages before lint: 26
  - active clusters before lint: 54
  - promotion candidates: `anova-analysis`, `multi-objective-optimization`
  - orphan refs: 없음
  - invalid relations/promotions: 없음
- lint dry-run 결과:
  - promotion 후보가 proposal로 조회됐다.
  - DB graph와 MinIO active/archive object는 변경되지 않았다.
- lint execute 결과:
  - `anova-analysis` concept page 생성
  - `multi-objective-optimization` concept page 생성
  - `anova-analysis uses_or_depends_on robust-design` relation link 생성
  - promoted cluster 2개가 active에서 제거되고 archive로 이동
  - post-lint dry-run에서 promotion 후보 0개 확인

## Query evidence source_refs 구조화

### 배경

query 답변의 같은 citation rank가 여러 원문 문서의 block을 동시에 참조할 수 있다. 기존 `source_document_id` + `source_block_ids` 구조는 document id가 하나뿐이라 `doc_a:B0001`, `doc_b:B0008` 같은 전역 ref를 정확히 표현하기 어렵다.

### 이번 작업

- `llmPipeline` query evidence에 `source_refs`를 추가했다.
- `doc_id:B0001` 전역 ref를 `{source_document_id, source_block_id}` JSON 배열로 구조화한다.
- 기존 호환 필드인 `source_document_id`, `source_block_ids`는 첫 번째 문서 기준으로 유지한다.

### 후속작업

1. Spring backend `PipelineQueryResponse`, `ChatMessageReference` 저장/조회 응답에 `source_refs`를 추가한다.
2. frontend citation 클릭 처리가 `source_refs`를 우선 사용해 rank 하나에 연결된 여러 문서 block을 모두 열거나 표시하도록 변경한다.
3. `/api/documents/{document_id}/blocks` 기반 원문 block 렌더링으로 전환해 markdown 재분할 기반 highlight 의존을 제거한다.

### 검증

```text
llmPipeline/.venv/bin/python -m pytest \
  tests/modules/query

41 passed
```

---


## Wiki 페이지 workspace 격리 미구현

### 배경

`feature/user-workspace-auth` 브랜치에서 User/Workspace/Auth 기반을 구현한 뒤, 기존 리소스(documents, wiki_pages)에 `workspace_id`/`user_id`를 연동하는 작업을 진행했다. Document는 Spring Boot가 CRUD를 전담하므로 문제없이 연동했지만, Wiki 페이지는 조사 결과 예상보다 훨씬 큰 범위의 작업으로 확인되어 이번 단계에서는 보류했다.

### 관련 문서

- `docs/spec/Fruition_MVP_Erd.md` — `wiki_pages` 테이블 정의, 소유 주체를 "Spring Boot"로 표기
- `docs/backend-architecture-decision-spring-fastapi.md` — Spring Boot와 llmPipeline 역할 분리 결정

### 현재 상태

`wiki_pages` 테이블에 실제로 새 행을 만드는 주체는 Spring Boot가 아니라 **llmPipeline(Python)** 이다.

- `WikiService`(Spring)는 `wikiPageRepository.findAll/findById/findByPageTypeAndSlug/findAllById`만 호출한다. 기존 행을 읽고 rename 등으로 수정할 뿐, `new WikiPage(...)` 생성 후 저장하는 코드가 없다.
- 실제 INSERT는 `llmPipeline/app/modules/wiki_ingestion/infrastructure/postgres_wiki_ingestion_repository.py`의 `_upsert_wiki_page()`(raw SQL, psycopg)가 담당한다. 이 파일이 `CREATE TABLE IF NOT EXISTS wiki_pages (...)` DDL도 직접 소유하고 있어, Spring 쪽 JPA 엔티티/마이그레이션과 별개로 스키마가 관리된다.
- `_upsert_wiki_page()`와 이를 호출하는 `_persist_wiki_outputs()`에는 `workspace_id`/`user_id` 개념이 전혀 없다.

### 추가로 발견한 문제: concept 페이지 ID 전역 충돌

concept 타입 Wiki 페이지 ID(`concept:{slug}`)가 **시스템 전체에서 전역으로 유니크**하게 upsert(`ON CONFLICT (id) DO UPDATE`)되고 있다. 서로 다른 workspace의 사용자가 각자 업로드한 문서에 동일한 개념(예: "머신러닝")이 등장하면, 현재 구조상 **하나의 concept 페이지로 병합**된다.

이는 컬럼 하나 추가로 해결되지 않는다. concept 페이지 생성/조회 로직 자체를 workspace 단위로 재설계해야 한다 (예: ID를 `concept:{workspace_id}:{slug}`로 바꾸거나, 별도 조인 테이블로 workspace별 concept 인스턴스를 분리하는 방식 등).

### 필요한 작업 (다음 단계에서 진행)

1. `llmPipeline`의 `wiki_pages` DDL/`_upsert_wiki_page()`/`_persist_wiki_outputs()` 호출 체인에 `workspace_id`/`user_id` 추가
2. concept 페이지 ID 전역 충돌 문제 해결 방식 결정 및 구현 (workspace 단위 격리)
3. Java 쪽 `WikiPage` 엔티티/마이그레이션에 컬럼 추가, `WikiService`/`WikiController`를 workspace 인증 기준으로 전환
4. `wiki_embedding_units`, `wiki_page_embeddings` 등 wiki 관련 나머지 테이블도 동일한 workspace 격리 적용 여부 검토

### 참고

- 이번 단계(2026-07-02)에서는 Document만 workspace 연동을 완료했다. 상세 내용은 `docs/changelog/backend.md` 참고.
