# 2026-07-15 프론트엔드 이슈

2026-06-16 ~ 2026-07-14 이슈 문서 전수 점검(머지된 PR 1~76 대조) 후 미해결 작업만 통합 이관한 문서입니다.
각 항목의 배경·계약 상세는 `docs/backlog/issue-YYYY-MM-DD.md` 원문을 참조합니다.

---

## 1. wiki API workspace 경로 마이그레이션 (최우선 — 현재 기능 깨짐)

- 백엔드 wiki API는 `/api/workspaces/{workspace_id}/wiki/...`로 이동 완료(`WikiController.java:26`), 구 경로 매핑 없음.
- 프론트는 여전히 구 경로 호출 → **현재 wiki graph/page 조회가 404로 실제 깨져 있음**:
  - `frontend/app/_lib/api.ts:155` — `apiFetch("/api/wiki/graph")`
  - `frontend/app/_lib/api.ts:178` — `/api/wiki/pages/{id}`
  - rename(`PATCH .../wiki/pages/{page_id}`) 호출은 프론트에 아예 없음.
- 선행 조건(토큰 부착, workspace 선택 상태)은 PR 68로 해소됨 — `auth.ts`의 `getAccessToken`/`getSelectedWorkspaceId` 사용.
- 상세: `docs/backlog/issue-2026-07-04.md` (전체), `docs/backlog/issue-2026-07-03.md` "프론트엔드 workspace-scoped API 마이그레이션" 표의 wiki 행.

## 2. Query 진행상황 SSE 전환

- `frontend/app/_lib/api.ts:163` `queryWiki()`가 동기 POST만 호출, `agentData.ts:4-10` 정적 3단계 표시 유지 중.
- 백엔드 비동기 run + SSE 중계는 PR 39/41로 구현 완료. 단 endpoint가 workspace 하위로 이동함:
  `POST /api/workspaces/{ws}/chat/sessions/{s}/query/runs` + `GET .../events` (SSE) — `docs/changelog/backend.md` 2026-06-20 항목 및 434행 부근 참조.
- 작업: run 생성 → EventSource 소비 → 단계별 진행 표시로 전환.
- 상세: `docs/backlog/issue-2026-06-20.md` #1, `docs/backlog/issue-2026-06-17.md` 프론트 후속 작업.

## 3. 문서 processing_state / processing_stage 표시

- 백엔드 상태 모델(starting/running/stalled/completed/failed, 60초 stalled 판정)은 PR 55로 구현 완료.
- 프론트에 `processing_state`/`processing_stage` 소비 코드 0건, 폴링 없음.
- 작업: 문서 목록/업로드 UI에서 상태 폴링 + stalled/failed 표시. 채팅 위키 run의 `processing_stage` 폴링 포함.
- 상세: `docs/backlog/issue-2026-06-26.md` frontend 구현 범위, `docs/backlog/issue-2026-07-09.md` 프론트엔드 후속.

## 4. 문서 삭제 API 연동

- `DELETE /api/.../documents/{document_id}`는 PR 55로 백엔드 구현 완료(`DocumentController.java:171`).
- 프론트에 `deleteDocument` 및 `method: "DELETE"` 호출 0건.
- 상세: `docs/backlog/issue-2026-06-26.md` 삭제 API frontend 구현 범위.

## 5. OAuth 로그인 code 교환 플로우

- `/oauth/callback` 라우트와 `POST /api/auth/oauth/exchange` 호출이 프론트에 미구현. 현재 `login/page.tsx`는 이메일/비밀번호만 지원.
- 상세: `docs/backlog/issue-2026-07-03.md` "OAuth 로그인 후 code 교환" 섹션.

## 6. wiki maintenance UI

- maintenance panel/proposal/summary UI(07-02 F1~F4), lint client 타입/재동기화(F6~F7) 전부 미구현.
- 선행 조건: backend lint proxy — `docs/issue/backend/2026-07-15.md` #1 참조.
- 상세: `docs/backlog/issue-2026-07-02.md` F1~F7.

## 7. query evidence source_refs 소비

- 프론트에 `source_refs` 소비 0건, `fetchDocumentBlocks`(`api.ts:183`)는 component에서 미사용 — 블록 기반 렌더링 전환 미완.
- 선행 조건: backend `ChatMessageReference` source_refs 노출 — `docs/issue/backend/2026-07-15.md` #2 참조.
- 상세: `docs/backlog/issue-2026-07-02.md` "Query evidence source_refs 구조화 — 후속작업".
