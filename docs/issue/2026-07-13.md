# 2026-07-13

## 조립 완료 Markdown의 선택적 Vision 평가

- 기존 PDF 복원 파이프라인 소스를 `tmp/paper_ingest/paper2/designing_axial_flux_motor_outputs/`에 복구했다.
- 기존 OCR/SLLM/Vision 복원 단계는 변경하지 않는다.
- 조립된 Markdown의 block metadata를 기준으로 SLLM이 이상 블록을 선별하고, 요청된 bbox crop만 Vision이 재검토한다.
- Vision 재시도는 `uncertain` 응답에만 적용하며 block당 최대 3회다.
- 구현 위치: `tmp/paper_ingest/paper2/designing_axial_flux_motor_outputs/scripts/evaluate_assembled_markdown.py`

### 검증

- 복구된 Python 스크립트 11개의 `py_compile`을 통과했다.
- evaluator parser/chunking/Vision 재시도 단위 테스트 3개를 통과했다.
- `Designing_and_optimizing_axial_flux_motor_for_electric_ships.md`는 84 blocks, 8 chunks로 분할됐다.
- 첫 chunk 실제 실행은 12 blocks, Vision 2 blocks 각 1회였고 로컬 CPU 컨테이너에서 2분 6초가 걸렸다.
- 실제 결과: `tmp/docling_primary_outputs_bundle 2/review_results/designing_hybrid_chunk1.json`

## 외부 document evaluator 입력 경계

- PDF 파싱, OCR, block 복원, Markdown 조립은 로컬 파이프라인에 유지한다.
- 조립 Markdown을 외부 evaluator용 `document-evaluation-job.v1` 계약으로 변환한다.
- API 설정이 없으면 `pending_external_evaluator` job만 생성하며 외부 호출을 하지 않는다.
- `DOCUMENT_EVALUATOR_LLM_ENDPOINT`, `DOCUMENT_EVALUATOR_LLM_API_KEY`, `DOCUMENT_EVALUATOR_LLM_MODEL`이 모두 설정된 경우에만 OpenAI-compatible API adapter를 사용한다.
- 구현 위치: `llmPipeline/app/modules/document_evaluation/`
- CLI 위치: `llmPipeline/document_evaluation_cli.py`

### 검증

- document evaluation 모듈 `py_compile`을 통과했다.
- job 생성, 선택적 evaluator 호출, API 미설정, 부분 설정 오류 단위 테스트 4개를 통과했다.
- 실제 논문 4개를 외부 호출 없이 `360 blocks / 33 chunks`의 pending job으로 생성했다.
- job 위치: `tmp/docling_primary_outputs_bundle 2/evaluation_jobs/*.job.json`

## canonical 복원 플로우 재실행

- `docs/current_flow.md`와 `docs/minutes.md`의 순서대로 Docling primary 84개 block을 새 작업 디렉터리에서 다시 처리했다.
- 본문/제목은 crop OCR 후보 생성 후 `qwen2.5:7b` 복구·evaluator와 `qwen2.5vl:7b` crop review를 순서대로 적용했다.
- 표/수식은 Tesseract, Paddle FormulaRecognition, pix2tex, structured parser, SLLM fallback을 적용한 뒤 Vision으로 검토했다.
- Docling 수식 crop의 오른쪽 항이 잘리지 않도록 수평/수직 여백을 `24pt`/`8pt`로 조정했다.
- 문법 evaluator를 통과한 Vision 오답 4개 수식과 표 1개는 실제 crop 근거로 `external_evaluator_crop_review`를 반영했다.
- 그림은 7개 모두 asset으로 남겼고, caption crop이 명확한 Fig. 1, 2, 6, 7만 accepted로 반영했다.

### 산출물과 검증

- Markdown: `tmp/current_restore_test/designing_original_flow_outputs/final/Designing_and_optimizing_axial_flux_motor_for_electric_ships.restored.md`
- 처리 리포트: `tmp/current_restore_test/designing_original_flow_outputs/final/Designing_and_optimizing_axial_flux_motor_for_electric_ships.restoration_report.md`
- evaluator 입력: `tmp/current_restore_test/designing_original_flow_outputs/final/Designing_and_optimizing_axial_flux_motor_for_electric_ships.evaluation_job.json`
- 수식 10/10, 표 9/9가 final에 반영됐고 display math delimiter는 20개로 균형이 맞다.
- 외부 evaluator job은 84 blocks를 7 chunks로 나누며 외부 API 호출 없이 `pending_external_evaluator`로 생성됐다.
- caption 근거가 없는 그림 3개만 `그림 caption Vision 복원 필요` 상태로 남겼다.

## 네 PDF 범용성 재검증

- 앞의 `designing_original_flow_outputs` 산출물은 crop을 사람이 보고 수정한 block이 있어 제품 범용성 지표에서 제외했다.
- 복구 코드와 prompt에 남아 있던 문서 변수명, OCR 오인식 토큰, block id, 샘플 slug 기반 규칙을 제거했다.
- 문자 깨짐 감지는 특정 단어 목록 대신 일반 text-quality 점수와 glyph-shift 후보 비교를 사용한다.
- figure block은 text-only/no-SLLM 경로에서 accepted될 수 없고 Vision 또는 외부 evaluator crop 검토를 필수로 한다.
- 범용 runner: `tmp/paper_ingest/paper2/designing_axial_flux_motor_outputs/scripts/run_document_restoration_pipeline.py`
- text quality: `tmp/paper_ingest/paper2/designing_axial_flux_motor_outputs/scripts/text_quality.py`
- 네 PDF 종합 리포트: `tmp/generalization_test/generalization_report.md`
- 문서별 final/report/evaluator job: `tmp/generalization_test/{designing,multi,iet,serial}/final/`

### 범용 deterministic 결과

| 문서 | primary blocks | 수식 accepted | 표 accepted | Vision 대기 figure |
| --- | ---: | ---: | ---: | ---: |
| Designing | 84 | 7/10 | 8/9 | 7 |
| Multi-Objective | 78 | 7/14 | 4/6 | 7 |
| IET | 135 | 7/12 | 8/9 | 19 |
| Serial-loop | 66 | 8/8 | 2/2 | 4 |

- 수동 정답 보정은 0건이다.
- 네 final 모두 display math delimiter 균형이 맞다.
- 외부 evaluator job은 API 호출 없이 모두 `pending_external_evaluator`로 생성됐다.
- `caffeinate -dimsu` 적용 후 Multi-Objective deterministic 복원은 약 2분이었다. 화면 off 후 system sleep에 진입한 실행 시간은 성능 측정에서 제외했다.

## evaluator 없는 Vision recovery 재검증

- 별도 LLM evaluator와 외부 evaluator API를 제외하고 deterministic/OCR/pix2tex 실패 block만 `qwen2.5vl:7b`로 복원했다.
- 범용 runner에 `--use-local-vision`, `--vision-model`, `--max-vision-attempts`를 추가했다.
- Vision prompt는 output 디렉터리가 아니라 공용 prompt 디렉터리를 사용하도록 경로를 수정했다.
- source/OCR/candidate evidence를 항목별 2,500자로 제한해 Ollama context 초과를 막았다.
- 수식의 누락된 display delimiter와 비숫자 equation tag placeholder는 재추론 대신 로컬 정규화한다.
- figure Vision의 샘플 문구 기반 거절 규칙을 제거하고, 깨졌거나 빈 caption만 Vision 대상으로 삼는다.
- runner는 이후 실행부터 단계별 wall time과 총 시간을 `final/{document_slug}.pipeline_timing.json`에 기록한다.
- 종합 결과와 문서별 시간: `tmp/no_evaluator_test/no_evaluator_recovery_report.md`의 `복원 결과`, `실행 시간` 섹션
- 문서별 Markdown: `tmp/no_evaluator_test/{designing,multi,iet,serial}/final/*.no_evaluator_restored.md`

### 결과

- Vision 대상 62개 중 47개 복원, 15개 실패, 총 100회 Vision 시도였다.
- 문서별 누적 Vision recovery 시간은 Designing 약 16분 22초, Multi 약 22분 36초, IET 약 18분 41초, Serial 3분 43초다.
- 네 문서 합계는 약 61분 22초이며 PDF 파싱·Docling·deterministic OCR 시간은 제외했다.
- Ollama는 `size_vram: 0`인 CPU-only 상태였다.
- IET figure는 caption이 full figure crop에 포함되지 않은 경우가 많아 11개 중 1개만 복원됐다. evaluator가 아니라 caption crop 분리가 잔여 병목이다.

## Figure caption crop 및 Mac Metal GPU 전환

- picture의 명시적 Docling caption ref를 우선 사용하고, 누락된 경우 같은 페이지의 인접 `FIGURE`/`Fig.` caption을 bbox 거리와 축 겹침으로 연결한다.
- caption bbox를 `layout/auto/assets/figure_captions/{block_id}.png`에 별도 생성한다.
- Figure Vision은 caption crop을 최우선으로 사용하며, caption crop 요청에는 full-figure OCR/SLLM 후보를 전달하지 않는다.
- 정상 source caption은 과거 Vision 결과보다 우선 조립한다.
- caption 근거가 없는 logo/subfigure 조각은 image-only로 유지한다.
- 범용 연결 결과는 Designing 7/7, Multi 7/7, IET 13/19, Serial 4/4다.
- IET final의 figure caption 실패 표시는 11개에서 0개로 줄었다. 연결 근거가 없는 6개는 image-only다.
- CPU-only `markdown-edit-ollama`를 중지하고 macOS native Ollama 0.31.2를 설치했다.
- `qwen2.5vl:7b`는 Metal에서 `size_vram=8,768,255,425`로 전체 GPU 적재됐다.
- Caption crop cold 추론 9.20초, warm 추론 2.33초, Designing 7개 batch 11.66초 및 7/7 성공을 확인했다.
- native `qwen2.5:7b`도 설치했으며 text와 Vision 모델 모두 Metal에 전체 적재됐다. 합산 VRAM은 약 15.3GB다.
- 12-block 하이브리드 evaluator chunk는 Vision 1회를 포함해 cold 22.81초, warm 6.97초였다.
- evaluator는 실제 중복 초록과 문장 중단을 선별했다. 구조만으로 확정 가능한 중복 수정은 Vision 요청에서 분리해야 한다.
- 구현·측정 상세: `tmp/no_evaluator_test/gpu_caption_recovery_report.md`
- 산출물: `tmp/no_evaluator_test/{designing,iet}/final/*.gpu_caption_restored.md`

## 최종 evaluator 본문·표 prompt 강화

- 조립 완료 Markdown evaluator가 mojibake, 혼합 문자, 토큰 파편, 문장 중단을 본문 깨짐 후보로 선별하도록 기준을 구체화했다.
- 전문용어, 고유명사, 변수명, 단위, 정상 수식 기호는 그 자체만으로 요청하지 않도록 과탐 제한을 추가했다.
- 표는 행별 셀 수, 헤더와 데이터 열 이동, 반복 행, 선형화, caption/각주 혼입을 검사한다.
- `table_candidate`도 표 전용 Vision prompt를 사용하며, 행·열 대응과 빈 셀을 유지한 Markdown 표를 요청한다.
- 최종 evaluator는 Vision 결과 block만 1:1로 판정하고, `corrected`의 transcription을 수정 없이 사용하도록 제한했다.
- Designing 첫 3 chunks 실험에서 깨진/중복 본문과 구조가 무너진 표 2개를 선별했다. 두 표의 행·열 및 값은 crop과 일치하게 복원됐다.
- `qwen2.5vl:7b`는 작은 변수 첨자를 평탄화하는 한계가 확인됐다. prompt 예시를 과도하게 강제하면 숫자에 불필요한 LaTeX가 추가되어 일반 지시만 유지했다.
- evaluator, table/text Vision, 최종 판정 prompt의 지시문은 영어로 전환했다. JSON의 `reason` 설명만 기존 리포트 호환성을 위해 한국어로 요청한다.
- 영어 prompt 첫 3 chunks 실행은 86.66초, Vision 8건이었다. 표 2개는 복원했지만 전문용어와 tight crop을 과탐해 high-precision 우선 제한을 추가했다.
- 문자 수준 OCR 근거 없는 문법·문체·전문용어 수정은 금지하고, crop이 현재 block 일부만 포함하면 `uncertain`으로 남기도록 했다.
- 7B evaluator의 허위 근거 생성을 줄이기 위해 이 pass의 선별 범위를 `[broken_text]`와 `[table_structure]`로 축소했다. 중복·수식·문법 평가는 기존 단계에 유지한다.
- `[broken_text]` 요청은 범용 glyph 검사와 language score를 함께 통과하거나 Unicode replacement/private/control 문자가 있을 때만 Vision으로 전달한다. 모델이 놓친 확정 깨짐은 로컬 검사 결과를 합친다.
- 최종 첫 3 chunks 실행은 20.93초였고, 초기 과탐 1건은 guard에서 제거되어 표 2건만 Vision으로 전달·수정됐다.
- 최종 결과: `tmp/no_evaluator_test/designing/final/Designing_and_optimizing_axial_flux_motor_for_electric_ships.english_evaluator_final.{md,json}`
- 최종 리포트: `tmp/no_evaluator_test/designing/final/Designing_and_optimizing_axial_flux_motor_for_electric_ships.english_evaluator_final_report.md`

## 문서 독립적 깨진 본문 후보 및 표 재조립

- 특정 PDF의 표명, 헤더명, 값, block id, 열 개수에 의존하는 판정은 사용하지 않는다.
- 반복 문자, 비정상 대소문자 전환, Unicode replacement/control/private 문자, glyph/language score는 본문 Vision 요청 후보 생성에만 사용한다.
- 문자 휴리스틱만으로 본문을 자동 교체하거나 Vision 결과를 거부하지 않는다.
- 유효하지 않은 `table`/`table_candidate`는 evaluator 분류와 무관하게 표 복원 후보로 추가한다.
- 표는 `PyMuPDF.find_tables(strategy="text")`의 일반 cell grid에 원본 PDF word bbox를 중심점 기준으로 다시 배치한다.
- 반복되는 본문 행 구조와 계층 헤더의 상대 위치로 leaf header path를 만들며, 로컬 조립이 불가능할 때만 Vision fallback을 사용한다.
- 구현: `tmp/paper_ingest/paper2/designing_axial_flux_motor_outputs/scripts/evaluate_assembled_markdown.py`의 `has_suspicious_text_pattern`, `assemble_markdown_table`, `restore_table_from_text_layout`
- 테스트: `tmp/paper_ingest/paper2/designing_axial_flux_motor_outputs/tests/test_evaluate_assembled_markdown.py`

### Multi-Objective 전체 재검증

- 전체 78 blocks, 8 chunks를 처리했으며 총 wall time은 214.35초다.
- 깨진 저자 정보 2개 block은 독립 crop Vision 전사로 복원됐다.
- `docling_table_p06_005`는 `method=text_layout`로 14열, 8개 데이터 행을 복원했다.
- 최종 표의 모든 행은 동일한 pipe count를 가지며 `is_valid_markdown_table` 검증을 통과했다.
- 다른 표 `docling_table_p02_002`는 구조를 확정하지 못해 자동 교체하지 않고 `unresolved`로 남겼다.
- Markdown: `tmp/no_evaluator_test/multi/final/Multi-Objective_Optimization_of_an_Axial-Flux_Magnetic_Gear_Using_Taguchi_Method.english_evaluator_full.md`
- JSON: `tmp/no_evaluator_test/multi/final/Multi-Objective_Optimization_of_an_Axial-Flux_Magnetic_Gear_Using_Taguchi_Method.english_evaluator_full.json`
- 리포트: `tmp/no_evaluator_test/multi/final/Multi-Objective_Optimization_of_an_Axial-Flux_Magnetic_Gear_Using_Taguchi_Method.english_evaluator_full_report.md`

## PDF부터 네 문서 전체 복원 및 최종 evaluator 재실행

- 기존 중간 JSON을 입력으로 재사용하지 않고 `tmp/pdfs`의 원본 PDF 4개부터 다시 실행했다.
- 범용 runner가 `--docling-json` 없이도 Docling OCR 기준 산출물을 직접 생성하도록 PDF 입력 경로를 추가했다.
- 실행 순서는 PDF → Docling OCR 기준 추출 → layout/수식 후보 탐지 → block manifest/crop → crop OCR 보강 → SLLM block 복원 → block Vision 검토 → figure Vision 복원 → `restored.md` 조립 → chunk evaluator → `evaluated.md` 확정이다.
- 파싱, OCR, block 복원, Markdown 조립은 로컬에서 수행하고 evaluator는 마지막 조립본에만 적용한다.
- evaluator는 Markdown block을 적당한 크기의 chunk로 나눠 검사하며, 이상 후보 block의 원본 crop만 Vision에 다시 요청한다.
- 문서 고유 표명, 값, block id, 고정 열 개수에 의존하는 하드코딩은 사용하지 않았다.
- 최종 evaluator의 수정은 Vision 또는 PDF text-layout 근거가 확정된 block에만 반영하고, 확정하지 못한 block은 `restored.md` 내용을 유지한다.

### Markdown 산출물 계보

1. `layout/auto/docling_ocr_baseline/docling.md`: PDF에서 새로 추출한 Docling 기준 Markdown
2. `layout/auto/docling_equation_candidates/summary.md`: 수식 후보 탐지 요약
3. `recovered_blocks/*.prompt.md`: 표·수식·그림 block별 SLLM 실제 입력과 근거
4. `recovered_blocks/*.md`: SLLM의 block별 복원 후보
5. `text_recovered/*.md`: 깨진 본문 후보를 Vision으로 재전사한 결과
6. `final/*.restored.md`: 승인된 block을 조립한 evaluator 입력본
7. `final/*.restoration_report.md`: PDF별 조립 단계 상세 기록
8. `final/*.evaluator_report.md`: chunk evaluator 판정, 수정안, 미확정 block 상세 기록
9. `final/*.evaluated.md`: evaluator가 확정한 수정까지 반영한 사용자용 최종 Markdown

- 실행 중 생성된 Markdown 243개를 모두 보존하고, 상대 경로를 유지한 수집본과 `overall_report.md`를 합쳐 244개로 정리했다.
- JSON, crop 이미지, manifest, timing JSON은 Markdown 수집본에는 복제하지 않고 원시 실행 결과에 유지했다.
- PDF별 상세 리포트는 통합 리포트와 일부 중복되지만 중간 판정 근거이므로 삭제하지 않았다.
- 실행 당시 원시 결과: `tmp/canonical_flow_run_2026-07-13/outputs/`
- 실행 당시 Markdown 수집본 및 통합 리포트: `tmp/canonical_flow_run_2026-07-13/collected_markdown/overall_report.md`
- 위 `canonical_flow_run_2026-07-13` 경로는 2026-07-14 의사록 갱신 시점에는 디스크에서 확인되지 않았다. 아래 수치와 경로는 실행 직후 검증 기록 기준이다.

### 실행 시간

| 문서 | 복원 pipeline | evaluator | 합계 |
| --- | ---: | ---: | ---: |
| Designing | 242.84초 | 262.56초 | 505.40초 |
| Multi | 508.50초 | 222.65초 | 731.15초 |
| IET | 460.70초 | 213.42초 | 674.12초 |
| Serial | 126.71초 | 98.82초 | 225.53초 |
| **전체** | **1,338.75초** | **797.45초** | **2,136.20초** |

- 정상 처리 총시간은 약 35분 36초다.
- 원본 PDF 경로가 실행 도중 사라져 실패한 evaluator 재시도 1회 20.61초를 포함하면 관측 총시간은 약 35분 57초다.
- 첫 Docling 실행에는 모델 초기 다운로드가 포함됐고, Multi의 block 복원에는 Paddle Formula 모델 초기 다운로드가 포함됐다.
- warm cache 상태의 반복 실행은 이 측정보다 짧을 수 있다.

### 복원 및 evaluator 결과

| 문서 | 평가 block | chunk | Vision 요청 | text-layout 복원 | 수정 반영 | 미확정 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Designing | 84 | 8 | 6 | 0 | 2 | 4 |
| Multi | 78 | 8 | 7 | 3 | 5 | 2 |
| IET | 134 | 12 | 15 | 8 | 13 | 2 |
| Serial | 65 | 6 | 4 | 0 | 3 | 1 |

- evaluator가 총 23개 block의 수정을 확정했다.
- Multi의 깨진 저자·ORCID 2개 block은 독립 crop Vision 전사로 복원됐다.
- Multi의 `docling_table_p06_005` TABLE V ANOVA 표는 초기 block Vision 검토에서 유효한 Markdown 표를 만들지 못했으나 최종 evaluator의 PDF text-layout 복원으로 14열 표가 반영됐다.
- IET의 `docling_table_p08_008`도 초기 Vision 결과가 거절됐지만 최종 evaluator 수정 대상으로 반영됐다.
- Serial의 복원 manifest는 66 blocks였으나 evaluator 입력에서는 footer 성격 block 하나가 제외돼 65 blocks로 집계됐다.
- evaluator·본문 품질 관련 단위 테스트 26개가 통과했고, 수정 스크립트의 `py_compile`도 통과했다.

### 잔여 미확정 block

- Designing: `docling_table_p02_001`, `docling_table_p02_002`, `docling_table_p05_007`, `docling_table_p06_009`
- Multi: `docling_table_p02_002`, `docling_table_p05_003`
- IET: `docling_text_p09_109`, `docling_text_p10_115`
- Serial: `docling_table_p04_002`
- 총 9개이며, 잘못된 추정으로 덮어쓰지 않고 기존 `restored.md` 내용을 최종 `evaluated.md`에 유지했다.
- 원본 정답 Markdown과의 자동 대조는 아니므로 이 9개와 복잡한 표·수식은 원본 PDF 기준 수동 확인이 필요하다.
