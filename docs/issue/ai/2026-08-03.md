# AI(llmPipeline) 이슈 — 2026-08-03

AI 작업 로그(저장·조회·변경분·되돌리기) Backend 구현이 끝났습니다.

> **2026-08-04 갱신.** `dev` 병합으로 확인하니 **L1·L2·L4가 이미 구현돼 있습니다.** 양쪽이 서로 다른 계약을 가정하고 병렬로 만들어 필드와 엔드포인트가 어긋나 있었고, **Backend를 llmPipeline 쪽에 맞췄습니다.** 아래에서 완료된 항목은 그렇게 표시했습니다.
>
> 남은 것은 **L3(콜백 인증 헤더)·L5(lint 확장)·L6(직렬 실행)**, 그리고 L4에 새로 생긴 **삭제 페이지의 링크·임베딩 정리**입니다.
>
> **L3이 가장 급합니다.** 콜백을 보내는 코드는 이미 있는데 `X-Internal-Token` 헤더만 빠져 Backend가 401로 거절합니다. **헤더 한 줄 때문에 ingest 결과가 하나도 들어오지 않습니다.**

---

## L1. ~~요청 스키마에 필드 2개 추가~~ — **완료**

> 2026-08-04 확인: `dev`에 구현돼 있습니다. 아래는 계약 기록용으로 남깁니다.

`app/modules/wiki_ingestion/interfaces/http/schemas.py` — `_PipelineRunBase`

```python
class _PipelineRunBase(BaseModel):
    model_config = ConfigDict(extra="forbid")   # ← 모르는 필드가 오면 422
    ...
    operation_id: str | None = None
    result_callback_url: str | None = None
```

`extra="forbid"` 때문에 지금 Backend가 `operation_id`를 보내면 **ingest 요청 전체가 422로 깨집니다.** 그래서 Backend는 `app.aihistory.ingest-logging-enabled=false`로 꺼둔 상태입니다.

이 두 필드를 받아 **실행 전 구간에 전달**해 주세요. L2의 object key와 L3의 콜백에 씁니다.

- 완료 조건: `extra="forbid"` 상태에서 Backend 요청이 422 없이 수용됨
- **2026-08-04 확인: `dev`에 이미 구현돼 있습니다.** `_PipelineRunBase`에 두 필드가 있고, 함께 오지 않으면 400으로 거절하는 검증까지 붙어 있습니다.

---

## L2. ~~본문 key 체계 변경 · 기여 조각 저장~~ — **완료**

> 2026-08-04 확인: `dev`에 구현돼 있습니다. Backend의 검증 규칙과 경로가 정확히 일치합니다. 아래는 계약 기록용으로 남깁니다.

### 2-1. 작업별 불변 key로 쓰기 (덮어쓰지 않기)

현재는 slug/document_id 기준 경로에 **매번 덮어씁니다.**

| 위치 | 현재 key |
|---|---|
| `postgres_wiki_output_persistence.py:99` | `wiki/{user_id}/{workspace_id}/sources/{document_id}.md` |
| `postgres_wiki_output_persistence.py:164` | `wiki/{user_id}/{workspace_id}/concepts/{slug}.md` |
| `postgres_wiki_ingestion_repository.py:621` | 동일 패턴 |

바꿀 형태:

```
wiki/{workspace_id}/pages/{page_id}/ops/{operation_id}.md     본문
wiki/{workspace_id}/pages/{page_id}/ops/{operation_id}.json   기여 조각
```

**덮어쓰지 않아야 하는 이유**: Backend가 콜백을 받아 그 시점 본문을 `wiki_page_versions`에 사본으로 남깁니다. 되돌릴 때는 그 사본을 새 revision으로 쌓으면 되므로 저장소를 다시 쓰지 않습니다. 다만 재조립(L4)은 **기여 조각을 다시 읽어야** 하므로, 조각만큼은 덮어쓰면 안 됩니다.

**`slug`가 아니라 `page_id`를 쓰는 이유**: slug는 rename으로 바뀝니다. 바뀌면 예전에 쓴 파일 경로를 다시 찾을 수 없습니다.

Backend는 이 경로를 **정확히 대조**한 뒤에만 읽습니다. 형식이 다르면 422로 거절합니다.

### 2-2. 기여 조각 저장 (신규)

`.json`에 **"이 문서가 이 페이지에 보탠 부분"** 을 남겨 주세요.

개념 페이지는 여러 원문 문서의 내용이 한 편의 글로 합쳐진 결과입니다. 문서 A를 되돌리려 해도 A 부분만 오려낼 수 없습니다. 그래서 되돌리기는 **남은 조각들을 다시 붙여 새로 쓰는** 방식입니다(L4).

조각이 없으면 재작성 자체가 불가능합니다. 형식은 llmPipeline이 재조립에 쓸 수 있으면 되고, Backend는 **경로만 보관**하고 내용을 해석하지 않습니다.

- 완료 조건: 저장한 조각만으로 페이지 재구성이 가능함
- **2026-08-04 확인: `dev`에 이미 구현돼 있습니다.** `operation_artifacts.py`가 `wiki/{ws}/pages/{page}/ops/{op}.{md,json}`로 남기며, Backend의 검증 규칙과 경로가 정확히 일치합니다.

### 2-3. ~~`wiki_pages` 갱신 제거~~ — **철회 (2026-08-03)**

처음에 `ON CONFLICT DO UPDATE`에서 `markdown_uri`와 `status`를 빼 달라고 요청했으나 **철회합니다.**

`wiki_pages`는 llmPipeline 소유가 맞습니다(`5f230a4`에서 rename까지 위임했습니다). Backend가 그 컬럼들을 쓰겠다는 전제가 잘못이었고, 그쪽 코드를 걷어냈습니다.

이제 Backend는:

- **현재 본문을 `wiki_page_versions`의 최신 revision에서 읽습니다.** `markdown_uri`를 보지 않으므로 llmPipeline이 자유롭게 갱신하면 됩니다. 임베딩 생성에 쓰는 그 값 그대로 두세요.
- **삭제를 `wiki_page_contributions`의 활성 기여 유무로 판단합니다.** `status='deleted'`를 쓰지 않습니다.

**대신 요청드릴 것이 하나 생겼습니다.** 복구로 페이지가 삭제되면 Backend는 기여만 끄고 `wiki_pages`와 링크 테이블을 건드리지 않습니다. 조립 지시서의 `deleted_pages`를 받아 **링크(`wiki_page_links`, `document_wiki_links`)와 임베딩 정리**를 해 주세요. 현재 복구 경로(`wiki_page_restore.py` 등)에 DB 접근이 없어 이 정리가 누락됩니다.

- 완료 조건
  - [ ] 같은 페이지를 두 번 처리해도 이전 object가 유지됨
  - [ ] `deleted_pages`의 링크와 임베딩이 정리됨
  - [ ] slug rename 이후에도 경로가 어긋나지 않음

---

## L3. [최우선] 결과 콜백에 인증 헤더 추가

> **2026-08-04 확인.** 콜백을 보내는 코드(`run_pipeline.py`, `pipeline_result_callback.py`)는 이미 있습니다. **인증 헤더만 빠져 있어 Backend가 401로 거절합니다.** 이 한 줄 때문에 ingest 결과가 하나도 들어오지 않습니다.

### 지금 상태

`pipeline_result_callback.py:63`

```python
request = urllib.request.Request(
    callback_url,
    data=body,
    headers={"Content-Type": "application/json; charset=utf-8"},   # ← X-Internal-Token 없음
    method="POST",
)
```

Backend는 이 헤더가 없으면 **401 `INVALID_CALLBACK_TOKEN`** 으로 거절하고, 저장소 객체를 열어보지도 않습니다. 콜백 경로에 인증이 없으면 아무나 결과를 위조해 남의 워크스페이스 위키를 바꿀 수 있어서 뺄 수 없는 검증입니다.

### 고칠 것

```python
headers={
    "Content-Type": "application/json; charset=utf-8",
    "X-Internal-Token": os.environ["INTERNAL_CALLBACK_TOKEN"],
}
```

토큰 값은 Backend의 `app.internal.callback-token`과 같아야 합니다. 두 서비스가 같은 `INTERNAL_CALLBACK_TOKEN` 환경변수를 공유하도록 배포 설정을 맞춰 주세요. Backend 기본값은 로컬 개발용 placeholder이며 배포 환경에서는 주입해야 합니다.

### 재시도 조건도 함께 봐 주세요

현재 재시도는 **422일 때만** 동작합니다(`pipeline_result_callback.py`). 401은 그대로 실패로 끝나 `notification_pending`에 쌓입니다.

토큰이 붙으면 401이 날 일은 없지만, **네트워크 오류·5xx도 재시도 대상**으로 넣어 두는 편이 안전합니다. 422는 "본문을 다시 쓰고 재전송"이고, 5xx는 "그대로 재전송"이라 처리가 다릅니다.

### 완료 조건

- [ ] 콜백에 `X-Internal-Token`이 실려 Backend가 200을 반환함
- [ ] Backend의 `app.aihistory.ingest-logging-enabled`를 켜도 ingest가 정상 동작함

> 이것이 풀리면 **ingest → 로그 조회 → 되돌리기 → 재조립**이 한 번에 이어집니다. 복구 지시서(L4)는 이미 맞춰져 있고 재조립 결과도 같은 콜백 경로를 씁니다.

---

### L3 참고 — 콜백 payload 계약

```http
POST {result_callback_url}
X-Internal-Token: {INTERNAL_CALLBACK_TOKEN}
```

```json
{
  "operation_id": "op_a2_7f3c9",
  "operation_type": "ingest",
  "status": "succeeded",
  "workspace_id": "ws_1",
  "user_id": "user_1",
  "target_document_id": "doc_A",
  "summary": "위키 페이지 3개를 갱신했습니다.",
  "changed_pages": [
    { "page_id": "wp_C2",
      "page_type": "concept",
      "markdown_key": "wiki/ws_1/pages/wp_C2/ops/op_a2_7f3c9.md",
      "contribution_key": "wiki/ws_1/pages/wp_C2/ops/op_a2_7f3c9.json",
      "content_hash": "sha256:3c4d..." }
  ]
}
```

**본문은 싣지 않습니다.** 어디에 썼는지와 지문만 보내면 Backend가 검증 후 읽습니다.

| 필드 | 없으면 |
|---|---|
| `operation_id` | 멱등 불가 |
| `page_id` | 기여 이력 누락 → 버전 계산이 어긋남 |
| `markdown_key` | 그 작업의 본문을 특정 못 함 |
| `contribution_key` | **재조립 불가** |
| `content_hash` | 쓰기가 잘렸는지 확인 불가 |

`workspace_id`·`user_id`·`target_document_id`는 **권한 근거로 쓰이지 않습니다.** 요청 등록 때 저장한 값과 일치하는지 대조만 합니다. 생략하면 대조하지 않습니다.

### 응답별 대응

| 응답 | 할 일 |
|---|---|
| 200 | 없음 |
| 200 (같은 `payload_hash` 재전송) | 없음 |
| 401 `INVALID_CALLBACK_TOKEN` | 토큰 확인 |
| 409 `AI_OPERATION_PAYLOAD_CONFLICT` | **중단.** 같은 작업에 다른 결과를 보냈음 |
| 422 `INVALID_CALLBACK_PAYLOAD` | 경로·해시 검증 실패. **다시 쓰고 재전송** |

응답 body는 `{ "operation_id", "status", "recorded_changes" }`입니다. `status`는 Backend가 실제로 확정한 값이라, 부분 실패면 `partially_succeeded`가 옵니다.

### 부분 실패도 만든 것은 보내주세요

`status: "failed"`로 보내더라도 **이미 만들어진 페이지는 `changed_pages`에 담아야 합니다.** 안 보내면 위키에는 페이지가 있는데 이력에 없어서, 그 페이지만 영영 복구 대상에서 빠집니다.

- 완료 조건: 성공·부분 실패·실패 각각에서 콜백 발송 / 안 바뀐 페이지는 `changed_pages`에 미포함 / 재전송 시 동일 payload

---

## L4. ~~재조립 엔드포인트 신설~~ — **이미 구현됨 (2026-08-04 확인)**

`dev` 병합으로 확인했습니다. `POST /wiki/ingest-restore-runs`와 `POST /wiki/lint-restore-runs`가 이미 있고 테스트도 갖춰져 있습니다. **Backend를 그쪽 계약에 맞췄습니다.**

Backend가 처음 가정했던 것과 달랐던 부분은 이렇게 정리했습니다.

| 항목 | 처리 |
|---|---|
| 엔드포인트 1개(`/wiki/restore-runs`) | ingest용·lint용 둘로 분리 |
| `excluded_operations` | `cancel_operation_ids`로 이름 맞춤 |
| `keep_contributions[].object_key` | **제거.** llmPipeline이 같은 규칙으로 조립하고 조각 안 식별자까지 대조함 |
| `contribution_count` | **제거.** Backend만 쓰는 값이라 `restore_manifest`에 보관 |
| `restored_pages`·`restored_from`·`user_id` | **제거.** 받지 않는 필드 |
| `restore_to_operation_id`·`source_page` | **추가.** Backend가 계획에서 계산해 채움 |

**source page는 양쪽이 각자 되돌립니다.** Backend는 `wiki_page_versions`에 새 revision을 쌓아 즉시 끝내고, llmPipeline은 자기 사본 객체를 만들어 `changed_pages`로 보고합니다. 본문이 같아 Backend가 `content_hash` 비교로 걸러내므로 revision이 중복 생기지 않습니다.

**요청드릴 것 하나가 남습니다.** 복구로 삭제된 페이지(`deleted_pages`)의 **링크(`wiki_page_links`, `document_wiki_links`)와 임베딩 정리**입니다. Backend는 기여만 끄고 그 테이블들을 건드리지 않는데, 현재 복구 경로(`wiki_page_restore.py`, `concept_contribution_rebuild.py` 등)에 DB 접근이 없어 정리가 누락됩니다.

- 완료 조건
  - [x] 지정한 조각만 사용하고 취소 대상 조각을 쓰지 않음
  - [x] 조각 유실 시 해당 페이지만 `failed_pages`로 보고
  - [ ] `deleted_pages`의 링크와 임베딩이 정리됨

---

## L5. lint 확장

`POST /wiki/maintenance/lint` 요청에 `operation_id`, 응답에 `changed_pages`를 더해 주세요.

lint는 **기여를 만들지 않습니다.** 이미 있는 글을 다듬는 것이라 새로 보태는 문서가 없습니다. 그래서 `contribution_key`가 없고 `contribution_count`도 올라가지 않습니다. 버전만 하나 올라갑니다.

`dry_run=true`면 `changed_pages`는 빈 배열입니다.

> 현재 Backend의 lint 연동도 없습니다(`WikiMaintenanceService`가 권한만 확인하고 그대로 위임). 응답에 `changed_pages`가 생기면 Backend가 로그 기록을 붙입니다.

- 완료 조건: `dry_run=true`면 빈 배열 / 실제 변경 시 페이지 목록과 `content_hash` 반환

---

## L6. workspace별 ingest 직렬 실행 보장

같은 워크스페이스의 ingest 2건이 동시에 실행되면, 같은 페이지에 대한 콜백이 동시에 도착해 revision 채번이 겹칠 수 있습니다.

> Backend 쪽에도 같은 성격의 미구현 항목이 있습니다(ingest 적재 시 행 잠금). 양쪽 중 하나만 있어도 대부분 막히지만, 1차 구현에서는 llmPipeline의 직렬 실행이 더 단순합니다.

- 완료 조건: 같은 workspace의 ingest 2건이 동시에 실행되지 않음

---

## 참고

- API 계약 전문: `docs/spec/api/ai-operation-log.md` — §4 llmPipeline 연동, §4.6 object key 검증
- 설계 배경: `docs/design/ai-operation-log.md` — §4 통신 계약, §5 복구 규칙
- 작업 계획: `docs/design/ai-operation-log-tasks.md` — TASK-101~106
- Backend 코드
  - `backend/src/main/java/fruition/aihistory/controller/OperationCallbackController.java` (콜백 수신)
  - `backend/src/main/java/fruition/aihistory/service/WikiObjectReader.java` (key prefix 검증 규칙)
  - `backend/src/main/java/fruition/aihistory/repository/PipelineRestoreRequester.java` (조립 지시서 형식)
  - `backend/src/main/resources/db/migration/V16__add_wiki_page_versions.sql`, `V17__add_wiki_page_deleted_status.sql`
- changelog: `docs/changelog/backend.md` 2026-07-31 ~ 2026-08-03

## 관련 이슈

- 프론트엔드: `docs/issue/frontend/2026-08-03.md` — 화면 연동. 위키 작업은 L1~L3이 끝나야 목록에 나타납니다.
